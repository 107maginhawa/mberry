---
phase: 14
plan: 14-02
title: Fix event form BigInt + invitation_token orgId + P0/P1 sweep
wave: 1
depends_on: []
files_modified:
  - apps/memberry/src/features/events/components/event-form.tsx
  - services/api-ts/src/handlers/invite/repos/invite.schema.ts
autonomous: true
verify: test
---

# Plan 14-02: Fix event form BigInt + invitation_token orgId + P0/P1 sweep

## Objective

Fix event form BigInt serialization bug, add organizationId to invitation_token table, verify all 18 P0/P1 items resolved.

## Tasks

<task id="1">
<title>Fix event form BigInt serialization</title>
<read_first>
- apps/memberry/src/features/events/components/event-form.tsx
</read_first>
<action>
At line 85 of event-form.tsx, replace:
```typescript
registrationFee: BigInt(Math.round(parseFloat(form.registrationFee || '0') * 100)),
```
with:
```typescript
registrationFee: Math.round(parseFloat(form.registrationFee || '0') * 100),
```

BigInt serializes to a JSON string, but the API expects a number. Math.round already returns an integer.
</action>
<acceptance_criteria>
- event-form.tsx does NOT contain `BigInt` on the registrationFee line
- event-form.tsx contains `Math.round(parseFloat(form.registrationFee` without BigInt wrapper
</acceptance_criteria>
</task>

<task id="2">
<title>Add organizationId to invitation_token table</title>
<read_first>
- services/api-ts/src/handlers/invite/repos/invite.schema.ts
- services/api-ts/src/handlers/association:member/repos/membership.schema.ts (reference for organizationId pattern)
</read_first>
<action>
In invite.schema.ts, add an `organizationId` column to the invitation_token table:

```typescript
organizationId: uuid('organization_id').references(() => organizations.id, { onDelete: 'cascade' }),
```

Import the organizations table reference. Add an index on organizationId. Follow the same pattern as other tables that have organizationId (e.g., membership.schema.ts).

Then generate the migration:
```bash
cd services/api-ts && bun run db:generate
```
</action>
<acceptance_criteria>
- invite.schema.ts contains `organizationId` column definition
- invite.schema.ts contains `.references(() => organizations.id`
- A new migration file exists in `src/generated/migrations/` for the invitation_token change
</acceptance_criteria>
</task>

<task id="3">
<title>P0/P1 tracking sweep</title>
<read_first>
- docs/audits/EXISTING_CODEBASE_ADOPTION_AUDIT.md (Risk Assessment section)
- docs/ver-3/GAP-BACKLOG.md
</read_first>
<action>
Review all 18 P0/P1 items from the audit and verify each is resolved:

P0 items (7 total — all should be DONE):
1. 2FA secrets plaintext → Fixed in P0 Group 2
2. castVote no validation → Fixed in Phase 12/13
3. uploadFile no MIME allowlist → Fixed in security waves
4. 36% tables not org-scoped → Fixed (15 tables got organizationId)
5. session tokens plaintext → Fixed in P0 Group 2
6. email verification disabled → Fixed in security waves
7. audit log no organizationId → Fixed in P0-7

P1 items (11 total — verify 15/18 complete claim):
8-18. Check each against git log

Update the audit report's risk assessment section to mark all items as RESOLVED with commit references. If any item is NOT resolved, flag it.
</action>
<acceptance_criteria>
- docs/audits/EXISTING_CODEBASE_ADOPTION_AUDIT.md Risk Assessment section shows all P0 items as RESOLVED
- No P0 items remain open
- GAP-BACKLOG.md reflects current state
</acceptance_criteria>
</task>

## must_haves
- Event create/update form submits registrationFee as number (not BigInt)
- invitation_token table has organizationId with FK to organizations
- All 18 P0/P1 audit items verified resolved or explicitly tracked
