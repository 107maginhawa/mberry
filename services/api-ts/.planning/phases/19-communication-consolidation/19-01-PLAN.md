---
phase: 19
plan: 19-01
title: Merge communications schema+repo into communication module
wave: 1
depends_on: []
files_modified:
  - services/api-ts/src/handlers/communication/repos/communication.schema.ts
  - services/api-ts/src/handlers/communication/repos/communication.repo.ts
  - services/api-ts/src/handlers/communication/archiveAnnouncement.ts
  - services/api-ts/src/handlers/communication/createAnnouncement.ts
  - services/api-ts/src/handlers/communication/deleteAnnouncement.ts
  - services/api-ts/src/handlers/communication/getAnnouncement.ts
  - services/api-ts/src/handlers/communication/listAnnouncements.ts
  - services/api-ts/src/handlers/communication/publishAnnouncement.ts
  - services/api-ts/src/handlers/communication/updateAnnouncement.ts
autonomous: true
verify: test
---

# Plan 19-01: Merge communications into communication module

## Objective

Merge `communications/` schema and repo into `communication/` module. Update all imports. The handler files already exist in `communication/` — they just import from the wrong location (`../communications/repos/`).

## Tasks

<task id="1">
<title>Merge announcement schema into communication.schema.ts</title>
<read_first>
- services/api-ts/src/handlers/communications/repos/communications.schema.ts
- services/api-ts/src/handlers/communication/repos/communication.schema.ts
</read_first>
<action>
Copy the following from `communications/repos/communications.schema.ts` into `communication/repos/communication.schema.ts`:

1. The `announcementStatusEnum` pgEnum definition
2. The `announcementVisibilityEnum` pgEnum definition  
3. The `announcements` pgTable definition (with all columns, indexes, FKs)
4. The `announcementStats` pgTable definition
5. All type exports: `Announcement`, `NewAnnouncement`, `AnnouncementStats`
6. Any required imports (e.g., `persons` table reference for FK)

Keep existing communication schema content intact. Add announcement tables at the bottom.
</action>
<acceptance_criteria>
- communication.schema.ts contains `announcements` table definition
- communication.schema.ts contains `announcementStats` table definition
- communication.schema.ts exports `Announcement`, `NewAnnouncement`, `AnnouncementStats` types
- communication.schema.ts contains both announcement enums
</acceptance_criteria>
</task>

<task id="2">
<title>Merge CommunicationsRepository into communication.repo.ts</title>
<read_first>
- services/api-ts/src/handlers/communications/repos/communications.repo.ts
- services/api-ts/src/handlers/communication/repos/communication.repo.ts
</read_first>
<action>
Copy all announcement-related methods from `CommunicationsRepository` class into the existing `CommunicationRepository` class in `communication/repos/communication.repo.ts`.

Methods to merge: all methods that operate on the `announcements` and `announcementStats` tables (create, update, delete, list, get, publish, archive, stats methods).

Update table references to use the local schema import (from `./communication.schema` instead of `../../communications/repos/communications.schema`).
</action>
<acceptance_criteria>
- communication.repo.ts contains all announcement CRUD methods
- communication.repo.ts imports announcement tables from local schema
- No imports from `../communications/` remain in communication.repo.ts
</acceptance_criteria>
</task>

<task id="3">
<title>Update all handler imports from communications to communication</title>
<read_first>
- services/api-ts/src/handlers/communication/archiveAnnouncement.ts
- services/api-ts/src/handlers/communication/createAnnouncement.ts
</read_first>
<action>
In ALL 7 announcement handler files in `communication/`:
- archiveAnnouncement.ts
- createAnnouncement.ts
- deleteAnnouncement.ts
- getAnnouncement.ts
- listAnnouncements.ts
- publishAnnouncement.ts
- updateAnnouncement.ts

Replace imports from `'../communications/repos/communications.repo'` with `'./repos/communication.repo'`.
Replace imports from `'../communications/repos/communications.schema'` with `'./repos/communication.schema'`.

Also check test files in communication/ for the same import pattern and update.
</action>
<acceptance_criteria>
- No file in `communication/` imports from `../communications/`
- All announcement handlers import from `./repos/communication.repo` or `./repos/communication.schema`
- `grep -r "communications/repos" services/api-ts/src/handlers/communication/` returns empty
</acceptance_criteria>
</task>

<task id="4">
<title>Update seed file and any other external references</title>
<read_first>
- services/api-ts/src/core/seed-rich.ts (or whatever seed file imports from communications)
</read_first>
<action>
Search for ALL imports from `communications/repos/communications.schema` or `communications/repos/communications.repo` across the entire `services/api-ts/src/` directory:

```bash
grep -r "communications/repos" services/api-ts/src/ --include='*.ts' -l
```

Update each to import from `communication/repos/communication.schema` or `communication/repos/communication.repo` instead.

Also check:
- `services/api-ts/src/app.ts` for route registration
- Any generated files that reference communications
</action>
<acceptance_criteria>
- `grep -r "from.*communications/repos" services/api-ts/src/` returns empty (excluding the communications/ directory itself which will be deleted)
- Seed file imports announcement tables from communication module
</acceptance_criteria>
</task>

<task id="5">
<title>Move BR test files and delete communications directory</title>
<read_first>
- services/api-ts/src/handlers/communications/br-35.feed-moderation.test.ts
- services/api-ts/src/handlers/communications/br-40.survey-anonymity.test.ts
</read_first>
<action>
1. Move BR test files from `communications/` to `communication/`:
```bash
mv services/api-ts/src/handlers/communications/br-35.feed-moderation.test.ts services/api-ts/src/handlers/communication/
mv services/api-ts/src/handlers/communications/br-40.survey-anonymity.test.ts services/api-ts/src/handlers/communication/
```

2. Update any imports in moved test files to use local paths.

3. Delete the `communications/` directory:
```bash
rm -rf services/api-ts/src/handlers/communications/
```

4. Run full test suite to verify nothing breaks.
</action>
<acceptance_criteria>
- `services/api-ts/src/handlers/communications/` directory does NOT exist
- BR test files exist in `communication/` directory
- `bun test` passes with 0 failures
</acceptance_criteria>
</task>

## must_haves
- All announcement functionality preserved in communication module
- communications/ directory deleted
- No broken imports anywhere
- All tests pass
