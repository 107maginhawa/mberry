// Curated icon set for the design system. Re-exported from lucide-react (a
// `packages/ui` dependency) so apps consume icons through the design system
// rather than each adding their own lucide dependency. Add icons here as
// screens need them — keep the list small and intentional (DESIGN.md: labeled
// icons only, never decorative noise).
export {
  Users,
  Calendar,
  Settings,
  Upload,
  Wallet,
  Megaphone,
  CreditCard,
  ChevronRight,
  Download,
} from "lucide-react"
