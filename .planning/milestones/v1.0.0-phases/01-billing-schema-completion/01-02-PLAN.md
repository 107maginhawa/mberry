---
phase: 01-billing-schema-completion
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - services/api-ts/src/handlers/billing/accessControl.test.ts
  - services/api-ts/src/handlers/billing/lifecycle.test.ts
  - services/api-ts/src/handlers/billing/listInvoices.test.ts
autonomous: true
requirements: [BILL-04]
must_haves:
  truths:
    - "E2E test proves admin can create, finalize, pay, and void an invoice in sequence"
    - "E2E test proves non-admin customer can only read their own invoices"
    - "E2E test proves non-admin cannot create/finalize/delete invoices"
    - "E2E test proves void is blocked after voidThresholdMinutes"
    - "listInvoices test proves all response fields are populated from DB"
  artifacts:
    - path: "services/api-ts/src/handlers/billing/lifecycle.test.ts"
      provides: "Full invoice lifecycle test (create->finalize->pay->void)"
      contains: "describe('Invoice Lifecycle'"
    - path: "services/api-ts/src/handlers/billing/accessControl.test.ts"
      provides: "Access control tests for admin/customer roles"
      contains: "describe('Billing Access Control'"
    - path: "services/api-ts/src/handlers/billing/listInvoices.test.ts"
      provides: "Response field completeness tests"
      contains: "describe('listInvoices'"
  key_links:
    - from: "services/api-ts/src/handlers/billing/lifecycle.test.ts"
      to: "services/api-ts/src/handlers/billing/createInvoice.ts"
      via: "handler import"
      pattern: "import.*createInvoice"
    - from: "services/api-ts/src/handlers/billing/accessControl.test.ts"
      to: "services/api-ts/src/handlers/billing/listInvoices.ts"
      via: "handler import"
      pattern: "import.*listInvoices"
---

<objective>
Write TDD tests covering the full invoice lifecycle (create -> finalize -> pay -> void), access control enforcement, and response field completeness.

Purpose: BILL-04 (E2E tests verify full invoice lifecycle). Refund is out of scope per D-05.
Output: 3 test files proving billing module correctness with all access controls and field mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-billing-schema-completion/01-CONTEXT.md
@.planning/phases/01-billing-schema-completion/01-RESEARCH.md
@.planning/phases/01-billing-schema-completion/01-PATTERNS.md
@.planning/phases/01-billing-schema-completion/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write lifecycle + access control + response field tests (RED then GREEN)</name>
  <files>
    services/api-ts/src/handlers/billing/lifecycle.test.ts,
    services/api-ts/src/handlers/billing/accessControl.test.ts,
    services/api-ts/src/handlers/billing/listInvoices.test.ts
  </files>
  <read_first>
    services/api-ts/src/handlers/billing/createInvoice.test.ts,
    services/api-ts/src/handlers/billing/voidInvoice.test.ts,
    services/api-ts/src/handlers/billing/createInvoice.ts,
    services/api-ts/src/handlers/billing/listInvoices.ts,
    services/api-ts/src/handlers/billing/getInvoice.ts,
    services/api-ts/src/handlers/billing/finalizeInvoice.ts,
    services/api-ts/src/handlers/billing/voidInvoice.ts,
    services/api-ts/src/handlers/billing/repos/billing.repo.ts
  </read_first>
  <behavior>
    **lifecycle.test.ts:**
    - Test 1: Admin creates invoice with lineItems -> returns 201 with invoiceNumber, status='draft'
    - Test 2: Admin finalizes invoice -> status='open', paymentDueAt populated
    - Test 3: Admin captures payment -> status='paid', paidAt populated, paidBy set
    - Test 4: Admin voids paid invoice (within threshold) -> status='void', voidedAt populated, voidedBy set
    - Test 5: Admin cannot void invoice after voidThresholdMinutes passes -> 422 VOID_THRESHOLD_EXCEEDED
    - Test 6: Cannot finalize already-open invoice -> error (per D-04 state machine)
    - Test 7: Cannot void draft invoice -> error (must be open per D-04)

    **accessControl.test.ts:**
    - Test 1: Non-admin non-merchant cannot create invoice -> 403
    - Test 2: Non-admin non-merchant cannot finalize invoice -> 403
    - Test 3: Non-admin non-merchant cannot delete invoice -> 403
    - Test 4: Merchant can create their own invoice -> 201
    - Test 5: Customer can read their own invoice (getInvoice) -> 200
    - Test 6: Customer cannot read another customer's invoice -> 403
    - Test 7: Admin can read any invoice -> 200

    **listInvoices.test.ts:**
    - Test 1: Response includes paymentCaptureMethod (not null/hardcoded)
    - Test 2: Response includes lineItems array with description, quantity, unitPrice, amount
    - Test 3: Response includes paidBy, voidedBy, authorizedAt, authorizedBy when set
    - Test 4: Non-admin only sees invoices where customer=self or merchant=self
    - Test 5: Admin sees all invoices regardless of customer/merchant
  </behavior>
  <action>
Follow existing test pattern from createInvoice.test.ts (mock injection via buildApp pattern).

**lifecycle.test.ts:**
- Import pattern: `import { describe, test, expect } from 'bun:test'`
- Use `makeSession(userId, 'admin')` for admin actions
- Mock `InvoiceRepository.prototype` methods to simulate state transitions
- For void threshold test: mock `invoice.paidAt` to be older than `voidThresholdMinutes`
- For state machine tests: mock invoice with wrong status and verify error

**accessControl.test.ts:**
- Use `makeSession(RANDOM_USER_ID, 'user')` for non-admin, non-merchant
- Use `makeSession(MERCHANT_ID, 'user')` for merchant-self
- Use `makeSession(CUSTOMER_ID, 'user')` for customer reads
- Verify ForbiddenError (403) responses for unauthorized access

**listInvoices.test.ts:**
- Mock repo to return invoices with ALL fields populated (non-null)
- Assert response body includes each field with correct value (not null)
- Mock line items batch fetch to return items linked to invoice
- For customer-scoped test: mock session as non-admin, verify only own invoices returned

Use fixture constants:
```typescript
const MERCHANT_ID = 'merch-uuid-1111-1111-1111-111111111111';
const CUSTOMER_ID = 'cust-uuid-2222-2222-2222-222222222222';
const OTHER_CUSTOMER = 'other-uuid-4444-4444-4444-444444444444';
const ADMIN_ID = 'admin-uuid-5555-5555-5555-555555555555';
```
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/billing/lifecycle.test.ts src/handlers/billing/accessControl.test.ts src/handlers/billing/listInvoices.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lifecycle.test.ts exists and contains `describe('Invoice Lifecycle'`
    - lifecycle.test.ts contains at least 5 `test(` calls
    - lifecycle.test.ts contains `VOID_THRESHOLD_EXCEEDED`
    - accessControl.test.ts exists and contains `describe('Billing Access Control'`
    - accessControl.test.ts contains `expect(resp.status).toBe(403)` (at least 3 occurrences)
    - listInvoices.test.ts contains `paymentCaptureMethod` assertion
    - listInvoices.test.ts contains `lineItems` assertion
    - All 3 test files pass: `bun test src/handlers/billing/lifecycle.test.ts src/handlers/billing/accessControl.test.ts src/handlers/billing/listInvoices.test.ts` exits 0
  </acceptance_criteria>
  <done>All lifecycle, access control, and response field tests pass, proving BILL-04 (full invoice lifecycle coverage minus refund per D-05)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> handler | Tests validate that trust boundaries are enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | lifecycle.test.ts | mitigate | Test proves customer-scoped read rejects cross-customer access |
| T-01-08 | Elevation of Privilege | accessControl.test.ts | mitigate | Test proves non-admin/non-merchant gets 403 on write endpoints |
| T-01-09 | Tampering | lifecycle.test.ts | mitigate | Test proves void threshold blocks late voids, state machine blocks invalid transitions |
</threat_model>

<verification>
```bash
# All new test files exist
ls services/api-ts/src/handlers/billing/lifecycle.test.ts services/api-ts/src/handlers/billing/accessControl.test.ts services/api-ts/src/handlers/billing/listInvoices.test.ts

# All tests pass
cd services/api-ts && bun test src/handlers/billing/ -x

# Test count (expect 15+ test cases across 3 files)
grep -c "test(" services/api-ts/src/handlers/billing/lifecycle.test.ts services/api-ts/src/handlers/billing/accessControl.test.ts services/api-ts/src/handlers/billing/listInvoices.test.ts
```
</verification>

<success_criteria>
- 3 new test files exist and pass
- lifecycle.test.ts covers: create, finalize, pay, void, threshold block, invalid transitions
- accessControl.test.ts covers: admin bypass, merchant self-service, customer read-only, cross-customer block
- listInvoices.test.ts covers: all response fields populated, line items included, customer-scoped filtering
- `bun test src/handlers/billing/` passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-billing-schema-completion/01-02-SUMMARY.md`
</output>
