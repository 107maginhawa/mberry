---
phase: 08-frontend-unit-tests
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-07]

must_haves:
  truths:
    - "CI unit-tests job runs frontend vitest alongside backend bun test"
    - "Frontend unit test failure blocks the CI pipeline"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Frontend unit test step in CI"
      contains: "cd apps/memberry && bun run test"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "apps/memberry/package.json"
      via: "bun run test script"
      pattern: "cd apps/memberry && bun run test"
---

<objective>
Add a frontend unit test step to the CI workflow so Memberry vitest runs alongside existing backend tests.

Purpose: Completes TEST-07 success criteria #3 -- "Unit tests run in CI alongside E2E and contract tests."
Output: Updated ci.yml with frontend unit test step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-frontend-unit-tests/08-RESEARCH.md
@.planning/phases/08-frontend-unit-tests/08-01-SUMMARY.md

<interfaces>
From .github/workflows/ci.yml (unit-tests job, current state):
```yaml
  unit-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres: ...
      minio: ...
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - name: Build OpenAPI spec
        run: bun --filter @monobase/api-spec run build
      - name: Generate API codegen
        run: bun --filter @monobase/api-ts run generate
      - name: Run unit tests
        run: cd services/api-ts && bun test
```

The `unit-tests` job has Postgres/MinIO services for the backend tests. Frontend unit tests do not need them but adding to the same job is harmless and avoids a separate checkout+install cycle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend unit test step to CI unit-tests job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>
    In the `unit-tests` job, after the existing "Run unit tests" step (`cd services/api-ts && bun test`), add a new step:

    ```yaml
      - name: Run frontend unit tests
        run: cd apps/memberry && bun run test
    ```

    This runs `vitest run` (the updated test script from Plan 01) in CI. The OpenAPI spec and codegen steps already run before this, so `@monobase/sdk-ts` and `@monobase/api-spec` workspace deps resolve correctly.

    Do NOT create a separate job -- reuse the existing `unit-tests` job. The Postgres/MinIO services are harmless overhead (unused by vitest) and save a full checkout+install cycle.

    Do NOT modify any other jobs (lint-typecheck, e2e, build-api, build-frontends, contract).
  </action>
  <verify>
    <automated>grep -A 2 "Run frontend unit tests" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains a step named "Run frontend unit tests"
    - That step runs `cd apps/memberry && bun run test`
    - Step appears after the existing "Run unit tests" step in the unit-tests job
  </acceptance_criteria>
  <done>CI workflow runs frontend vitest alongside backend tests in the unit-tests job</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- CI configuration only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | N/A | CI config | accept | No security surface -- adds a test step to existing workflow |
</threat_model>

<verification>
1. `grep "Run frontend unit tests" .github/workflows/ci.yml` returns a match
2. `grep "cd apps/memberry && bun run test" .github/workflows/ci.yml` returns a match
3. The step is inside the `unit-tests` job (not in e2e or other jobs)
</verification>

<success_criteria>
Frontend unit tests will run in CI on every PR and push to main, alongside existing backend unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-unit-tests/08-03-SUMMARY.md`
</output>
