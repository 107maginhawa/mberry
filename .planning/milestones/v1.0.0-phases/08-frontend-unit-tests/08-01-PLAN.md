---
phase: 08-frontend-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/memberry/package.json
  - apps/memberry/vitest.config.ts
  - apps/memberry/src/test/setup.ts
  - apps/memberry/src/test/utils.tsx
  - apps/memberry/src/features/dues/lib/money.test.ts
  - apps/memberry/src/features/dues/lib/fund-math.test.ts
  - apps/memberry/src/features/chapters/lib/royalty-split.test.ts
  - apps/memberry/src/features/directory/lib/visibility.test.ts
  - apps/memberry/src/features/invite/lib/token-validation.test.ts
  - apps/memberry/src/features/profile/lib/profile-display.test.ts
  - apps/memberry/src/features/membership/lib/membership-status.test.ts
autonomous: true
requirements: [TEST-07]

must_haves:
  truths:
    - "vitest run exits 0 with all existing lib tests passing"
    - "All test files import from vitest, not bun:test"
    - "renderWithProviders utility exists for component test use"
  artifacts:
    - path: "apps/memberry/vitest.config.ts"
      provides: "Vitest config extending vite.config.ts with happy-dom"
      contains: "environment: 'happy-dom'"
    - path: "apps/memberry/src/test/setup.ts"
      provides: "jest-dom matchers import"
      contains: "@testing-library/jest-dom"
    - path: "apps/memberry/src/test/utils.tsx"
      provides: "renderWithProviders helper with QueryClientProvider"
      contains: "QueryClientProvider"
  key_links:
    - from: "apps/memberry/vitest.config.ts"
      to: "apps/memberry/vite.config.ts"
      via: "mergeConfig"
      pattern: "mergeConfig.*viteConfig"
    - from: "apps/memberry/package.json"
      to: "vitest"
      via: "test script"
      pattern: "vitest run"
---

<objective>
Install Vitest, configure it with happy-dom environment, create test utilities, and migrate all 6 existing lib tests from bun:test to vitest imports.

Purpose: Establishes the test runner foundation so component tests (Plan 02) and CI (Plan 03) can build on it.
Output: Working vitest setup with all existing tests passing under the new runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-unit-tests/08-RESEARCH.md

<interfaces>
From apps/memberry/vite.config.ts:
```typescript
import { defineConfig } from 'vite'
import viteReact from '@vitejs/plugin-react'
import tsConfigPaths from 'vite-tsconfig-paths'
import { tanstackRouter } from '@tanstack/router-plugin/vite'

export default defineConfig({
  server: { port: 3004, proxy: { '/api': { ... } } },
  plugins: [tsConfigPaths({ ignoreConfigErrors: true }), tanstackRouter({ ... }), viteReact()],
  build: { outDir: './dist', emptyOutDir: true, minify: 'esbuild' },
})
```

From apps/memberry/package.json (current test script):
```json
"test": "bun test src/"
```

Existing lib test files (all import from `bun:test` except fund-math.test.ts which imports from `vitest`):
- src/features/dues/lib/money.test.ts
- src/features/dues/lib/fund-math.test.ts
- src/features/chapters/lib/royalty-split.test.ts
- src/features/directory/lib/visibility.test.ts
- src/features/invite/lib/token-validation.test.ts
- src/features/profile/lib/profile-display.test.ts
- src/features/membership/lib/membership-status.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest + jest-dom, create config and test utilities</name>
  <files>
    apps/memberry/package.json
    apps/memberry/vitest.config.ts
    apps/memberry/src/test/setup.ts
    apps/memberry/src/test/utils.tsx
  </files>
  <read_first>
    apps/memberry/package.json
    apps/memberry/vite.config.ts
    apps/memberry/tsconfig.json
  </read_first>
  <action>
    1. Install vitest and @testing-library/jest-dom as devDependencies:
       `cd apps/memberry && bun add -d vitest @testing-library/jest-dom`

    2. Create `apps/memberry/vitest.config.ts`:
       - Import `defineConfig, mergeConfig` from `vitest/config`
       - Import `viteConfig` from `./vite.config`
       - Use `mergeConfig(viteConfig, defineConfig({ test: { globals: true, environment: 'happy-dom', setupFiles: ['./src/test/setup.ts'], include: ['src/**/*.test.{ts,tsx}'], exclude: ['tests/e2e/**', 'node_modules/**'] } }))` as default export
       - The `mergeConfig` pattern inherits tsconfig-paths plugin so `@/` and `@monobase/ui` resolve correctly

    3. Create `apps/memberry/src/test/setup.ts`:
       - Single line: `import '@testing-library/jest-dom'`

    4. Create `apps/memberry/src/test/utils.tsx`:
       - Export `renderWithProviders(ui: ReactNode)` that wraps with `QueryClientProvider` using a fresh `QueryClient({ defaultOptions: { queries: { retry: false } } })`
       - Import `QueryClient, QueryClientProvider` from `@tanstack/react-query`
       - Import `render` from `@testing-library/react`
       - Re-export `screen, within, waitFor` from `@testing-library/react` for convenience

    5. Update `apps/memberry/package.json` scripts:
       - Change `"test"` to `"vitest run"`
       - Add `"test:watch": "vitest"`
       - Keep all existing e2e scripts unchanged
  </action>
  <verify>
    <automated>cd apps/memberry && bun run test -- --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists with `environment: 'happy-dom'`
    - src/test/setup.ts exists with jest-dom import
    - src/test/utils.tsx exists with `QueryClientProvider`
    - package.json `test` script is `vitest run`
  </acceptance_criteria>
  <done>vitest runs without errors (may show 0 or more passing tests at this point)</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 6 existing lib tests from bun:test to vitest imports</name>
  <files>
    apps/memberry/src/features/dues/lib/money.test.ts
    apps/memberry/src/features/chapters/lib/royalty-split.test.ts
    apps/memberry/src/features/directory/lib/visibility.test.ts
    apps/memberry/src/features/invite/lib/token-validation.test.ts
    apps/memberry/src/features/profile/lib/profile-display.test.ts
    apps/memberry/src/features/membership/lib/membership-status.test.ts
  </files>
  <read_first>
    apps/memberry/src/features/dues/lib/money.test.ts
    apps/memberry/src/features/dues/lib/fund-math.test.ts
    apps/memberry/src/features/chapters/lib/royalty-split.test.ts
    apps/memberry/src/features/directory/lib/visibility.test.ts
    apps/memberry/src/features/invite/lib/token-validation.test.ts
    apps/memberry/src/features/profile/lib/profile-display.test.ts
    apps/memberry/src/features/membership/lib/membership-status.test.ts
  </read_first>
  <action>
    In each of the 6 files that import from `bun:test`, replace:
    ```
    import { describe, test, expect } from 'bun:test'
    ```
    with:
    ```
    import { describe, test, expect } from 'vitest'
    ```

    fund-math.test.ts already imports from `vitest` -- leave it unchanged.

    After migration, run `cd apps/memberry && bun run test` to verify all 7 lib tests pass under vitest.

    If any test uses bun-specific APIs (like `mock` from `bun:test`), replace with vitest equivalent (`vi.fn()`, `vi.mock()`).
  </action>
  <verify>
    <automated>cd apps/memberry && bun run test 2>&1 | grep -E "(Tests|PASS|FAIL|✓|×)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Zero files contain `from 'bun:test'` under apps/memberry/src/
    - `cd apps/memberry && bun run test` exits 0 with all 7 lib tests passing
  </acceptance_criteria>
  <done>All 7 existing lib tests pass under vitest runner with no bun:test imports remaining</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- test infrastructure only. No user input, no auth, no network calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | N/A | Test infra | accept | No security surface -- test runner config and test utilities only |
</threat_model>

<verification>
1. `cd apps/memberry && bun run test` exits 0
2. `grep -r "from 'bun:test'" apps/memberry/src/` returns nothing
3. `test -f apps/memberry/vitest.config.ts && echo "exists"` prints "exists"
4. `test -f apps/memberry/src/test/utils.tsx && echo "exists"` prints "exists"
</verification>

<success_criteria>
Vitest configured, all 7 existing lib tests pass, test utility helpers ready for component tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-unit-tests/08-01-SUMMARY.md`
</output>
