---
phase: 10-deploy-platform-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - DEVX-02
must_haves:
  truths:
    - "Staging deploy job runs real Railway + Cloudflare commands (no TODO placeholders)"
    - "Production deploy job runs real Railway + Cloudflare commands (no TODO placeholders)"
    - "Health checks target real endpoints after deploy"
    - "Required secrets are documented for GitHub repository setup"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Complete deploy workflow with Railway API + Cloudflare Pages commands"
      contains: "railway up"
  key_links:
    - from: ".github/workflows/deploy.yml deploy-staging"
      to: "Railway staging service"
      via: "railway up with RAILWAY_TOKEN secret"
      pattern: "RAILWAY_TOKEN"
    - from: ".github/workflows/deploy.yml deploy-staging"
      to: "Cloudflare Pages"
      via: "wrangler pages deploy with CLOUDFLARE_API_TOKEN"
      pattern: "wrangler pages deploy"
user_setup:
  - service: railway
    why: "API + Postgres hosting"
    env_vars:
      - name: RAILWAY_TOKEN
        source: "Railway Dashboard -> Account Settings -> Tokens -> Create Token"
    dashboard_config:
      - task: "Create staging project with Postgres addon"
        location: "Railway Dashboard -> New Project"
      - task: "Create production project with Postgres addon"
        location: "Railway Dashboard -> New Project"
      - task: "Set RAILWAY_TOKEN as GitHub Actions secret for staging and production environments"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> Environment secrets"
  - service: cloudflare
    why: "Frontend static hosting (CDN)"
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: "Cloudflare Dashboard -> My Profile -> API Tokens -> Create Token (Edit Cloudflare Pages)"
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "Cloudflare Dashboard -> any domain -> Overview sidebar (Account ID)"
    dashboard_config:
      - task: "Create 6 Cloudflare Pages projects (3 staging + 3 production)"
        location: "Cloudflare Dashboard -> Pages -> Create a project -> Direct Upload"
      - task: "Set CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID as GitHub Actions secrets"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> Environment secrets"
  - service: github
    why: "Environment variables for health check URLs"
    env_vars:
      - name: STAGING_API_URL
        source: "Railway staging project URL (e.g., https://memberry-staging.up.railway.app)"
      - name: PRODUCTION_API_URL
        source: "Railway production project URL"
    dashboard_config:
      - task: "Set STAGING_API_URL as GitHub Actions variable on staging environment"
        location: "GitHub repo -> Settings -> Environments -> staging -> Environment variables"
      - task: "Set PRODUCTION_API_URL as GitHub Actions variable on production environment"
        location: "GitHub repo -> Settings -> Environments -> production -> Environment variables"
---

<objective>
Replace all 4 TODO placeholder blocks in deploy.yml with real Railway (API) and Cloudflare Pages (frontends) deploy commands.

Purpose: Complete the deploy pipeline so merges to main auto-deploy to staging, and manual dispatch deploys to production.
Output: Working deploy.yml with real commands for Railway + Cloudflare Pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace TODO blocks with Railway + Cloudflare deploy commands</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (the file being modified — read in full)
  </read_first>
  <action>
Replace all 4 TODO blocks in deploy.yml with real deploy commands. Per user decision: Railway for API, Cloudflare Pages for frontends.

**Staging API deploy (replace lines ~124-129):**
```yaml
      - name: Deploy API to staging
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          npm install -g @railway/cli
          railway up --service memberry-api --environment staging --image ${{ needs.build.outputs.image_tag }}
```

**Staging frontend deploy (replace lines ~131-134):**
```yaml
      - name: Deploy frontends to staging
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          npm install -g wrangler
          wrangler pages deploy apps/memberry/dist/ --project-name memberry-staging --branch main
          wrangler pages deploy apps/admin/dist/ --project-name memberry-admin-staging --branch main
          wrangler pages deploy apps/account/dist/ --project-name memberry-account-staging --branch main
```

**Production API deploy (replace lines ~176-181):**
```yaml
      - name: Deploy API to production
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          npm install -g @railway/cli
          railway up --service memberry-api --environment production --image ${{ needs.build.outputs.image_tag }}
```

**Production frontend deploy (replace lines ~183-186):**
```yaml
      - name: Deploy frontends to production
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          npm install -g wrangler
          wrangler pages deploy apps/memberry/dist/ --project-name memberry-production --branch main
          wrangler pages deploy apps/admin/dist/ --project-name memberry-admin-production --branch main
          wrangler pages deploy apps/account/dist/ --project-name memberry-account-production --branch main
```

Add a comment block at the top of the file (after `name: Deploy`) documenting required secrets:
```yaml
# Required GitHub Secrets (per environment: staging, production):
#   RAILWAY_TOKEN          — Railway API token (Account Settings -> Tokens)
#   CLOUDFLARE_API_TOKEN   — Cloudflare API token (Edit Pages permission)
#   CLOUDFLARE_ACCOUNT_ID  — Cloudflare account ID
#
# Required GitHub Variables (per environment):
#   STAGING_API_URL        — Railway staging URL (e.g., https://memberry-staging.up.railway.app)
#   PRODUCTION_API_URL     — Railway production URL
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry && grep -v '^#' .github/workflows/deploy.yml | grep -c 'TODO' | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TODO" .github/workflows/deploy.yml returns 0
    - grep -c "railway up" .github/workflows/deploy.yml returns 2 (staging + production)
    - grep -c "wrangler pages deploy" .github/workflows/deploy.yml returns 6 (3 apps x 2 envs)
    - grep -c "RAILWAY_TOKEN" .github/workflows/deploy.yml returns at least 2
    - grep -c "CLOUDFLARE_API_TOKEN" .github/workflows/deploy.yml returns at least 2
    - grep -c "CLOUDFLARE_ACCOUNT_ID" .github/workflows/deploy.yml returns at least 2
  </acceptance_criteria>
  <done>All 4 TODO blocks replaced with real Railway + Cloudflare commands. Zero TODO references remain. Secrets documented in file header comment.</done>
</task>

<task type="auto">
  <name>Task 2: Validate workflow YAML syntax</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (verify after Task 1 edits)
  </read_first>
  <action>
Validate the modified deploy.yml is valid YAML and valid GitHub Actions syntax:

1. Run `npx yaml-lint .github/workflows/deploy.yml` or `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"` to verify YAML parses without errors.
2. Verify the workflow structure: `on`, `jobs`, each job has `runs-on`, `steps`, correct `needs` references.
3. Verify no dangling references: `needs.build.outputs.image_tag` is still correctly referenced in both deploy jobs.
4. Verify environment blocks are present: `environment: staging` and `environment: production` on their respective jobs.

If any syntax issues found, fix them in deploy.yml.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry && python3 -c "import yaml; y=yaml.safe_load(open('.github/workflows/deploy.yml')); assert 'deploy-staging' in y['jobs']; assert 'deploy-production' in y['jobs']; print('YAML valid')"</automated>
  </verify>
  <acceptance_criteria>
    - python3 yaml.safe_load parses deploy.yml without exception
    - deploy-staging job exists with environment: staging
    - deploy-production job exists with environment: production
    - needs.build.outputs.image_tag referenced in both deploy jobs
  </acceptance_criteria>
  <done>deploy.yml is valid YAML, valid GitHub Actions structure, all job references intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> Railway | Deploy token crosses to external platform |
| GitHub Actions -> Cloudflare | API token crosses to external CDN |
| Secrets store -> workflow | Secrets injected via environment variables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | S (Spoofing) | RAILWAY_TOKEN | mitigate | Token scoped to project, stored as GitHub environment secret (not repo-level), rotatable |
| T-10-02 | S (Spoofing) | CLOUDFLARE_API_TOKEN | mitigate | Token scoped to "Edit Pages" permission only, stored as environment secret |
| T-10-03 | I (Information Disclosure) | deploy.yml | mitigate | No secrets hardcoded in workflow file; all via ${{ secrets.* }} references |
| T-10-04 | T (Tampering) | Docker image | accept | Image tagged by SHA, same image promoted staging->production (already implemented in build job) |
| T-10-05 | E (Elevation) | production deploy | mitigate | Production environment requires manual workflow_dispatch (not auto-triggered on push) |
</threat_model>

<verification>
1. Zero TODO blocks remain in deploy.yml
2. Railway deploy commands present for both staging and production
3. Cloudflare Pages deploy commands present for all 3 apps in both environments
4. Secrets documentation comment block at top of file
5. YAML syntax validates cleanly
6. Health checks unchanged and still reference correct endpoints
</verification>

<success_criteria>
- deploy.yml contains real Railway + Cloudflare commands with no placeholders
- All required secrets/variables documented in file header
- Workflow YAML parses without errors
- Staging auto-deploys on push to main, production requires manual dispatch
</success_criteria>

<output>
After completion, create `.planning/phases/10-deploy-platform-decision/10-01-SUMMARY.md`
</output>
