---
phase: 15-domain-remediation
plan: "07"
type: tdd
wave: 2
depends_on: []
files_modified:
  - services/api-ts/src/handlers/association:member/createCandidate.ts
  - services/api-ts/src/handlers/association:member/createCandidate.test.ts
autonomous: true
requirements: [BR-34]
must_haves:
  truths:
    - "Non-member cannot be nominated"
    - "Suspended/terminated member cannot be nominated"
    - "Nomination outside nomination period rejected"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/createCandidate.test.ts"
      provides: "Unit tests enforcing BR-34 nomination eligibility"
    - path: "services/api-ts/src/handlers/association:member/createCandidate.ts"
      provides: "Handler with eligibility checks before candidate creation"
---

<objective>
Fix BR-34 bypass in createCandidate — add nomination eligibility checks for active membership, good standing, and nomination period.
</objective>

<context>
See 15-CONTEXT.md for key business rules and file inventory.
See 15-PATTERNS.md for test infrastructure and transaction patterns.

Bug: `services/api-ts/src/handlers/association:member/createCandidate.ts:24` creates a candidate without checking BR-34 nomination eligibility. Any authenticated user can be nominated regardless of membership status or nomination period.
</context>

<tasks>
## RED phase
1. Create `createCandidate.test.ts` with failing tests:
   - Non-member nomination returns 403/422
   - Suspended member nomination rejected
   - Terminated member nomination rejected
   - Nomination outside nomination period returns 422
   - Happy path: active member within nomination period succeeds

## GREEN phase
2. In `createCandidate.ts`, before creating candidate:
   - Look up nominee's membership in the org — reject if not found
   - Check membership status via `computeMembershipStatus` — reject if not active/good standing
   - Look up election — verify current date is within nomination period
3. Run tests — all pass

## REFACTOR phase
4. Extract eligibility check into a reusable utility if pattern repeats across election handlers
5. Verify 2123+ tests still pass
</tasks>
