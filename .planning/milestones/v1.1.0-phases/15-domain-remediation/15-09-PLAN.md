---
phase: 15-domain-remediation
plan: "09"
type: tdd
wave: 2
depends_on: []
files_modified:
  - services/api-ts/src/handlers/association:member/castBallot.ts
  - services/api-ts/src/handlers/association:member/castBallot.test.ts
autonomous: true
requirements: [BR-33, BR-34]
must_haves:
  truths:
    - "Non-member cannot vote"
    - "Cannot vote for nominee not in this election"
    - "Cannot vote for nominee running for different position"
    - "Cannot vote twice for same position (app-level + DB should both check)"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/castBallot.test.ts"
      provides: "Unit tests enforcing voter/nominee/position validation"
    - path: "services/api-ts/src/handlers/association:member/castBallot.ts"
      provides: "Handler with voter eligibility, nominee-election, and nominee-position checks"
---

<objective>
Fix missing voter eligibility, nominee-election consistency, and nominee-position validation in castBallot.
</objective>

<context>
See 15-CONTEXT.md for key business rules and file inventory.
See 15-PATTERNS.md for test infrastructure and transaction patterns.

Bug: `services/api-ts/src/handlers/association:member/castBallot.ts:35` has no voter eligibility check (any authenticated user can vote), no check that the nominee belongs to this election, and no validation that the nominee is running for the voted position.
</context>

<tasks>
## RED phase
1. Create `castBallot.test.ts` with failing tests:
   - Non-member of org tries to vote — 403
   - Suspended member tries to vote — 403
   - Vote for nominee not in this election — 422
   - Vote for nominee running for different position than voted — 422
   - Duplicate vote for same position — 409
   - Happy path: active member votes for valid nominee in correct position — 200

## GREEN phase
2. In `castBallot.ts`:
   - Look up voter's membership — reject if not active member
   - Look up nominee — verify nominee.electionId matches this election
   - Verify nominee.positionId matches the voted position
   - Check for existing vote by this voter for this position — reject duplicate
3. Run tests — all pass

## REFACTOR phase
4. Ensure DB-level unique constraint (plan 15-13) will backstop the app-level duplicate check
5. Verify 2123+ tests still pass
</tasks>
