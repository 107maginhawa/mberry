---
phase: 15-domain-remediation
plan: "10"
type: tdd
wave: 2
depends_on: []
files_modified:
  - services/api-ts/src/handlers/association:member/certifyElection.ts
  - services/api-ts/src/handlers/association:member/certifyElection.test.ts
autonomous: true
requirements: [BR-33]
must_haves:
  truths:
    - "Non-President officer cannot certify election"
    - "Cannot certify election still in votingOpen state"
    - "President can certify closed election"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/certifyElection.test.ts"
      provides: "Unit tests enforcing President-only certification and state checks"
    - path: "services/api-ts/src/handlers/association:member/certifyElection.ts"
      provides: "Handler with President role check and votingClosed state requirement"
---

<objective>
Fix certifyElection — require President position and votingClosed state before certification.
</objective>

<context>
See 15-CONTEXT.md for key business rules and file inventory.
See 15-PATTERNS.md for test infrastructure and transaction patterns.

Bug: `services/api-ts/src/handlers/association:member/certifyElection.ts:27` can publish from votingOpen directly with no President-only check. Any authenticated user can certify any election in any state.
</context>

<tasks>
## RED phase
1. Create `certifyElection.test.ts` with failing tests:
   - Non-officer tries to certify — 403
   - Officer (non-President) tries to certify — 403
   - President certifies election in votingOpen state — 422
   - President certifies election in nomination state — 422
   - President certifies election in votingClosed state — 200

## GREEN phase
2. In `certifyElection.ts`:
   - Look up caller's officer term — verify position is "President"
   - Check election state — must be 'votingClosed'
   - Only then proceed with certification
3. Run tests — all pass

## REFACTOR phase
4. Consider extracting position-check utility for reuse in plan 15-11
5. Verify 2123+ tests still pass
</tasks>
