---
phase: 15-domain-remediation
plan: "11"
type: tdd
wave: 2
depends_on: ["07", "08", "09", "10"]
files_modified:
  - services/api-ts/src/handlers/association:member/createElection.ts
  - services/api-ts/src/handlers/association:member/openElectionVoting.ts
  - services/api-ts/src/handlers/association:member/closeElectionVoting.ts
  - services/api-ts/src/handlers/association:member/certifyElection.ts
  - services/api-ts/src/handlers/association:member/createCandidate.ts
  - services/api-ts/src/handlers/association:member/castBallot.ts
  - services/api-ts/src/handlers/association:member/election-role-enforcement.test.ts
autonomous: true
requirements: [BR-33, BR-34]
must_haves:
  truths:
    - "Regular member cannot create election"
    - "Regular member cannot open/close voting"
    - "Officer can manage elections for their org"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/election-role-enforcement.test.ts"
      provides: "Cross-handler role enforcement tests for all election operations"
    - path: "services/api-ts/src/handlers/association:member/createElection.ts"
      provides: "Handler with officer role requirement"
---

<objective>
Add role enforcement (requireOfficerTerm or requirePosition) to all election handlers that currently lack it.
</objective>

<context>
See 15-CONTEXT.md for key business rules and file inventory.
See 15-PATTERNS.md for test infrastructure and transaction patterns.

Bug: Almost no role enforcement across election handlers. Any authenticated user can create elections, open voting, close voting, etc. Plans 07-10 fix specific handlers; this plan sweeps all remaining election handlers for consistent role checks.

Depends on plans 07-10 so that individual handler fixes land first, and this plan provides the uniform enforcement layer.
</context>

<tasks>
## RED phase
1. Create `election-role-enforcement.test.ts` with failing tests per handler:
   - createElection: regular member rejected, officer succeeds
   - openElectionVoting: regular member rejected, officer succeeds
   - closeElectionVoting: regular member rejected, officer succeeds
   - createCandidate: verify officer check exists (from plan 07)
   - certifyElection: verify President check exists (from plan 10)
   - castBallot: verify active member check exists (from plan 09)

## GREEN phase
2. Add `requireOfficerTerm` or equivalent check to:
   - createElection — require officer of the org
   - openElectionVoting — require officer (if not already added in plan 08)
   - closeElectionVoting — require officer
3. Reuse position-check utility from plan 10 where applicable
4. Run tests — all pass

## REFACTOR phase
5. Consolidate role-check patterns into shared middleware or utility
6. Verify 2123+ tests still pass
</tasks>
