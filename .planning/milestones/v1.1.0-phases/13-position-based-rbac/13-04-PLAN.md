---
phase: 13-position-based-rbac
plan: "04"
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - services/api-ts/src/app.ts
autonomous: true
requirements: [REQ-03, REQ-06]
must_haves:
  truths:
    - "Treasurer + President can access dues dashboard route"
    - "Society Officer + President can access credit compliance route"
    - "President only can PUT org profile"
    - "Shared read routes (GET org-profile, members, applications, officer-terms) remain accessible to any officer"
    - "Position-specific RED tests from Plan 13-01 now pass (GREEN)"
  artifacts:
    - path: "services/api-ts/src/app.ts"
      provides: "Position-restricted inline officer routes"
      contains: "requirePosition"
  key_links:
    - from: "services/api-ts/src/app.ts"
      to: "services/api-ts/src/utils/officer-check.ts"
      via: "import requirePosition"
      pattern: "import.*requirePosition.*from.*officer-check"
---

<objective>
Wire `requirePosition` to the 3 position-restricted inline routes in app.ts and run the full position-rbac test suite to verify GREEN.

Purpose: Complete the backend position-based RBAC wiring. After this plan, all position-restricted endpoints enforce the correct position per D-01. The RED tests from Plan 13-01 should now pass.

Output: app.ts updated with inline `requirePosition` calls, all position-rbac tests GREEN.
</objective>

<execution_context>
@/Users/elad-mini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elad-mini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/elad-mini/Desktop/memberry/.planning/PROJECT.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-CONTEXT.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-PATTERNS.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-01-SUMMARY.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-02-SUMMARY.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-03-SUMMARY.md

<interfaces>
From services/api-ts/src/utils/officer-check.ts:
```typescript
export async function requirePosition(ctx: BaseContext, allowedTitles: string[]): Promise<Response | null>
```

From services/api-ts/src/utils/position-titles.ts:
```typescript
export const POSITION_TITLES = {
  PRESIDENT: 'President',
  TREASURER: 'Treasurer',
  SECRETARY: 'Secretary',
  SOCIETY_OFFICER: 'Society Officer',
} as const;
```

app.ts routes to modify (line numbers approximate):
| Route | Line | Position Restriction |
|-------|------|---------------------|
| GET /credit-compliance/:orgId | ~272 | Society Officer, President |
| PUT /membership/org-profile/:orgId | ~322 | President only |
| GET /dues/dashboard/:orgId | ~381 | Treasurer, President |

app.ts routes to KEEP as-is (any officer):
| Route | Line | Reason |
|-------|------|--------|
| GET /officer-terms/:orgId | ~134 | Shared read (D-01) |
| GET /membership/org-profile/:orgId | ~313 | Shared read (D-01) |
| GET /membership/members/:orgId | ~334 | Shared read (D-01) |
| GET /membership/applications/:orgId | ~360 | Shared read (D-01) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requirePosition to 3 position-restricted app.ts inline routes</name>
  <files>services/api-ts/src/app.ts</files>
  <read_first>
    - services/api-ts/src/app.ts (full file — find exact line numbers for the 3 routes and the import section)
    - services/api-ts/src/utils/officer-check.ts (confirm requirePosition export)
    - services/api-ts/src/utils/position-titles.ts (confirm POSITION_TITLES export)
  </read_first>
  <action>
**Step 1 — Add imports** near the existing `officerAuthMiddleware` import (around line 50):
```typescript
import { requirePosition } from '@/utils/officer-check';
import { POSITION_TITLES } from '@/utils/position-titles';
```

**Step 2 — Add requirePosition inside 3 inline handlers:**

**Route 1: GET /credit-compliance/:orgId (~line 272)**
Add as first lines inside the async handler function body:
```typescript
const denied = await requirePosition(ctx, [POSITION_TITLES.SOCIETY_OFFICER, POSITION_TITLES.PRESIDENT]);
if (denied) return denied;
```
Keep `officerAuthMiddleware()` in the middleware chain (it runs first as route middleware, then requirePosition runs inside handler).

**Route 2: PUT /membership/org-profile/:orgId (~line 322)**
Add as first lines inside the async handler function body:
```typescript
const denied = await requirePosition(ctx, [POSITION_TITLES.PRESIDENT]);
if (denied) return denied;
```

**Route 3: GET /dues/dashboard/:orgId (~line 381)**
Add as first lines inside the async handler function body:
```typescript
const denied = await requirePosition(ctx, [POSITION_TITLES.TREASURER, POSITION_TITLES.PRESIDENT]);
if (denied) return denied;
```

**DO NOT modify these shared-read routes (keep officerAuthMiddleware only):**
- GET /officer-terms/:orgId (~line 134) — any officer
- GET /membership/org-profile/:orgId (~line 313) — any officer read
- GET /membership/members/:orgId (~line 334) — any officer
- GET /membership/applications/:orgId (~line 360) — any officer

Per D-01: "Any officer: org profile (read), member list (read)" — shared read stays as requireOfficerTerm via officerAuthMiddleware.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && grep -c "requirePosition" src/app.ts</automated>
  </verify>
  <acceptance_criteria>
    - app.ts contains `import { requirePosition } from '@/utils/officer-check'`
    - app.ts contains `import { POSITION_TITLES } from '@/utils/position-titles'`
    - grep -c "requirePosition" src/app.ts returns 6 (2 import lines + 3 function calls + 1 denied check line... actually just count the function call references: 3 calls of requirePosition in handler bodies)
    - The GET /credit-compliance route handler body starts with requirePosition check for SOCIETY_OFFICER, PRESIDENT
    - The PUT /membership/org-profile route handler body starts with requirePosition check for PRESIDENT
    - The GET /dues/dashboard route handler body starts with requirePosition check for TREASURER, PRESIDENT
    - GET /officer-terms, GET /membership/org-profile, GET /membership/members, GET /membership/applications do NOT contain requirePosition
  </acceptance_criteria>
  <done>3 position-restricted inline routes in app.ts upgraded. 4 shared-read routes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Run position-rbac tests — verify GREEN</name>
  <files>services/api-ts/src/tests/position-rbac.test.ts</files>
  <read_first>
    - services/api-ts/src/tests/position-rbac.test.ts (review test expectations to confirm they match the wiring done in Plans 13-02, 13-03, and this plan)
  </read_first>
  <action>
Run the position-rbac test suite. All tests from Plan 13-01 should now pass (GREEN) since all handlers and app.ts routes are wired with requirePosition.

```bash
cd services/api-ts && bun test src/tests/position-rbac.test.ts
```

**If any test fails:**
1. Check if the failing test is for a handler that was NOT correctly wired in Plans 13-02/13-03
2. Check if the app.ts route was not correctly wired in Task 1 above
3. Check if the test endpoint URL matches the actual route registration
4. Fix the handler or test and re-run

**Also run the full test suite** to verify no regressions:
```bash
cd services/api-ts && bun test
```

If any Phase 12 route-protection tests regressed, investigate — requirePosition should be a strict superset of requireOfficerTerm (officers with correct position still pass, only wrong-position officers are now blocked).
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/tests/position-rbac.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/tests/position-rbac.test.ts` exits 0 (all tests pass)
    - `bun test` (full suite) exits 0 (no regressions)
    - Output shows all position-rbac describe blocks passing
  </acceptance_criteria>
  <done>All position-rbac tests GREEN. Full test suite passes with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> app.ts inline handler | Position check inside handler after officerAuthMiddleware |
| officerAuthMiddleware -> requirePosition | Two-layer check: middleware confirms officer, handler confirms position |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Elevation of Privilege | app.ts inline routes | mitigate | 3 position-restricted routes get requirePosition calls; 4 shared-read routes stay any-officer per D-01 |
| T-13-09 | Elevation of Privilege | shared read routes | accept | GET routes for org-profile, members, applications, officer-terms accessible to any officer — intentional per D-01 shared read access |
| T-13-10 | Elevation of Privilege | missing position check on future endpoints | mitigate | RED-phase tests catch missing guards; any new officer endpoint must add a position-rbac test |
</threat_model>

<verification>
- `bun test src/tests/position-rbac.test.ts` exits 0
- `bun test` (full suite) exits 0
- `grep -c "requirePosition" services/api-ts/src/app.ts` >= 3
</verification>

<success_criteria>
- All 3 position-restricted app.ts routes wired with correct requirePosition calls
- All 4 shared-read routes left unchanged (any officer access)
- All position-rbac tests pass (GREEN)
- Full test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/13-position-based-rbac/13-04-SUMMARY.md`
</output>
