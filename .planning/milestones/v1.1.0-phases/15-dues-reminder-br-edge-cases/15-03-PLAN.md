---
phase: 15
plan: "03"
title: "Integration Verification"
wave: 2
autonomous: true
files_modified:
  - apps/memberry/src/routes/_authenticated/org/$orgId/officer/payments/index.tsx
  - services/api-ts/src/handlers/dues/jobs/reminderProcessor.integration.test.ts
---

<objective>
Verify frontend works with the now-real backend. Fix response shape mismatches.
Add integration test for the full reminder flow.
</objective>

<tasks>

## Task 1: Verify generateDuesInvoicesForOrg response shape

Read `generated/openapi/validators.ts` for the exact GenerateDuesInvoicesForOrg response type.
Compare with what the handler now returns (from 15-01).
Compare with what the frontend reads in `officer/payments/index.tsx`.

If mismatch: fix the handler response to match the OpenAPI-generated type.

## Task 2: Add integration test for reminder flow

File: `services/api-ts/src/handlers/dues/jobs/reminderProcessor.integration.test.ts` (NEW)

Test the full flow:
1. Setup: create org, dues config, reminder schedules, member with expiry date
2. Execute: call processDuesReminders (or the handler)
3. Assert: notification created, reminder log exists
4. Re-run: assert idempotent (no duplicate notifications)

## Task 3: Fix frontend response shape if needed

If Task 1 found mismatches in `officer/payments/index.tsx`:
- Update the onSuccess callback to read the correct response fields
- Verify toast message uses correct count

## Task 4: Run full test suite

```bash
cd services/api-ts && bun test
bun run --filter '*' typecheck
```

Confirm 0 regressions. Report final test count.

</tasks>

<verification>
- [ ] Response shape matches OpenAPI spec
- [ ] Integration test passes
- [ ] Frontend reads correct response fields
- [ ] Full test suite passes with 0 regressions
</verification>
