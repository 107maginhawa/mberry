---
phase: 34-wave-g1-stabilization
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/api-ts/src/handlers/elections/updateElectionStatus.test.ts
  - apps/memberry/e2e/elections-nomination.spec.ts
autonomous: true
requirements: [V-07, BR-34]

must_haves:
  truths:
    - "updateElectionStatus min-candidate check has dedicated test coverage"
    - "BR-34 nomination eligibility has E2E test verifying the full flow"
    - "E2E test covers: create election → add nominees → verify eligibility rejection for ineligible member"
  artifacts:
    - path: "services/api-ts/src/handlers/elections/updateElectionStatus.test.ts"
      provides: "Tests for INSUFFICIENT_CANDIDATES error on votingOpen transition"
      contains: "INSUFFICIENT_CANDIDATES"
    - path: "apps/memberry/e2e/elections-nomination.spec.ts"
      provides: "E2E test for nomination eligibility flow"
      contains: "nomination eligibility"
---

<objective>
Close test gaps for election integrity (V-07) and nomination eligibility (BR-34).

V-07 is already RESOLVED in code — `updateElectionStatus.ts:32-41` has the min 2-candidate check using `countNomineesByPosition`. But no dedicated test exists for this specific guard. Need test coverage.

BR-34 has comprehensive backend tests in `createNominee.test.ts` (conditions 1-3 covered) but no E2E test. The BR coverage matrix shows BR-34 as PARTIAL because E2E column = 0.

TDD approach:
1. RED: Write test for min-candidate check in updateElectionStatus
2. GREEN: Tests pass immediately (code exists)
3. Write E2E test for nomination eligibility flow
</objective>

<tasks>
1. **Write unit tests for updateElectionStatus min-candidate guard**
   - Test: transition to votingOpen with 0 nominees → throws INSUFFICIENT_CANDIDATES
   - Test: transition to votingOpen with 1 nominee per position → throws INSUFFICIENT_CANDIDATES
   - Test: transition to votingOpen with 2+ nominees per position → succeeds
   - Test: transition to other statuses (nominationsOpen, cancelled) → no candidate check
   File: `services/api-ts/src/handlers/elections/updateElectionStatus.test.ts`

2. **Write E2E test for nomination eligibility flow (BR-34)**
   - Navigate to elections page
   - Create election with nominations open
   - Attempt to nominate ineligible member (verify rejection)
   - Nominate eligible member (verify success)
   - Verify nominee appears in candidates list
   File: `apps/memberry/e2e/elections-nomination.spec.ts`

3. **Verify all election tests pass**
   Run: `cd services/api-ts && bun test handlers/elections/`
</tasks>
