---
phase: 34-wave-g1-stabilization
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - specs/api/src/modules/dues.tsp
  - services/api-ts/src/handlers/association:member/createDuesConfig.ts
  - services/api-ts/src/handlers/association:member/dues-config.test.ts
autonomous: true
requirements: [V-10]

must_haves:
  truths:
    - "gracePeriodDays has maximum value of 90 enforced at TypeSpec level"
    - "Generated Zod validator includes .lte(90) constraint"
    - "createDuesConfig rejects gracePeriodDays > 90 with 400"
    - "updateDuesConfig rejects gracePeriodDays > 90 with 400"
    - "Tests cover boundary values: 0, 30 (default), 90 (max), 91 (rejected)"
  artifacts:
    - path: "specs/api/src/modules/dues.tsp"
      provides: "@maxValue(90) constraint on gracePeriodDays"
      contains: "maxValue(90)"
    - path: "services/api-ts/src/handlers/association:member/dues-config.test.ts"
      provides: "Tests for grace period range validation"
      contains: "gracePeriodDays"
---

<objective>
Add upper bound validation (max 90 days) to gracePeriodDays in dues config (V-10).

Currently `gracePeriodDays` has `@minValue(0)` in TypeSpec generating `z.number().int().gte(0)` but no upper bound. Per BR-02, grace period should be 0-90 days. Values above 90 are nonsensical (3 months is already generous).

TDD approach:
1. RED: Write tests asserting 400 for gracePeriodDays > 90
2. GREEN: Add @maxValue(90) to TypeSpec, rebuild, verify generated validator includes .lte(90)
3. Verify existing tests pass with valid values
</objective>

<tasks>
1. **RED — Write failing tests for grace period upper bound**
   - Test: `gracePeriodDays: 91` → 400 validation error
   - Test: `gracePeriodDays: 365` → 400 validation error
   - Test: `gracePeriodDays: 90` → 201 (boundary, accepted)
   - Test: `gracePeriodDays: 0` → 201 (minimum, accepted)
   File: `services/api-ts/src/handlers/association:member/dues-config.test.ts`

2. **GREEN — Add @maxValue(90) to TypeSpec dues config**
   - Find `gracePeriodDays` in `specs/api/src/modules/dues.tsp`
   - Add `@maxValue(90)` decorator alongside existing `@minValue(0)`
   - Rebuild: `cd specs/api && bun run build`
   - Regenerate: `cd services/api-ts && bun run generate`
   - Verify generated validator now has `.lte(90)`
   Files: `specs/api/src/modules/dues.tsp`, then regenerate

3. **Verify all dues and association:member tests pass**
   Run: `cd services/api-ts && bun test handlers/association:member/dues`
</tasks>
