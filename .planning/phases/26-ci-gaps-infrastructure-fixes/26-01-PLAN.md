---
phase: 26-ci-gaps-infrastructure-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-ts/src/core/jobs.test.ts
  - services/api-ts/src/tests/route-protection-association.test.ts
  - services/api-ts/src/handlers/booking/jobs/slotGenerator.test.ts
  - package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Zero expect(true).toBe(true) non-sentinel assertions exist in the codebase"
    - "bun run test:br executes br-coverage.ts and prints coverage summary"
    - "All modified test files still pass"
  artifacts:
    - path: "package.json"
      provides: "test:br script"
      contains: "test:br"
    - path: "services/api-ts/src/core/jobs.test.ts"
      provides: "Real assertion for duplicate registration test"
    - path: "services/api-ts/src/tests/route-protection-association.test.ts"
      provides: "Documentation test converted to .todo() or real assertion"
    - path: "services/api-ts/src/handlers/booking/jobs/slotGenerator.test.ts"
      provides: "Meaningful fallback assertion replacing expect(true)"
  key_links:
    - from: "package.json"
      to: "scripts/br-coverage.ts"
      via: "test:br script"
      pattern: "bun run scripts/br-coverage.ts"
---

<objective>
Eliminate all meaningless `expect(true).toBe(true)` assertions from the test suite and add the `test:br` script to root package.json.

Purpose: Per D-02, meaningless assertions hide test gaps. Per D-03, developers need `test:br` for local BR coverage checks.
Output: Three test files with real assertions, root package.json with `test:br` script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix meaningless assertions in three test files</name>
  <files>
    services/api-ts/src/core/jobs.test.ts
    services/api-ts/src/tests/route-protection-association.test.ts
    services/api-ts/src/handlers/booking/jobs/slotGenerator.test.ts
  </files>
  <read_first>
    services/api-ts/src/core/jobs.test.ts
    services/api-ts/src/tests/route-protection-association.test.ts
    services/api-ts/src/handlers/booking/jobs/slotGenerator.test.ts
  </read_first>
  <action>
Fix each `expect(true).toBe(true)` per D-02:

**File 1: `services/api-ts/src/core/jobs.test.ts` line 184**
Context: Test "duplicate registration: registerCron overwrites handler without throwing". The test already proves no-throw by reaching line 184. Replace the meaningless assertion with `expect(() => { scheduler.registerCron('dup-cron', '*/2 * * * *', handler2); }).not.toThrow();` — wrap the SECOND registerCron call in the expect-not-toThrow so the assertion is explicit. Remove the bare `expect(true).toBe(true)`.

**File 2: `services/api-ts/src/tests/route-protection-association.test.ts` line 53**
Context: "DOCUMENTED: orgContextMiddleware always sets role=member" — pure documentation, no behavior tested. Convert the entire `test(...)` block to `test.todo('Pitfall 2: orgContextMiddleware always sets role=member — verify handler-level officer checks query officer_term')`. Remove the test body and the `expect(true).toBe(true)`. Keep the surrounding `describe` block and its JSDoc comment intact.

**File 3: `services/api-ts/src/handlers/booking/jobs/slotGenerator.test.ts` line 524**
Context: Inside "deletes existing available slots before regenerating" — the `if (!deleteWasCalled)` branch uses `expect(true).toBe(true)` as a safety net for parallel test pollution. Replace the entire if/else block at lines 522-528 with:
```typescript
    // In parallel execution, prototype pollution may prevent db.delete from
    // being called. Assert the expected path; skip gracefully if race detected.
    if (!deleteWasCalled) {
      console.warn('[slotGenerator.test] Skipped delete assertion — parallel prototype pollution detected');
    }
    expect(deleteWasCalled || true).toBe(true); // Passes but logs warning
```
Actually, better approach: just remove the if/else entirely and use `expect(deleteWasCalled).toBe(true)` directly. If the test is flaky due to parallel execution, it should be fixed at the source, not papered over. But since the comment explains this is a known Bun limitation, convert to: remove the if/else block and replace with a single line that documents the race but still asserts:
```typescript
    // Known: Bun parallel execution can cause prototype pollution.
    // If deleteWasCalled is false here, it's a test-infra issue, not a code bug.
    expect(deleteWasCalled).toBe(true);
```
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry && grep -r "expect(true)\.toBe(true)" services/api-ts/src/ | grep -v node_modules | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep for `expect(true).toBe(true)` across `services/api-ts/src/` returns 0 matches
    - `cd services/api-ts && bun test src/core/jobs.test.ts` passes
    - `cd services/api-ts && bun test src/tests/route-protection-association.test.ts` passes (the .todo test shows as skipped, not failed)
    - `cd services/api-ts && bun test src/handlers/booking/jobs/slotGenerator.test.ts` passes
  </acceptance_criteria>
  <done>Zero `expect(true).toBe(true)` in codebase. All three test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add test:br script to root package.json</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Per D-03: Add `"test:br": "bun run scripts/br-coverage.ts"` to the `scripts` section of root `package.json`. Place it alphabetically near the other `test:*` scripts (after `test:contract:fuzz`, before `test:inventory`).

No flags by default — `--ci` is for CI gate use only. This matches the existing pattern where `test:registry` and `test:contract` have no flags in the script definition.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry && grep -v '^#' package.json | grep -c '"test:br"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"test:br"' package.json` returns exactly 1 match
    - `bun run test:br` executes successfully and prints BR coverage summary
    - Script value is exactly `"bun run scripts/br-coverage.ts"` with no flags
  </acceptance_criteria>
  <done>Root package.json has test:br script. `bun run test:br` prints coverage report and exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this plan modifies test files and a package.json script. No production code, no user input, no network calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | T (Tampering) | package.json script | accept | Script runs local static analysis only, no elevated privileges |
</threat_model>

<verification>
1. `grep -r "expect(true).toBe(true)" services/api-ts/src/` returns 0 matches
2. `bun run test:br` runs and prints coverage report
3. All three modified test files pass individually
</verification>

<success_criteria>
- Zero meaningless assertions in codebase
- test:br script works locally
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/26-ci-gaps-infrastructure-fixes/26-01-SUMMARY.md`
</output>
