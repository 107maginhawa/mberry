---
phase: 13-position-based-rbac
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - services/api-ts/src/handlers/association:member/recordDuesPayment.ts
  - services/api-ts/src/handlers/association:member/refundDuesPayment.ts
  - services/api-ts/src/handlers/association:member/createDuesConfig.ts
  - services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.ts
  - services/api-ts/src/handlers/association:member/addRosterMember.ts
  - services/api-ts/src/handlers/association:member/importRosterMembers.ts
  - services/api-ts/src/handlers/association:member/approveMembershipApplication.ts
  - services/api-ts/src/handlers/association:member/denyMembershipApplication.ts
  - services/api-ts/src/handlers/association:member/createMembership.ts
  - services/api-ts/src/handlers/association:member/updateMembership.ts
  - services/api-ts/src/handlers/association:member/createAnnouncement.ts
  - services/api-ts/src/handlers/association:member/publishAnnouncement.ts
  - services/api-ts/src/handlers/association:member/createElection.ts
  - services/api-ts/src/handlers/association:member/createOfficerTerm.ts
  - services/api-ts/src/handlers/association:member/createPosition.ts
  - services/api-ts/src/handlers/association:member/updateOrganizationProfile.ts
autonomous: true
requirements: [REQ-03, REQ-05]
must_haves:
  truths:
    - "Treasurer-domain handlers (dues) only accept Treasurer or President"
    - "Secretary-domain handlers (roster, applications, announcements) only accept Secretary or President"
    - "President-only handlers (elections, positions, org profile update) only accept President"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/recordDuesPayment.ts"
      provides: "Position-restricted dues payment handler"
      contains: "requirePosition"
    - path: "services/api-ts/src/handlers/association:member/createElection.ts"
      provides: "President-only election handler"
      contains: "requirePosition"
  key_links:
    - from: "services/api-ts/src/handlers/association:member/recordDuesPayment.ts"
      to: "services/api-ts/src/utils/officer-check.ts"
      via: "import requirePosition"
      pattern: "import.*requirePosition.*from.*officer-check"
---

<objective>
Wire `requirePosition` to all 16 association:member and communications handler files, replacing `requireOfficerTerm` calls with position-specific title arrays per the D-01 permission matrix.

Purpose: Convert binary officer access to position-based access for member/communications handlers. After this plan, Treasurer can only access dues handlers, Secretary can only access roster/application/announcement handlers, and President-only governance endpoints are locked down.

Output: 16 handler files upgraded from `requireOfficerTerm(ctx)` to `requirePosition(ctx, [...titles])`.
</objective>

<execution_context>
@/Users/elad-mini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elad-mini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/elad-mini/Desktop/memberry/.planning/PROJECT.md
@/Users/elad-mini/Desktop/memberry/.planning/ROADMAP.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-CONTEXT.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-PATTERNS.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 13-01 — requirePosition signature and position titles -->

From services/api-ts/src/utils/officer-check.ts (after Plan 13-01):
```typescript
export async function requirePosition(ctx: BaseContext, allowedTitles: string[]): Promise<Response | null>
// Returns null if allowed, 403 Response if denied
// Internally checks: auth -> org -> officer term -> position title match
// Case-insensitive matching, ANY position matches = allowed
```

From services/api-ts/src/utils/position-titles.ts:
```typescript
export const POSITION_TITLES = {
  PRESIDENT: 'President',
  TREASURER: 'Treasurer',
  SECRETARY: 'Secretary',
  SOCIETY_OFFICER: 'Society Officer',
} as const;
```

Handler upgrade pattern (every file follows this):
```typescript
// BEFORE (current):
import { requireOfficerTerm } from '@/utils/officer-check';
// ...
const denied = await requireOfficerTerm(ctx);
if (denied) return denied;

// AFTER:
import { requirePosition } from '@/utils/officer-check';
import { POSITION_TITLES } from '@/utils/position-titles';
// ...
const denied = await requirePosition(ctx, [POSITION_TITLES.TREASURER, POSITION_TITLES.PRESIDENT]);
if (denied) return denied;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire requirePosition to Treasurer + Secretary + communications handlers (12 files)</name>
  <files>
    services/api-ts/src/handlers/association:member/recordDuesPayment.ts,
    services/api-ts/src/handlers/association:member/refundDuesPayment.ts,
    services/api-ts/src/handlers/association:member/createDuesConfig.ts,
    services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.ts,
    services/api-ts/src/handlers/association:member/addRosterMember.ts,
    services/api-ts/src/handlers/association:member/importRosterMembers.ts,
    services/api-ts/src/handlers/association:member/approveMembershipApplication.ts,
    services/api-ts/src/handlers/association:member/denyMembershipApplication.ts,
    services/api-ts/src/handlers/association:member/createMembership.ts,
    services/api-ts/src/handlers/association:member/updateMembership.ts,
    services/api-ts/src/handlers/communications/createAnnouncement.ts,
    services/api-ts/src/handlers/communications/publishAnnouncement.ts
  </files>
  <read_first>
    - services/api-ts/src/handlers/association:member/recordDuesPayment.ts (representative — see current import + guard call pattern)
    - services/api-ts/src/handlers/association:member/addRosterMember.ts (representative — Secretary domain)
    - services/api-ts/src/handlers/communications/createAnnouncement.ts (communications domain)
    - services/api-ts/src/utils/officer-check.ts (confirm requirePosition exists from Plan 13-01)
    - services/api-ts/src/utils/position-titles.ts (confirm POSITION_TITLES exists from Plan 13-01)
  </read_first>
  <action>
For each handler file, make exactly 2 changes:

**Change 1 — Import:** Replace `import { requireOfficerTerm } from '@/utils/officer-check';` with:
```typescript
import { requirePosition } from '@/utils/officer-check';
import { POSITION_TITLES } from '@/utils/position-titles';
```

**Change 2 — Guard call:** Replace `const denied = await requireOfficerTerm(ctx);` with position-specific call.

**Treasurer + President (4 files):**
- `recordDuesPayment.ts`: `const denied = await requirePosition(ctx, [POSITION_TITLES.TREASURER, POSITION_TITLES.PRESIDENT]);`
- `refundDuesPayment.ts`: same as above
- `createDuesConfig.ts`: same as above
- `generateDuesInvoicesForOrg.ts`: same as above

**Secretary + President (6 files):**
- `addRosterMember.ts`: `const denied = await requirePosition(ctx, [POSITION_TITLES.SECRETARY, POSITION_TITLES.PRESIDENT]);`
- `importRosterMembers.ts`: same as above
- `approveMembershipApplication.ts`: same as above
- `denyMembershipApplication.ts`: same as above
- `createMembership.ts`: same as above
- `updateMembership.ts`: same as above

**Secretary + President — communications (2 files):**
- `createAnnouncement.ts`: `const denied = await requirePosition(ctx, [POSITION_TITLES.SECRETARY, POSITION_TITLES.PRESIDENT]);`
- `publishAnnouncement.ts`: same as above

Do NOT change anything else in these files. The `if (denied) return denied;` line stays the same.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && grep -rl "requirePosition" src/handlers/association:member/ src/handlers/communications/ | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep -rl "requirePosition" in handlers returns 12 files (this task) — will be 16 after Task 2
    - grep -rl "requireOfficerTerm" in handlers returns 13 files (13 operations handlers not yet upgraded)
    - recordDuesPayment.ts contains `requirePosition(ctx, [POSITION_TITLES.TREASURER, POSITION_TITLES.PRESIDENT])`
    - addRosterMember.ts contains `requirePosition(ctx, [POSITION_TITLES.SECRETARY, POSITION_TITLES.PRESIDENT])`
    - createAnnouncement.ts contains `requirePosition(ctx, [POSITION_TITLES.SECRETARY, POSITION_TITLES.PRESIDENT])`
    - No file contains both `requireOfficerTerm` and `requirePosition` imports (each file uses one or the other)
  </acceptance_criteria>
  <done>12 handler files upgraded: 4 Treasurer+President, 6 Secretary+President, 2 communications Secretary+President</done>
</task>

<task type="auto">
  <name>Task 2: Wire requirePosition to President-only governance handlers (4 files)</name>
  <files>
    services/api-ts/src/handlers/association:member/createElection.ts,
    services/api-ts/src/handlers/association:member/createOfficerTerm.ts,
    services/api-ts/src/handlers/association:member/createPosition.ts,
    services/api-ts/src/handlers/association:member/updateOrganizationProfile.ts
  </files>
  <read_first>
    - services/api-ts/src/handlers/association:member/createElection.ts (confirm current requireOfficerTerm pattern)
    - services/api-ts/src/handlers/association:member/updateOrganizationProfile.ts (confirm current pattern)
  </read_first>
  <action>
Same 2-change pattern as Task 1, but with President-only restriction.

**Change 1 — Import:** Replace `import { requireOfficerTerm } from '@/utils/officer-check';` with:
```typescript
import { requirePosition } from '@/utils/officer-check';
import { POSITION_TITLES } from '@/utils/position-titles';
```

**Change 2 — Guard call (President only, 4 files):**
- `createElection.ts`: `const denied = await requirePosition(ctx, [POSITION_TITLES.PRESIDENT]);`
- `createOfficerTerm.ts`: same as above
- `createPosition.ts`: same as above
- `updateOrganizationProfile.ts`: same as above

Per D-01: These are governance endpoints — only President can access them. No other position is included.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && grep -c "POSITION_TITLES.PRESIDENT\])" src/handlers/association:member/createElection.ts src/handlers/association:member/createOfficerTerm.ts src/handlers/association:member/createPosition.ts src/handlers/association:member/updateOrganizationProfile.ts</automated>
  </verify>
  <acceptance_criteria>
    - createElection.ts contains `requirePosition(ctx, [POSITION_TITLES.PRESIDENT])`
    - createOfficerTerm.ts contains `requirePosition(ctx, [POSITION_TITLES.PRESIDENT])`
    - createPosition.ts contains `requirePosition(ctx, [POSITION_TITLES.PRESIDENT])`
    - updateOrganizationProfile.ts contains `requirePosition(ctx, [POSITION_TITLES.PRESIDENT])`
    - grep -rl "requireOfficerTerm" in association:member/ returns 0 files (all 16 upgraded)
    - grep -rl "requirePosition" in association:member/ + communications/ returns 16 files total
  </acceptance_criteria>
  <done>4 President-only governance handlers upgraded. All 16 member + communications handlers now use requirePosition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> handler | Request passes through officerAuthMiddleware but handler must verify specific position |
| requirePosition -> DB | Position title comes from DB JOIN, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Elevation of Privilege | handler files | mitigate | Every handler file replaces requireOfficerTerm with requirePosition using specific title array per D-01 matrix |
| T-13-05 | Elevation of Privilege | President-only handlers | mitigate | createElection, createOfficerTerm, createPosition, updateOrganizationProfile restricted to PRESIDENT only — no other title in array |
| T-13-06 | Information Disclosure | position error message | accept | Error reveals required position titles — acceptable since position structure is not secret and aids debugging |
</threat_model>

<verification>
- `grep -rl "requireOfficerTerm" services/api-ts/src/handlers/association:member/ services/api-ts/src/handlers/communications/` returns 0 files
- `grep -rl "requirePosition" services/api-ts/src/handlers/association:member/ services/api-ts/src/handlers/communications/` returns 16 files
- `grep -c "POSITION_TITLES.PRESIDENT\])" services/api-ts/src/handlers/association:member/createElection.ts` returns 1
</verification>

<success_criteria>
- All 16 handler files in association:member and communications upgraded from requireOfficerTerm to requirePosition
- Position title arrays match D-01 matrix exactly: Treasurer+President for dues, Secretary+President for roster/applications/announcements, President for governance
- No handler uses requireOfficerTerm anymore (replaced, not doubled)
</success_criteria>

<output>
After completion, create `.planning/phases/13-position-based-rbac/13-02-SUMMARY.md`
</output>
