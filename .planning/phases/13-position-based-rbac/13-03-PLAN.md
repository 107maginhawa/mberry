---
phase: 13-position-based-rbac
plan: "03"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - services/api-ts/src/handlers/association:operations/createEvent.ts
  - services/api-ts/src/handlers/association:operations/updateEvent.ts
  - services/api-ts/src/handlers/association:operations/deleteEvent.ts
  - services/api-ts/src/handlers/association:operations/publishEvent.ts
  - services/api-ts/src/handlers/association:operations/cancelEvent.ts
  - services/api-ts/src/handlers/association:operations/createCheckIn.ts
  - services/api-ts/src/handlers/association:operations/createTraining.ts
  - services/api-ts/src/handlers/association:operations/updateTraining.ts
  - services/api-ts/src/handlers/association:operations/deleteTraining.ts
  - services/api-ts/src/handlers/association:operations/publishTraining.ts
  - services/api-ts/src/handlers/association:operations/createCourse.ts
  - services/api-ts/src/handlers/association:operations/updateCourse.ts
  - services/api-ts/src/handlers/association:operations/deleteCourse.ts
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - "Society Officer + President can access event/training/course handlers"
    - "Treasurer and Secretary get 403 on event/training/course endpoints"
  artifacts:
    - path: "services/api-ts/src/handlers/association:operations/createEvent.ts"
      provides: "Position-restricted event handler"
      contains: "requirePosition"
  key_links:
    - from: "services/api-ts/src/handlers/association:operations/createEvent.ts"
      to: "services/api-ts/src/utils/officer-check.ts"
      via: "import requirePosition"
      pattern: "import.*requirePosition.*from.*officer-check"
---

<objective>
Wire `requirePosition` to all 13 association:operations handler files (events, training, courses), replacing `requireOfficerTerm` with Society Officer + President restriction.

Purpose: Lock down event/training/course management to Society Officers and Presidents only. Runs in parallel with Plan 13-02 (no file overlap).

Output: 13 operations handler files upgraded to `requirePosition(ctx, [SOCIETY_OFFICER, PRESIDENT])`.
</objective>

<execution_context>
@/Users/elad-mini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elad-mini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/elad-mini/Desktop/memberry/.planning/PROJECT.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-CONTEXT.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-PATTERNS.md
@/Users/elad-mini/Desktop/memberry/.planning/phases/13-position-based-rbac/13-01-SUMMARY.md

<interfaces>
From services/api-ts/src/utils/officer-check.ts (after Plan 13-01):
```typescript
export async function requirePosition(ctx: BaseContext, allowedTitles: string[]): Promise<Response | null>
```

From services/api-ts/src/utils/position-titles.ts:
```typescript
export const POSITION_TITLES = {
  PRESIDENT: 'President',
  TREASURER: 'Treasurer',
  SECRETARY: 'Secretary',
  SOCIETY_OFFICER: 'Society Officer',
} as const;
```

Handler upgrade pattern:
```typescript
// BEFORE:
import { requireOfficerTerm } from '@/utils/officer-check';
const denied = await requireOfficerTerm(ctx);

// AFTER:
import { requirePosition } from '@/utils/officer-check';
import { POSITION_TITLES } from '@/utils/position-titles';
const denied = await requirePosition(ctx, [POSITION_TITLES.SOCIETY_OFFICER, POSITION_TITLES.PRESIDENT]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire requirePosition to all 13 operations handlers (Society Officer + President)</name>
  <files>
    services/api-ts/src/handlers/association:operations/createEvent.ts,
    services/api-ts/src/handlers/association:operations/updateEvent.ts,
    services/api-ts/src/handlers/association:operations/deleteEvent.ts,
    services/api-ts/src/handlers/association:operations/publishEvent.ts,
    services/api-ts/src/handlers/association:operations/cancelEvent.ts,
    services/api-ts/src/handlers/association:operations/createCheckIn.ts,
    services/api-ts/src/handlers/association:operations/createTraining.ts,
    services/api-ts/src/handlers/association:operations/updateTraining.ts,
    services/api-ts/src/handlers/association:operations/deleteTraining.ts,
    services/api-ts/src/handlers/association:operations/publishTraining.ts,
    services/api-ts/src/handlers/association:operations/createCourse.ts,
    services/api-ts/src/handlers/association:operations/updateCourse.ts,
    services/api-ts/src/handlers/association:operations/deleteCourse.ts
  </files>
  <read_first>
    - services/api-ts/src/handlers/association:operations/createEvent.ts (representative — see import + guard pattern)
    - services/api-ts/src/handlers/association:operations/createTraining.ts (confirm same pattern)
    - services/api-ts/src/utils/officer-check.ts (confirm requirePosition exists)
    - services/api-ts/src/utils/position-titles.ts (confirm POSITION_TITLES exists)
  </read_first>
  <action>
For ALL 13 files in `association:operations/`, make exactly 2 changes each:

**Change 1 — Import:** Replace `import { requireOfficerTerm } from '@/utils/officer-check';` with:
```typescript
import { requirePosition } from '@/utils/officer-check';
import { POSITION_TITLES } from '@/utils/position-titles';
```

**Change 2 — Guard call:** Replace `const denied = await requireOfficerTerm(ctx);` with:
```typescript
const denied = await requirePosition(ctx, [POSITION_TITLES.SOCIETY_OFFICER, POSITION_TITLES.PRESIDENT]);
```

All 13 files get the SAME position array: `[SOCIETY_OFFICER, PRESIDENT]` per D-01.

Files (all in `services/api-ts/src/handlers/association:operations/`):
1. createEvent.ts
2. updateEvent.ts
3. deleteEvent.ts
4. publishEvent.ts
5. cancelEvent.ts
6. createCheckIn.ts
7. createTraining.ts
8. updateTraining.ts
9. deleteTraining.ts
10. publishTraining.ts
11. createCourse.ts
12. updateCourse.ts
13. deleteCourse.ts

Do NOT change anything else. The `if (denied) return denied;` line stays the same.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && grep -rl "requirePosition" src/handlers/association:operations/ | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep -rl "requirePosition" in association:operations/ returns 13 files
    - grep -rl "requireOfficerTerm" in association:operations/ returns 0 files
    - createEvent.ts contains `requirePosition(ctx, [POSITION_TITLES.SOCIETY_OFFICER, POSITION_TITLES.PRESIDENT])`
    - createTraining.ts contains `requirePosition(ctx, [POSITION_TITLES.SOCIETY_OFFICER, POSITION_TITLES.PRESIDENT])`
    - createCourse.ts contains `requirePosition(ctx, [POSITION_TITLES.SOCIETY_OFFICER, POSITION_TITLES.PRESIDENT])`
    - All 13 files import both `requirePosition` and `POSITION_TITLES`
  </acceptance_criteria>
  <done>All 13 association:operations handlers upgraded to requirePosition with Society Officer + President restriction</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> operations handler | Must verify Society Officer or President position |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Elevation of Privilege | operations handlers | mitigate | All 13 handlers require SOCIETY_OFFICER or PRESIDENT position via requirePosition |
</threat_model>

<verification>
- `grep -rl "requireOfficerTerm" services/api-ts/src/handlers/association:operations/` returns 0 files
- `grep -rl "requirePosition" services/api-ts/src/handlers/association:operations/` returns 13 files
- Combined with Plan 13-02: `grep -rl "requireOfficerTerm" services/api-ts/src/handlers/` returns 0 files across all handler dirs
</verification>

<success_criteria>
- All 13 operations handler files upgraded from requireOfficerTerm to requirePosition
- All use [SOCIETY_OFFICER, PRESIDENT] array per D-01
- Zero handler files still using requireOfficerTerm
</success_criteria>

<output>
After completion, create `.planning/phases/13-position-based-rbac/13-03-SUMMARY.md`
</output>
