---
phase: 21-officer-daily-ops
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - services/api-ts/src/handlers/membership/repos/membership.repo.ts
  - services/api-ts/src/handlers/association:member/listRosterMembers.ts
  - services/api-ts/src/handlers/association:member/listRosterMembers.test.ts
autonomous: true
requirements: [OPS-01, OPS-04]

must_haves:
  truths:
    - "Officer can load chapter roster showing member name, dues status, and training credit summary in a single request"
    - "Roster query uses server-side JOIN (no N+1) — person + membership + dues_invoice + credit_entry"
    - "Officer can filter roster by duesStatus and receive only matching results"
    - "Officer can filter roster by trainingCompliant and receive only matching results"
    - "Filters are applied at DB level (WHERE clauses), not client-side"
  artifacts:
    - path: "services/api-ts/src/handlers/membership/repos/membership.repo.ts"
      provides: "listMembersWithOfficerStatus() method with JOINed dues + training data + filters"
      contains: "listMembersWithOfficerStatus"
    - path: "services/api-ts/src/handlers/association:member/listRosterMembers.ts"
      provides: "Handler calling new repo method, returning OfficerRosterMember shape"
      contains: "listMembersWithOfficerStatus"
    - path: "services/api-ts/src/handlers/association:member/listRosterMembers.test.ts"
      provides: "Unit tests for OPS-01 and OPS-04"
      contains: "duesInvoiceStatus"
  key_links:
    - from: "services/api-ts/src/handlers/association:member/listRosterMembers.ts"
      to: "services/api-ts/src/handlers/membership/repos/membership.repo.ts"
      via: "repo.listMembersWithOfficerStatus(filters)"
      pattern: "listMembersWithOfficerStatus"
    - from: "services/api-ts/src/handlers/membership/repos/membership.repo.ts"
      to: "dues_invoice table"
      via: "correlated subquery for latest invoice status"
      pattern: "dues_invoice.*membership_id"
    - from: "services/api-ts/src/handlers/membership/repos/membership.repo.ts"
      to: "credit_entry table"
      via: "correlated subquery for SUM(credit_amount) with cycle bounds"
      pattern: "credit_entry.*credit_amount"
---

<objective>
Extend the roster endpoint to return per-member dues status and training compliance via server-side JOINs, and add DB-level filtering by duesStatus and trainingCompliant.

Purpose: OPS-01 requires officers to see dues + training status in a single roster request. OPS-04 requires server-side filtering. Both must avoid N+1 queries.

Output: Extended repo method, updated handler, unit tests.
</objective>

<execution_context>
@.claude/skills/handler/SKILL.md
@.claude/skills/test-api/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-officer-daily-ops/21-RESEARCH.md
@.planning/phases/21-officer-daily-ops/21-01-SUMMARY.md

<interfaces>
From services/api-ts/src/handlers/membership/repos/membership.repo.ts:
```typescript
// MembershipRepository.listMembers() — existing method to extend
export class MembershipRepository {
  constructor(private db: DatabaseInstance) {}

  async listMembers(filters: {
    organizationId: string;
    status?: string;
    categoryId?: string;
    search?: string;
    limit?: number;
    offset?: number;
  }): Promise<{ data: any[]; total: number }>
}
```

From services/api-ts/src/handlers/association:member/listRosterMembers.ts:
```typescript
// Current handler — delegates to repo.listMembers(), flattens result
export async function listRosterMembers(
  ctx: ValidatedContext<never, ListRosterMembersQuery, never>
): Promise<Response>
```

From services/api-ts/src/handlers/association:member/repos/dues.schema.ts:
```typescript
export const duesInvoiceStatusEnum = pgEnum('dues_invoice_status', [
  'generated', 'sent', 'paid', 'overdue', 'cancelled', 'writtenOff'
]);
export const duesInvoices = pgTable('dues_invoice', { ... });
// Key columns: membershipId, status, createdAt
```

From services/api-ts/src/handlers/association:member/repos/credits.schema.ts:
```typescript
export const creditEntries = pgTable('credit_entry', { ... });
// Key columns: personId, organizationId, creditAmount, cycleStart, cycleEnd
```

From utils/officer-check.ts:
```typescript
export function requirePosition(ctx, positions: string[]): Promise<Response | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add listMembersWithOfficerStatus to MembershipRepository</name>
  <files>
    services/api-ts/src/handlers/membership/repos/membership.repo.ts
    services/api-ts/src/handlers/association:member/listRosterMembers.test.ts
  </files>
  <read_first>
    - services/api-ts/src/handlers/membership/repos/membership.repo.ts (full file — extract listMembers pattern)
    - services/api-ts/src/handlers/association:member/repos/dues.schema.ts (duesInvoices table schema)
    - services/api-ts/src/handlers/association:member/repos/credits.schema.ts (creditEntries table schema)
    - services/api-ts/src/handlers/association:member/getCreditCompliance.ts (credit SUM pattern, threshold default)
  </read_first>
  <behavior>
    - Test 1: listMembersWithOfficerStatus returns duesInvoiceStatus field from latest invoice
    - Test 2: listMembersWithOfficerStatus returns creditsEarned as sum of credit_amount for current cycle
    - Test 3: listMembersWithOfficerStatus returns trainingCompliant=true when creditsEarned >= 40
    - Test 4: listMembersWithOfficerStatus returns trainingCompliant=false when creditsEarned < 40
    - Test 5: duesStatus filter returns only members with matching latest invoice status
    - Test 6: trainingCompliant=true filter returns only compliant members
    - Test 7: trainingCompliant=false filter returns only non-compliant members
    - Test 8: member with no invoices has duesInvoiceStatus=null
    - Test 9: member with no credits has creditsEarned=0 and trainingCompliant=false
  </behavior>
  <action>
  Add a new method `listMembersWithOfficerStatus` to `MembershipRepository` that:

  1. Accepts all existing `listMembers` filters PLUS `duesStatus?: string` and `trainingCompliant?: boolean`
  2. Uses the same base query as `listMembers` (SELECT from memberships JOIN persons JOIN membershipCategories)
  3. Adds two correlated subqueries in the SELECT:
     - `latestInvoiceStatus`: `(SELECT status FROM dues_invoice WHERE membership_id = memberships.id ORDER BY created_at DESC LIMIT 1)`
     - `totalCredits`: `(SELECT COALESCE(SUM(credit_amount), 0) FROM credit_entry WHERE person_id = memberships.personId AND organization_id = memberships.organizationId AND cycle_start <= NOW() AND cycle_end >= NOW())`
  4. Computes `trainingCompliant` as `totalCredits >= 40` (hardcode threshold per A1 assumption)
  5. Adds WHERE clauses for filters:
     - `duesStatus` filter: `AND (SELECT status FROM dues_invoice WHERE membership_id = memberships.id ORDER BY created_at DESC LIMIT 1) = :duesStatus`
     - `trainingCompliant=true` filter: `AND (SELECT COALESCE(SUM(credit_amount), 0) FROM credit_entry WHERE person_id = memberships.personId AND organization_id = memberships.organizationId AND cycle_start <= NOW() AND cycle_end >= NOW()) >= 40`
     - `trainingCompliant=false` filter: same subquery `< 40`
  6. Returns `{ data, total }` where each data row includes: membership fields, person fields (firstName, lastName, email, avatar), category fields, plus `duesInvoiceStatus`, `creditsEarned`, `trainingCompliant`

  Import `duesInvoices` from `../../association:member/repos/dues.schema` and `creditEntries` from `../../association:member/repos/credits.schema` for column references in sql template tags.

  Use `sql` template literal for the subqueries (Drizzle raw SQL pattern already used in this repo).

  Write tests in `listRosterMembers.test.ts`. Tests should create test data (memberships, invoices, credit entries) and verify the method returns correct joined data and filters work.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/association:member/listRosterMembers.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "listMembersWithOfficerStatus" services/api-ts/src/handlers/membership/repos/membership.repo.ts returns >= 1
    - grep -c "duesInvoiceStatus" services/api-ts/src/handlers/membership/repos/membership.repo.ts returns >= 1
    - grep -c "credit_amount" services/api-ts/src/handlers/membership/repos/membership.repo.ts returns >= 1
    - grep -c "trainingCompliant" services/api-ts/src/handlers/membership/repos/membership.repo.ts returns >= 1
    - grep -v '^#' services/api-ts/src/handlers/association:member/listRosterMembers.test.ts | grep -c "test\|it(" returns >= 5
  </acceptance_criteria>
  <done>Repo method returns joined dues + training data. All filter tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update listRosterMembers handler to use new repo method and add requirePosition</name>
  <files>services/api-ts/src/handlers/association:member/listRosterMembers.ts</files>
  <read_first>
    - services/api-ts/src/handlers/association:member/listRosterMembers.ts (current handler)
    - services/api-ts/src/generated/openapi/validators.ts (search for ListRosterMembersQuery — confirm new params exist)
  </read_first>
  <action>
  Update the handler to:

  1. Add `requirePosition` guard: `[POSITION_TITLES.SECRETARY, POSITION_TITLES.PRESIDENT, POSITION_TITLES.SOCIETY_OFFICER]`
  2. Extract `duesStatus` and `trainingCompliant` from `query` (these are now in the generated validator from Plan 01)
  3. Call `repo.listMembersWithOfficerStatus()` instead of `repo.listMembers()`, passing all filters including the new ones
  4. Update the flatten/map logic to include `duesInvoiceStatus`, `creditsEarned`, `trainingCompliant` in the response
  5. Add `email` to the flattened response (it's already in the person JOIN from the repo)

  Import `requirePosition` from `@/utils/officer-check` and `POSITION_TITLES` from `@/utils/position-titles`.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bunx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "requirePosition" services/api-ts/src/handlers/association:member/listRosterMembers.ts returns >= 1
    - grep -c "listMembersWithOfficerStatus" services/api-ts/src/handlers/association:member/listRosterMembers.ts returns >= 1
    - grep -c "duesInvoiceStatus" services/api-ts/src/handlers/association:member/listRosterMembers.ts returns >= 1
    - grep -c "trainingCompliant" services/api-ts/src/handlers/association:member/listRosterMembers.ts returns >= 1
  </acceptance_criteria>
  <done>Handler uses new repo method, returns OfficerRosterMember shape with dues + training fields, officer-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> listRosterMembers | Untrusted query params; must be officer-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Information Disclosure | listRosterMembers | mitigate | requirePosition guard restricts to Secretary/President/Society Officer; organizationId from officer's org context |
| T-21-03 | Tampering | duesStatus query param | mitigate | Generated Zod validator restricts to valid string; SQL parameterized via Drizzle sql template |
| T-21-04 | Information Disclosure | Roster data leak cross-org | mitigate | organizationId WHERE clause scopes all queries to officer's chapter |
</threat_model>

<verification>
- `bun test src/handlers/association:member/listRosterMembers.test.ts` passes
- `bunx tsc --noEmit` passes
- Handler returns duesInvoiceStatus, creditsEarned, trainingCompliant per row
- Filters by duesStatus and trainingCompliant work at DB level
</verification>

<success_criteria>
Roster endpoint returns joined dues/training data per member. DB-level filters for duesStatus and trainingCompliant work correctly. Officer auth enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/21-officer-daily-ops/21-02-SUMMARY.md`
</output>
