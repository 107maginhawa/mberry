---
phase: 15
plan: "01"
title: "Batch Dues Reminder Backend"
wave: 1
autonomous: true
files_modified:
  - services/api-ts/src/handlers/association:member/repos/dues.schema.ts
  - services/api-ts/src/handlers/dues/jobs/reminderProcessor.ts
  - services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.ts
  - services/api-ts/src/handlers/association:member/repos/membership.repo.ts
  - services/api-ts/src/handlers/dues/jobs/reminderProcessor.test.ts
---

<objective>
Replace TODO placeholders in reminderProcessor.ts and generateDuesInvoicesForOrg.ts with
real notification creation logic. Add idempotency tracking via duesReminderLogs table.

CRITICAL: New tables go in `handlers/association:member/repos/dues.schema.ts` (the LIVE schema),
NOT in `handlers/dues/repos/dues-payments.schema.ts` (dead code per learning [dues-dual-model-dead-code]).

Read generated validators at `generated/openapi/validators.ts` for exact response shapes before implementing.
</objective>

<tasks>

## Task 1: Add duesReminderLogs table to live dues schema

File: `services/api-ts/src/handlers/association:member/repos/dues.schema.ts`

Add `duesReminderLogs` pgTable:
- `id` uuid primary key
- `organizationId` uuid NOT NULL (FK to organizations)
- `personId` uuid NOT NULL (FK to persons)
- `scheduleId` uuid (FK to duesReminderSchedules, nullable for manual sends)
- `duesConfigId` uuid NOT NULL (FK to duesConfigs)
- `periodKey` varchar(20) NOT NULL — e.g. "2026" for annual, "2026-Q1" for quarterly
- `daysOffset` integer NOT NULL — from schedule entry
- `channel` varchar(20) NOT NULL — "in-app", "email", "push"
- `notificationId` uuid (FK to notifications, nullable)
- `sentAt` timestamp NOT NULL default now()
- Plus baseEntityFields

Add unique constraint on `(personId, scheduleId, periodKey, daysOffset)` for idempotency.

## Task 2: Add findMembersExpiringOn to membership repo

File: `services/api-ts/src/handlers/association:member/repos/membership.repo.ts`

Add method:
```typescript
async findMembersExpiringOn(orgId: string, targetDate: string): Promise<{id: string, personId: string, organizationId: string, duesExpiryDate: Date}[]>
```

Query `memberships` where:
- `organizationId = orgId`
- `duesExpiryDate` = targetDate (date comparison)
- `status IN ('active', 'grace')`

## Task 3: Write RED tests for reminderProcessor

File: `services/api-ts/src/handlers/dues/jobs/reminderProcessor.test.ts` (NEW)

Test cases:
1. Processes all orgs with enabled reminder schedules
2. Skips already-sent reminders (idempotency via duesReminderLogs)
3. Creates in-app notification for members expiring on target date
4. Handles empty member list (no errors, sent=0)
5. Respects channel flags (channelEmail, channelPush, channelInapp)
6. Re-run is idempotent (same result, no duplicate notifications)

Use existing test patterns from `services/api-ts/src/handlers/dues/utils/fund-math.test.ts`.

## Task 4: Implement reminderProcessor GREEN

File: `services/api-ts/src/handlers/dues/jobs/reminderProcessor.ts`

Replace the TODO logging block with real logic:
1. For each org's dues config with enabled reminder schedules:
   a. For each enabled schedule entry, calculate target date = today + daysOffset
   b. Query `findMembersExpiringOn(orgId, targetDate)`
   c. For each member, check `duesReminderLogs` for existing `(personId, scheduleId, periodKey, daysOffset)`
   d. If not sent: create notification via existing notification mechanism with type `'billing'`
   e. Insert row into `duesReminderLogs`
   f. Increment `result.sent` or `result.skipped`
2. Return `ReminderResult { processed, sent, skipped, errors }`

## Task 5: Implement generateDuesInvoicesForOrg

File: `services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.ts`

Replace placeholder `{ count: 0 }` with:
1. Read generated validator for exact response shape
2. Call reminderProcessor for the specific org (filter by `body.organizationId`)
3. Return proper response matching the OpenAPI-generated type

## Task 6: Generate DB migration

```bash
cd services/api-ts && bun run db:generate
```

Verify migration SQL is correct for the new `duesReminderLogs` table.

</tasks>

<verification>
- [ ] duesReminderLogs table in live dues schema (NOT dead code schema)
- [ ] findMembersExpiringOn query works
- [ ] reminderProcessor creates real notifications
- [ ] generateDuesInvoicesForOrg returns real count
- [ ] Idempotency: re-run doesn't duplicate
- [ ] All existing tests still pass
</verification>
