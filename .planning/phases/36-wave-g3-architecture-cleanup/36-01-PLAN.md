---
phase: 36-wave-g3-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-ts/src/handlers/events/registerForEvent.ts
  - services/api-ts/src/handlers/events/registerForEvent.test.ts
  - services/api-ts/src/handlers/events/utils/membership-check.ts
autonomous: true
requirements: [V-18]

must_haves:
  truths:
    - "registerForEvent.ts no longer imports directly from association:member/repos/"
    - "Membership check extracted to a local utility or service boundary"
    - "Existing registration behavior unchanged (active members can register, non-active blocked)"
  artifacts:
    - path: "services/api-ts/src/handlers/events/utils/membership-check.ts"
      provides: "Membership check utility wrapping the cross-context query"
      exports: ["checkActiveMembership"]
---

<objective>
Decouple registerForEvent from direct MembershipRepository import (V-18).

Currently `registerForEvent.ts` imports `MembershipRepository` directly from `../association:member/repos/membership.repo`. This creates tight coupling between the events and membership bounded contexts.

Fix: Extract membership check to a local utility in the events module that wraps the query. This doesn't eliminate the dependency but creates a clear seam — future refactoring can replace the utility with an API call or event-based check.
</objective>

<tasks>
1. **Create membership check utility in events module**
   - Create `services/api-ts/src/handlers/events/utils/membership-check.ts`
   - Export `checkActiveMembership(db, personId, orgId): Promise<boolean>`
   - Internally uses MembershipRepository (the dependency moves here, not to handler)
   File: `services/api-ts/src/handlers/events/utils/membership-check.ts`

2. **Refactor registerForEvent to use local utility**
   - Replace direct `MembershipRepository` import with `checkActiveMembership` import
   - Behavior unchanged: active members register, non-active get error
   File: `services/api-ts/src/handlers/events/registerForEvent.ts`

3. **Add test for membership check utility**
   - Test: active member → returns true
   - Test: non-active member → returns false
   - Test: non-member → returns false
   File: `services/api-ts/src/handlers/events/utils/membership-check.test.ts`

4. **Verify events tests pass**
   Run: `cd services/api-ts && bun test handlers/events/`
</tasks>
