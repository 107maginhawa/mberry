---
phase: 25-email-notif-guards-handler-tests
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - services/api-ts/src/handlers/notifs/listNotifications.test.ts
  - services/api-ts/src/handlers/notifs/getNotification.test.ts
  - services/api-ts/src/handlers/notifs/markAllNotificationsAsRead.test.ts
  - services/api-ts/src/handlers/notifs/markAllNotificationsRead.test.ts
  - services/api-ts/src/handlers/platformadmin/getAdminRole.test.ts
  - services/api-ts/src/handlers/platformadmin/listOrganizations.test.ts
  - services/api-ts/src/handlers/platformadmin/updateOrganization.test.ts
  - services/api-ts/src/handlers/platformadmin/listAssociations.test.ts
  - services/api-ts/src/handlers/platformadmin/updateAssociation.test.ts
  - services/api-ts/src/handlers/platformadmin/updateAdmin.test.ts
  - services/api-ts/src/handlers/platformadmin/revokeAdmin.test.ts
  - services/api-ts/src/handlers/platformadmin/getAssociation.test.ts
  - services/api-ts/src/handlers/platformadmin/setFeatureFlag.test.ts
  - services/api-ts/src/handlers/platformadmin/inviteAdmin.test.ts
  - services/api-ts/src/handlers/platformadmin/getOrganization.test.ts
  - services/api-ts/src/handlers/platformadmin/createAssociation.test.ts
  - services/api-ts/src/handlers/platformadmin/deleteFeatureFlag.test.ts
  - services/api-ts/src/handlers/platformadmin/listFeatureFlags.test.ts
  - services/api-ts/src/handlers/platformadmin/deleteAssociation.test.ts
  - services/api-ts/src/handlers/platformadmin/listAdmins.test.ts
  - services/api-ts/src/handlers/platformadmin/createOrganization.test.ts
  - services/api-ts/src/handlers/documents/createDocument.test.ts
  - services/api-ts/src/handlers/documents/getDocument.test.ts
  - services/api-ts/src/handlers/documents/updateDocument.test.ts
  - services/api-ts/src/handlers/documents/deleteDocument.test.ts
  - services/api-ts/src/handlers/documents/searchDocuments.test.ts
  - services/api-ts/src/handlers/documents/archiveDocument.test.ts
  - services/api-ts/src/handlers/documents/getDocumentVersion.test.ts
  - services/api-ts/src/handlers/documents/listDocumentVersions.test.ts
  - services/api-ts/src/handlers/documents/uploadNewDocumentVersion.test.ts
  - services/api-ts/src/handlers/documents/getDocumentAccessLog.test.ts
  - services/api-ts/src/handlers/documents/createDocumentTag.test.ts
  - services/api-ts/src/handlers/documents/getDocumentTag.test.ts
  - services/api-ts/src/handlers/documents/listDocumentTags.test.ts
  - services/api-ts/src/handlers/documents/updateDocumentTag.test.ts
  - services/api-ts/src/handlers/documents/deleteDocumentTag.test.ts
  - services/api-ts/src/handlers/billing/getInvoice.test.ts
  - services/api-ts/src/handlers/billing/finalizeInvoice.test.ts
  - services/api-ts/src/handlers/billing/updateInvoice.test.ts
  - services/api-ts/src/handlers/billing/getMerchantAccount.test.ts
  - services/api-ts/src/handlers/billing/captureInvoicePayment.test.ts
  - services/api-ts/src/handlers/billing/createMerchantAccount.test.ts
  - services/api-ts/src/handlers/billing/onboardMerchantAccount.test.ts
  - services/api-ts/src/handlers/billing/refundInvoicePayment.test.ts
  - services/api-ts/src/handlers/billing/getMerchantDashboard.test.ts
  - services/api-ts/src/handlers/billing/deleteInvoice.test.ts
  - services/api-ts/src/handlers/billing/markInvoiceUncollectible.test.ts
autonomous: true
requirements: [EML-05]

must_haves:
  truths:
    - "All 4 notifs handlers have unit tests"
    - "All 17 platformadmin handlers have unit tests"
    - "All 15 documents handlers have unit tests"
    - "All 11 billing handlers have unit tests"
  artifacts:
    - path: "services/api-ts/src/handlers/notifs/listNotifications.test.ts"
      provides: "Notifs handler tests"
      contains: "describe"
    - path: "services/api-ts/src/handlers/platformadmin/listAdmins.test.ts"
      provides: "Platform admin handler tests"
      contains: "describe"
    - path: "services/api-ts/src/handlers/documents/getDocument.test.ts"
      provides: "Documents handler tests"
      contains: "describe"
    - path: "services/api-ts/src/handlers/billing/getInvoice.test.ts"
      provides: "Billing handler tests"
      contains: "describe"
  key_links: []
---

<objective>
Add unit test coverage for notifs (4), platformadmin (17), documents (15), and billing (11) handlers.

Purpose: EML-05 second batch. These 47 handlers cover notifications (guard integration), platform administration, document management, and billing modules.

Output: 47 test files, each with auth check + happy path tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From services/api-ts/src/test-utils/make-ctx.ts:
```typescript
export function makeCtx(overrides: Record<string, any> = {}): any;
export function stubRepo(RepoClass: any, methods: Record<string, Function>): void;
export function restoreRepo(RepoClass: any): void;
```

Canonical test pattern (same as Plan 04):
```typescript
import { describe, test, expect, beforeEach, afterEach } from 'bun:test';
import { makeCtx, stubRepo, restoreRepo } from '@/test-utils/make-ctx';
import { SomeRepository } from './repos/some.repo';
import { someHandler } from './someHandler';

describe('someHandler', () => {
  beforeEach(() => { restoreRepo(SomeRepository); });
  afterEach(() => { restoreRepo(SomeRepository); });

  test('returns 401 when unauthenticated', async () => {
    const ctx = makeCtx({ user: null, session: null });
    // Check handler source for auth style
  });

  test('happy path', async () => {
    stubRepo(SomeRepository, { method: async () => result });
    const ctx = makeCtx({ _params: { id: 'x' } });
    const res = await someHandler(ctx);
    expect(res.status).toBe(200);
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notifs + platformadmin tests (21 handlers)</name>
  <files>
    services/api-ts/src/handlers/notifs/*.test.ts (4 new - but check for existing markNotificationAsRead.test.ts and notifs-handlers.test.ts)
    services/api-ts/src/handlers/platformadmin/*.test.ts (17 new)
  </files>
  <read_first>
    - services/api-ts/src/handlers/notifs/listNotifications.ts (notification handler pattern)
    - services/api-ts/src/handlers/notifs/markAllNotificationsAsRead.ts (check if same as markAllNotificationsRead — may be duplicated)
    - services/api-ts/src/handlers/notifs/notifs-handlers.test.ts (existing test file — may already cover some handlers)
    - services/api-ts/src/handlers/notifs/markNotificationAsRead.test.ts (existing test pattern)
    - services/api-ts/src/handlers/notifs/repos/ (ls for repo class names)
    - services/api-ts/src/handlers/platformadmin/listAdmins.ts (admin handler pattern — likely requires platform_admin auth)
    - services/api-ts/src/handlers/platformadmin/createOrganization.ts (admin create pattern)
    - services/api-ts/src/handlers/platformadmin/repos/ (ls for repo class names)
  </read_first>
  <action>
    1. Check `notifs-handlers.test.ts` — it may already cover some of the 4 "untested" handlers. Only create new test files for handlers NOT covered by existing test files. If `markAllNotificationsAsRead` and `markAllNotificationsRead` are the same handler (duplicate file), note it and test only one.

    2. Create test files for the 4 notifs handlers (if not already covered):
       - listNotifications, getNotification, markAllNotificationsAsRead, markAllNotificationsRead
       - These are user-facing (auth required, scoped to user's notifications)

    3. Create 17 test files for all platformadmin handlers:
       - Admin CRUD: listAdmins, inviteAdmin, updateAdmin, revokeAdmin, getAdminRole
       - Organization CRUD: listOrganizations, getOrganization, createOrganization, updateOrganization
       - Association CRUD: listAssociations, getAssociation, createAssociation, updateAssociation, deleteAssociation
       - Feature flags: listFeatureFlags, setFeatureFlag, deleteFeatureFlag
       - Platform admin handlers require admin auth — test pattern should check for 403 when non-admin user calls them (use `makeCtx({ user: { id: 'u1', role: 'user' } })`)
       - Find the correct repo class from `platformadmin/repos/`

    IMPORTANT: Platform admin handlers may use a different auth pattern (platformAdminAuthMiddleware checks against platform_admin table, not user.role). Read a few handlers to understand the auth check before writing tests.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/notifs/ && bun test src/handlers/platformadmin/</automated>
  </verify>
  <acceptance_criteria>
    - All notifs handlers have test coverage (4 handlers, accounting for existing tests)
    - All 17 platformadmin handlers have test files
    - Auth patterns correctly tested (user auth for notifs, admin auth for platformadmin)
    - All tests pass
  </acceptance_criteria>
  <done>Notifs and platformadmin handlers fully tested</done>
</task>

<task type="auto">
  <name>Task 2: Documents + billing tests (26 handlers)</name>
  <files>
    services/api-ts/src/handlers/documents/*.test.ts (15 new)
    services/api-ts/src/handlers/billing/*.test.ts (11 new)
  </files>
  <read_first>
    - services/api-ts/src/handlers/documents/createDocument.ts (document handler pattern)
    - services/api-ts/src/handlers/documents/uploadNewDocumentVersion.ts (version upload pattern)
    - services/api-ts/src/handlers/documents/repos/ (ls for repo class names — likely DocumentRepository, DocumentTagRepository, DocumentVersionRepository)
    - services/api-ts/src/handlers/billing/getInvoice.ts (billing handler pattern)
    - services/api-ts/src/handlers/billing/captureInvoicePayment.ts (payment action handler)
    - services/api-ts/src/handlers/billing/repos/ (ls for repo class names — likely InvoiceRepository, MerchantAccountRepository)
  </read_first>
  <action>
    1. Create 15 test files for documents handlers:
       - Document CRUD: createDocument, getDocument, updateDocument, deleteDocument, searchDocuments, archiveDocument
       - Versions: getDocumentVersion, listDocumentVersions, uploadNewDocumentVersion
       - Access logs: getDocumentAccessLog
       - Tags: createDocumentTag, getDocumentTag, listDocumentTags, updateDocumentTag, deleteDocumentTag
       - Documents likely require org context + auth

    2. Create 11 test files for billing handlers:
       - Invoice: getInvoice, finalizeInvoice, updateInvoice, deleteInvoice, markInvoiceUncollectible
       - Payment: captureInvoicePayment, refundInvoicePayment
       - Merchant: getMerchantAccount, createMerchantAccount, onboardMerchantAccount, getMerchantDashboard
       - Billing handlers may require officer/admin auth — read each to determine

    3. For `uploadNewDocumentVersion`: this handler may involve file upload. Stub any storage/S3 interactions. Focus on the auth and validation logic, not the actual upload.

    4. For billing payment handlers: these interact with Stripe or payment repos. Stub all external service calls. Test auth + basic response shape.

    Same pattern as all other handler tests: makeCtx + stubRepo + restoreRepo in beforeEach/afterEach.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/documents/ && bun test src/handlers/billing/</automated>
  </verify>
  <acceptance_criteria>
    - 15 documents test files created
    - 11 billing test files created
    - All repos stubbed (no real DB or S3 calls)
    - All tests pass
  </acceptance_criteria>
  <done>Documents and billing handlers fully tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test-only plan; no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | N/A | test files | accept | Tests only; no production attack surface |
</threat_model>

<verification>
```bash
cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/notifs/ && bun test src/handlers/platformadmin/ && bun test src/handlers/documents/ && bun test src/handlers/billing/
```
</verification>

<success_criteria>
- 47 new test files total
- All tests pass with no regressions
- Full suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/25-email-notif-guards-handler-tests/25-05-SUMMARY.md`
</output>
