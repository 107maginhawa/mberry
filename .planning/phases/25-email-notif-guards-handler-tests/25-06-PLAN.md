---
phase: 25-email-notif-guards-handler-tests
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - services/api-ts/src/handlers/training/createAccreditedProvider.test.ts
  - services/api-ts/src/handlers/training/updateAccreditedProvider.test.ts
  - services/api-ts/src/handlers/training/deleteAccreditedProvider.test.ts
  - services/api-ts/src/handlers/training/listEnrollments.test.ts
  - services/api-ts/src/handlers/training/listAccreditedProviders.test.ts
  - services/api-ts/src/handlers/reviews/listReviews.test.ts
  - services/api-ts/src/handlers/reviews/deleteReview.test.ts
  - services/api-ts/src/handlers/reviews/getReview.test.ts
  - services/api-ts/src/handlers/events/listRegistrations.test.ts
  - services/api-ts/src/handlers/system/readiness.test.ts
  - services/api-ts/src/handlers/system/liveness.test.ts
  - services/api-ts/src/handlers/system/listFeatureFlags.test.ts
  - services/api-ts/src/handlers/booking/getBooking.test.ts
  - services/api-ts/src/handlers/booking/listBookings.test.ts
  - services/api-ts/src/handlers/booking/getBookingEvent.test.ts
  - services/api-ts/src/handlers/booking/listBookingEvents.test.ts
  - services/api-ts/src/handlers/booking/deleteBookingEvent.test.ts
  - services/api-ts/src/handlers/booking/updateBookingEvent.test.ts
  - services/api-ts/src/handlers/booking/getTimeSlot.test.ts
  - services/api-ts/src/handlers/booking/listEventSlots.test.ts
  - services/api-ts/src/handlers/booking/markNoShowBooking.test.ts
  - services/api-ts/src/handlers/booking/rejectBooking.test.ts
  - services/api-ts/src/handlers/booking/getScheduleException.test.ts
  - services/api-ts/src/handlers/booking/listScheduleExceptions.test.ts
  - services/api-ts/src/handlers/booking/createScheduleException.test.ts
  - services/api-ts/src/handlers/booking/updateScheduleException.test.ts
  - services/api-ts/src/handlers/booking/deleteScheduleException.test.ts
  - services/api-ts/src/handlers/membership/getOrgProfile.test.ts
  - services/api-ts/src/handlers/membership/listOrgApplications.test.ts
  - services/api-ts/src/handlers/membership/listOrgMembers.test.ts
  - services/api-ts/src/handlers/membership/updateOrgProfile.test.ts
  - services/api-ts/src/handlers/invite/validateInvite.test.ts
autonomous: true
requirements: [EML-05]

must_haves:
  truths:
    - "All 5 training handlers have unit tests"
    - "All 3 reviews handlers have unit tests"
    - "The 1 untested events handler has a unit test"
    - "All 3 system handlers have unit tests"
    - "All 15 booking handlers have unit tests"
    - "All 4 membership handlers have unit tests"
    - "The 1 untested invite handler has a unit test"
  artifacts:
    - path: "services/api-ts/src/handlers/training/createAccreditedProvider.test.ts"
      provides: "Training handler tests"
      contains: "describe"
    - path: "services/api-ts/src/handlers/booking/getBooking.test.ts"
      provides: "Booking handler tests"
      contains: "describe"
  key_links: []
---

<objective>
Add unit test coverage for the remaining 32 untested handlers across training (5), reviews (3), events (1), system (3), booking (15), membership (4), and invite (1).

Purpose: EML-05 final batch. Completes the handler test coverage goal for all non-association:member, non-comms, non-storage handlers.

Output: 32 test files covering all remaining untested handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From services/api-ts/src/test-utils/make-ctx.ts:
```typescript
export function makeCtx(overrides: Record<string, any> = {}): any;
export function stubRepo(RepoClass: any, methods: Record<string, Function>): void;
export function restoreRepo(RepoClass: any): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Training + reviews + events + system tests (12 handlers)</name>
  <files>
    services/api-ts/src/handlers/training/*.test.ts (5 new)
    services/api-ts/src/handlers/reviews/*.test.ts (3 new)
    services/api-ts/src/handlers/events/listRegistrations.test.ts
    services/api-ts/src/handlers/system/*.test.ts (3 new)
  </files>
  <read_first>
    - services/api-ts/src/handlers/training/createAccreditedProvider.ts (accredited provider pattern)
    - services/api-ts/src/handlers/training/repos/ (ls for repo class names)
    - services/api-ts/src/handlers/reviews/getReview.ts (review handler pattern)
    - services/api-ts/src/handlers/reviews/repos/ (ls for repo class names)
    - services/api-ts/src/handlers/events/listRegistrations.ts (registration list pattern)
    - services/api-ts/src/handlers/events/repos/ (ls for repo class names)
    - services/api-ts/src/handlers/system/readiness.ts (health check — may not need auth)
    - services/api-ts/src/handlers/system/liveness.ts (health check)
    - services/api-ts/src/handlers/system/listFeatureFlags.ts (may need admin auth)
  </read_first>
  <action>
    Create 12 test files:

    1. Training (5): createAccreditedProvider, updateAccreditedProvider, deleteAccreditedProvider, listEnrollments, listAccreditedProviders
       - These are officer-level handlers added in Phase 22
       - Read repo classes from training/repos/

    2. Reviews (3): listReviews, deleteReview, getReview
       - Standard CRUD pattern

    3. Events (1): listRegistrations
       - Read handler to understand registration query pattern

    4. System (3): readiness, liveness, listFeatureFlags
       - Health checks (readiness/liveness) are PUBLIC endpoints — test they return 200 without auth
       - listFeatureFlags may require auth or be public — read handler first

    Each test: auth check (unless public) + happy path. Stub all repos.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/training/ && bun test src/handlers/reviews/ && bun test src/handlers/events/listRegistrations.test.ts && bun test src/handlers/system/</automated>
  </verify>
  <acceptance_criteria>
    - 12 new test files created
    - System health checks tested as public (no auth required)
    - All tests pass
  </acceptance_criteria>
  <done>Training, reviews, events, system handlers fully tested</done>
</task>

<task type="auto">
  <name>Task 2: Booking + membership + invite tests (20 handlers)</name>
  <files>
    services/api-ts/src/handlers/booking/*.test.ts (15 new)
    services/api-ts/src/handlers/membership/*.test.ts (4 new)
    services/api-ts/src/handlers/invite/validateInvite.test.ts
  </files>
  <read_first>
    - services/api-ts/src/handlers/booking/getBooking.ts (booking handler pattern)
    - services/api-ts/src/handlers/booking/markNoShowBooking.ts (state change handler)
    - services/api-ts/src/handlers/booking/rejectBooking.ts (state change handler)
    - services/api-ts/src/handlers/booking/repos/ (ls for repo class names — likely BookingRepository, BookingEventRepository, ScheduleExceptionRepository, TimeSlotRepository)
    - services/api-ts/src/handlers/membership/getOrgProfile.ts (membership handler pattern)
    - services/api-ts/src/handlers/membership/repos/ (ls for repo class names)
    - services/api-ts/src/handlers/invite/validateInvite.ts (invite validation — may be public)
    - services/api-ts/src/handlers/invite/repos/ (ls for repo class names)
  </read_first>
  <action>
    Create 20 test files:

    1. Booking (15):
       - Core booking: getBooking, listBookings
       - Events: getBookingEvent, listBookingEvents, deleteBookingEvent, updateBookingEvent
       - Slots: getTimeSlot, listEventSlots
       - Actions: markNoShowBooking, rejectBooking
       - Schedule exceptions: getScheduleException, listScheduleExceptions, createScheduleException, updateScheduleException, deleteScheduleException
       - Booking has multiple repos (booking, event, slot, exception) — identify which repo each handler uses

    2. Membership (4): getOrgProfile, listOrgApplications, listOrgMembers, updateOrgProfile
       - These are org-scoped handlers (require org context)

    3. Invite (1): validateInvite
       - May be public (no auth) — read handler to determine

    Each test: auth check + happy path. Stub all repos. restoreRepo in beforeEach/afterEach.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/booking/ && bun test src/handlers/membership/ && bun test src/handlers/invite/</automated>
  </verify>
  <acceptance_criteria>
    - 15 booking test files created
    - 4 membership test files created
    - 1 invite test file created
    - All tests pass
  </acceptance_criteria>
  <done>Booking, membership, invite handlers fully tested — EML-05 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test-only plan; no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-13 | N/A | test files | accept | Tests only; no production attack surface |
</threat_model>

<verification>
```bash
cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/training/ && bun test src/handlers/reviews/ && bun test src/handlers/events/ && bun test src/handlers/system/ && bun test src/handlers/booking/ && bun test src/handlers/membership/ && bun test src/handlers/invite/
```

Final EML-05 verification (all handlers have test files):
```bash
cd /Users/elad-mini/Desktop/memberry/services/api-ts/src/handlers && for dir in communication person notifs platformadmin documents billing training reviews events system booking membership invite; do untested=$(find "$dir" -maxdepth 1 -name "*.ts" ! -name "*.test.ts" ! -name "*.schema.ts" ! -name "index.ts" | while read f; do base="${f%.ts}"; if [ ! -f "${base}.test.ts" ]; then echo "$f"; fi; done | wc -l); echo "$dir: $untested untested"; done
```
All modules should show 0 untested.
</verification>

<success_criteria>
- 32 new test files total
- All tests pass with no regressions
- EML-05 fully satisfied: 0 untested handlers across all 13 modules in scope
</success_criteria>

<output>
After completion, create `.planning/phases/25-email-notif-guards-handler-tests/25-06-SUMMARY.md`
</output>
