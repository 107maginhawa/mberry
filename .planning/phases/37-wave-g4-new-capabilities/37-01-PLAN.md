---
phase: 37-wave-g4-new-capabilities
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/api-ts/src/core/auth.ts
  - services/api-ts/src/core/auth.test.ts
autonomous: true
requirements: [V-15]

must_haves:
  truths:
    - "Better-Auth configured with session.maximumActiveDevices or equivalent limit"
    - "Users exceeding session limit get oldest session revoked automatically"
    - "Default limit is 5 concurrent sessions (configurable via config)"
    - "Tests verify session limit enforcement"
  artifacts:
    - path: "services/api-ts/src/core/auth.ts"
      provides: "Session limit configuration in betterAuth() call"
      contains: "maximumActive"
---

<objective>
Add concurrent session limits via Better-Auth configuration (V-15).

Currently no limit on concurrent sessions per user. Add `session.maximumActiveDevices: 5` (or equivalent Better-Auth config) to enforce a limit. When exceeded, oldest session is automatically revoked.

Better-Auth supports this natively — check their docs for the exact config key. It may be `session.maximumActiveDevices`, `session.maxSessions`, or similar.
</objective>

<tasks>
1. **Research Better-Auth session limit config**
   - Read `services/api-ts/src/core/auth.ts` to understand current betterAuth() config
   - Check Better-Auth docs/types for session limit options
   - Identify the correct config key

2. **RED — Write test asserting session limit behavior**
   - Test: creating 6th session triggers oldest session revocation
   File: `services/api-ts/src/core/auth.test.ts` (or new file)

3. **GREEN — Add session limit to Better-Auth config**
   - Add the session limit config to `betterAuth()` call in `auth.ts`
   - Make limit configurable via `config.ts` with default of 5
   File: `services/api-ts/src/core/auth.ts`

4. **Verify auth tests pass**
   Run: `cd services/api-ts && bun test core/`
</tasks>
