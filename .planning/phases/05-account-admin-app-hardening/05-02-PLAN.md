---
phase: 05-account-admin-app-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/account/tests/e2e/activation.spec.ts
  - apps/memberry/tests/e2e/security.spec.ts
autonomous: true
requirements: [TEST-05]

must_haves:
  truths:
    - "Account app activation test verifies the app boots and renders homepage content"
    - "Memberry security spec verifies unauthenticated user gets redirected to sign-in"
    - "Memberry security spec verifies authenticated user can access security settings"
  artifacts:
    - path: "apps/account/tests/e2e/activation.spec.ts"
      provides: "Minimal account app activation E2E test"
      contains: "localhost:3002"
    - path: "apps/memberry/tests/e2e/security.spec.ts"
      provides: "Memberry security flow E2E tests"
      contains: "signIn"
  key_links:
    - from: "apps/memberry/tests/e2e/security.spec.ts"
      to: "apps/memberry/tests/e2e/helpers/auth.ts"
      via: "import signIn"
      pattern: "import.*signIn.*from.*helpers/auth"
---

<objective>
Write account app activation test and memberry security flow spec.

Purpose: Satisfy TEST-05 — account app activation check (minimal per CONTEXT.md) + memberry security flow gap fill.
Output: Two new spec files — one in account app, one in memberry app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-account-admin-app-hardening/05-PATTERNS.md

<interfaces>
<!-- Account app has no auth helper — test is unauthenticated smoke check -->

Account app baseURL: http://localhost:3002 (from playwright.config.ts)
Account app webServer: COMMENTED OUT in playwright.config.ts — must boot manually before running

From apps/memberry/tests/e2e/helpers/auth.ts:
```typescript
export async function signIn(page: Page, email: string, password: string): Promise<void>
export async function signUp(page: Page): Promise<{ email: string; password: string; name: string }>
```

Seeded member credentials: email='test@memberry.ph', password='TestPass123!'
Memberry app baseURL: http://localhost:3004
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Account app activation spec</name>
  <files>apps/account/tests/e2e/activation.spec.ts</files>
  <read_first>
    - apps/account/tests/e2e/onboarding.spec.ts (analog — account app E2E pattern)
    - apps/account/playwright.config.ts (baseURL, webServer config)
    - apps/account/src/routes/index.tsx (homepage content to assert against)
  </read_first>
  <action>
Create minimal activation spec per CONTEXT.md locked decision: "Account app: minimal test -- just verify account is activated."

**activation.spec.ts:**
- Import `{ test, expect }` from `@playwright/test`
- test.describe('Account app activation'):
  1. Test: "landing page renders for unauthenticated user"
     - `await page.goto('/')`
     - `await page.waitForLoadState('networkidle')`
     - Assert homepage content is visible. Read `apps/account/src/routes/index.tsx` to find the actual heading text (likely "Smart Scheduling" or similar). Use `page.locator('body')` with `toContainText()` or `page.getByRole('heading')` with the actual text found.
  2. Test: "auth redirect works — sign-in page loads"
     - `await page.goto('/auth/sign-in')`
     - `await page.waitForLoadState('networkidle')`
     - Assert sign-in form visible: `await expect(page.locator('input[name="email"]')).toBeVisible()`

Only 2 tests. Do NOT add booking, settings, or security tests for account app — those are deferred per CONTEXT.md.
  </action>
  <verify>
    <automated>cd apps/account && bun run test:e2e 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/account/tests/e2e/activation.spec.ts contains `test.describe('Account app activation'`
    - apps/account/tests/e2e/activation.spec.ts contains `page.goto('/')`
    - apps/account/tests/e2e/activation.spec.ts contains `waitForLoadState('networkidle')`
    - apps/account/tests/e2e/activation.spec.ts contains `page.goto('/auth/sign-in')`
    - apps/account/tests/e2e/activation.spec.ts does NOT contain `booking` or `/bookings` (deferred)
  </acceptance_criteria>
  <done>Account activation spec has exactly 2 tests. Both pass against running account app on port 3002.</done>
</task>

<task type="auto">
  <name>Task 2: Memberry security flow spec</name>
  <files>apps/memberry/tests/e2e/security.spec.ts</files>
  <read_first>
    - apps/memberry/tests/e2e/officer/guard-enforcement.spec.ts (analog — auth guard redirect pattern)
    - apps/memberry/tests/e2e/auth.spec.ts (unauthenticated redirect pattern, lines 136-146)
    - apps/memberry/tests/e2e/settings.spec.ts (security section test C3 — already exists, check for overlap)
    - apps/memberry/tests/e2e/helpers/auth.ts (signIn helper)
  </read_first>
  <action>
Create security.spec.ts covering security-specific flows that are NOT already in settings.spec.ts (which covers C3: security heading + portal link) or auth.spec.ts (which covers A2: unauthenticated redirect).

**security.spec.ts:**
- Add comment: `// Business Rules: Security flow coverage for Phase 5 hardening`
- Import `{ test, expect }` from `@playwright/test`
- Import `{ signIn }` from `./helpers/auth`
- test.describe('Security Flows'):
  1. Test: "unauthenticated user cannot access member dashboard"
     - `await page.context().clearCookies()`
     - `await page.goto('/my/dashboard')`
     - `await page.waitForLoadState('networkidle')`
     - `await page.waitForTimeout(2000)`
     - `expect(page.url()).toContain('/auth/')`
  2. Test: "unauthenticated user cannot access settings"
     - `await page.context().clearCookies()`
     - `await page.goto('/my/settings')`
     - `await page.waitForLoadState('networkidle')`
     - `await page.waitForTimeout(2000)`
     - `expect(page.url()).toContain('/auth/')`
  3. Test: "authenticated user can access protected routes"
     - `await signIn(page, 'test@memberry.ph', 'TestPass123!')`
     - `await page.goto('/my/dashboard')`
     - `await page.waitForLoadState('networkidle')`
     - Assert page does NOT redirect to auth: `expect(page.url()).not.toContain('/auth/')`
     - Assert some dashboard content is visible (heading or card)
  4. Test: "error case — invalid credentials show error"
     - `await page.goto('/auth/sign-in')`
     - `await page.waitForLoadState('networkidle')`
     - Fill email `nonexistent@test.com` and password `WrongPass123!`
     - Submit form
     - Wait for error indicator (toast, error message, or form stays on sign-in page)
     - `expect(page.url()).toContain('/auth/')`

Happy path + one error case per flow, as specified in CONTEXT.md.
  </action>
  <verify>
    <automated>cd apps/memberry && bun run test:e2e -- --grep "Security" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/memberry/tests/e2e/security.spec.ts contains `test.describe('Security Flows'`
    - apps/memberry/tests/e2e/security.spec.ts contains `import { signIn } from './helpers/auth'`
    - apps/memberry/tests/e2e/security.spec.ts contains `clearCookies()`
    - apps/memberry/tests/e2e/security.spec.ts contains `expect(page.url()).toContain('/auth/')`
    - apps/memberry/tests/e2e/security.spec.ts contains `test@memberry.ph`
  </acceptance_criteria>
  <done>Security spec has 4 tests (2 unauthenticated redirects, 1 authenticated access, 1 invalid credentials error). All pass against running memberry app on port 3004.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> memberry app | Tests exercise auth guard behavior via browser navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | S (Spoofing) | Auth redirect tests | mitigate | Tests explicitly verify unauthenticated users cannot bypass auth guards |
| T-05-04 | I (Info Disclosure) | Seeded credentials in spec | accept | Only seeded test account credentials; standard E2E pattern |
</threat_model>

<verification>
Account spec passes: `cd apps/account && bun run test:e2e`
Security spec passes: `cd apps/memberry && bun run test:e2e -- --grep "Security"`
</verification>

<success_criteria>
- activation.spec.ts has 2 tests (homepage render, auth page render)
- security.spec.ts has 4 tests (2 unauthenticated, 1 authenticated, 1 error case)
- No overlap with existing settings.spec.ts C3 or auth.spec.ts A2
- All pass locally
</success_criteria>

<output>
After completion, create `.planning/phases/05-account-admin-app-hardening/05-02-SUMMARY.md`
</output>
