---
phase: 03-data-model-unification
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - services/api-ts/src/app.ts
  - services/api-ts/src/middleware/org-context.ts
  - services/api-ts/src/middleware/org-context.test.ts
  - services/api-ts/src/handlers/training/repos/training.types.ts
  - services/api-ts/src/handlers/events/repos/events.types.ts
  - services/api-ts/src/handlers/membership/repos/membership.types.ts
  - services/api-ts/src/handlers/dues/repos/dues.types.ts
  - services/api-ts/src/handlers/communications/repos/communication.types.ts
  - services/api-ts/src/handlers/certificates/repos/certificates.types.ts
  - services/api-ts/src/handlers/elections/repos/elections.types.ts
  - services/api-ts/src/handlers/documents/repos/documents.types.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "No handler reads tenantId from context — all use orgId"
    - "No *.types.ts file defines a pgTable that duplicates a *.schema.ts definition"
    - "All routes in app.ts resolve to working handler modules"
    - "org-context middleware no longer sets ctx.var.tenantId"
  artifacts:
    - path: "services/api-ts/src/middleware/org-context.ts"
      provides: "Middleware without tenantId alias"
    - path: "services/api-ts/src/app.ts"
      provides: "Route registrations pointing to canonical handlers"
  key_links:
    - from: "services/api-ts/src/app.ts"
      to: "handlers/association:*/index.ts"
      via: "route registration"
      pattern: "app.route"
    - from: "services/api-ts/src/middleware/org-context.ts"
      to: "handler context reads"
      via: "ctx.get('orgId')"
      pattern: "ctx\\.get\\('orgId'\\)"
---

<objective>
Remove duplicate old handler modules, eliminate tenantId context alias from middleware, update all handler code to use orgId instead of tenantId, and standardize enum naming to camelCase. Per D-03, D-06, D-10.

Purpose: Eliminate translation glue code and duplicate module definitions so each entity is defined exactly once.
Output: Clean handler structure with no tenantId references in runtime code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-model-unification/03-RESEARCH.md
@.planning/phases/03-data-model-unification/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tenantId from middleware and handler context reads</name>
  <files>
    services/api-ts/src/middleware/org-context.ts
    services/api-ts/src/middleware/org-context.test.ts
    services/api-ts/src/core/types.ts
  </files>
  <read_first>
    - services/api-ts/src/middleware/org-context.ts
    - services/api-ts/src/middleware/org-context.test.ts
    - services/api-ts/src/core/types.ts
  </read_first>
  <action>
Per D-03 (remove translation glue):

1. **org-context.ts**: Remove both `ctx.set('tenantId', orgId)` lines (lines 58 and 96). Keep `ctx.set('orgId', orgId)` — that is the canonical context variable.

2. **core/types.ts** (or wherever `Variables` type is defined): Remove `tenantId: string` from the Variables interface. Keep `orgId: string`.

3. **org-context.test.ts**: Update test assertion from `expect(vars['tenantId']).toBe('org-1')` to verify `vars['orgId']` instead. Remove any tenantId-specific test expectations.

4. **All handler files** that read `ctx.get('tenantId')` or `ctx.var.tenantId`: Change to `ctx.get('orgId')` or `ctx.var.orgId`. Find these with:
   ```bash
   grep -r "ctx.get('tenantId')\|ctx.var.tenantId\|ctx\.get(\"tenantId\")" services/api-ts/src/handlers/
   ```
   Update each occurrence to use `orgId`.

5. **All repo/handler files** that pass `tenantId` to query builders: Change parameter name from `tenantId` to `organizationId` to match the updated schema field name. E.g.:
   ```typescript
   // BEFORE: .where(eq(trainings.tenantId, tenantId))
   // AFTER:  .where(eq(trainings.organizationId, organizationId))
   ```

This is the most impactful change — it touches many handler files. Use grep to find ALL occurrences and update systematically.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && grep -r "tenantId" src/middleware/ src/core/types.ts 2>/dev/null | grep -v node_modules | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `tenantId` in `src/middleware/org-context.ts`
    - Zero occurrences of `tenantId` in `src/core/types.ts` (or equivalent Variables type file)
    - `org-context.test.ts` tests pass with `bun test src/middleware/org-context.test.ts`
    - `grep -r "ctx.get('tenantId')" src/handlers/` returns 0 matches
    - `grep -r "ctx.var.tenantId" src/handlers/` returns 0 matches
  </acceptance_criteria>
  <done>All runtime code uses orgId/organizationId exclusively. No tenantId in middleware, types, or handler context reads.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate old handler modules into association:* structure</name>
  <files>
    services/api-ts/src/app.ts
    services/api-ts/src/handlers/training/
    services/api-ts/src/handlers/events/
    services/api-ts/src/handlers/membership/
    services/api-ts/src/handlers/dues/
    services/api-ts/src/handlers/communications/
    services/api-ts/src/handlers/certificates/
    services/api-ts/src/handlers/elections/
    services/api-ts/src/handlers/documents/
  </files>
  <read_first>
    - services/api-ts/src/app.ts
    - services/api-ts/src/handlers/training/index.ts
    - services/api-ts/src/handlers/events/index.ts
    - services/api-ts/src/handlers/membership/index.ts
    - services/api-ts/src/handlers/dues/index.ts
    - services/api-ts/src/handlers/communications/index.ts
    - services/api-ts/src/handlers/certificates/index.ts
    - services/api-ts/src/handlers/elections/index.ts
    - services/api-ts/src/handlers/association:operations/repos/training.schema.ts
    - services/api-ts/src/handlers/association:member/repos/membership.schema.ts
  </read_first>
  <action>
Per D-06 (each entity defined ONCE) and RESEARCH Pattern 4 (old module deletion):

**Strategy:** The old modules (`training/`, `events/`, `membership/`, `dues/`, `communications/`, `certificates/`, `elections/`) contain the ACTIVE route handlers wired in `app.ts`. The `association:*` directories contain the canonical `*.schema.ts` files. The old modules use `*.types.ts` for schema — these are the DUPLICATES to eliminate.

**For each old module:**

1. **Delete `*.types.ts` files** from old module `repos/` directories. These are the duplicate schema definitions that conflict with the canonical `*.schema.ts` files in `association:*`.

2. **Update imports in old handler files**: Change imports from local `./repos/training.types` to the canonical schema location:
   ```typescript
   // BEFORE:
   import { trainings } from './repos/training.types';
   // AFTER:
   import { trainings } from '@/handlers/association:operations/repos/training.schema';
   ```

3. **For `elections/`** (no association:* equivalent per Research): Rename `elections/repos/elections.types.ts` → `elections/repos/elections.schema.ts`. Update its content to use `organizationId` instead of `tenantId`. This makes it a canonical schema file picked up by Drizzle glob.

4. **Update `app.ts`**: Keep the same route paths (`/dues`, `/membership`, etc.) but verify imports still resolve after *.types.ts deletion. The route handlers in the old directories are still the active ones — only the duplicate SCHEMA definitions are being removed.

5. **Remove `tenantId` from any remaining *.types.ts → *.schema.ts renames:** All converted files must use `organizationId: uuid('organization_id')` exclusively.

IMPORTANT: Do NOT delete the old handler directories themselves (they contain active route handler code like `createTraining.ts`, `listEvents.ts`, etc.). Only delete/consolidate the `*.types.ts` schema files.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && grep -r "types" src/handlers/{training,events,membership,dues,communications,certificates,elections}/repos/ 2>/dev/null | grep "\.types" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero `*.types.ts` files remain in old handler module `repos/` directories (except elections.schema.ts which was renamed)
    - All handler files import from `association:*/repos/*.schema` or local `*.schema.ts` (no *.types.ts imports)
    - `bun run typecheck` passes (all imports resolve)
    - `app.ts` still registers all 7 route groups (`/dues`, `/membership`, `/communications`, `/certificates`, `/events`, `/training`, `/elections`)
    - No `pgTable` definition for the same DB table name appears in more than one file
  </acceptance_criteria>
  <done>Each DB entity has exactly one schema definition. Old *.types.ts duplicates eliminated. All handler imports point to canonical schema files.</done>
</task>

<task type="auto">
  <name>Task 3: Standardize enum naming to camelCase (D-10)</name>
  <files>
    services/api-ts/src/handlers/association:operations/repos/training.schema.ts
    services/api-ts/src/handlers/association:operations/repos/events.schema.ts
    services/api-ts/src/handlers/association:member/repos/membership.schema.ts
    services/api-ts/src/handlers/association:member/repos/dues.schema.ts
    services/api-ts/src/handlers/association:member/repos/governance.schema.ts
  </files>
  <read_first>
    - services/api-ts/src/handlers/association:operations/repos/training.schema.ts
    - services/api-ts/src/handlers/association:operations/repos/events.schema.ts
    - services/api-ts/src/handlers/association:member/repos/membership.schema.ts
  </read_first>
  <action>
Per D-10 (keep per-module enums but standardize to camelCase naming):

1. Review all `pgEnum` definitions across schema files. Ensure enum VALUE strings use camelCase:
   - `'pending_approval'` → `'pendingApproval'`
   - `'in_progress'` → `'inProgress'`
   - Single-word values (`'draft'`, `'active'`, `'cancelled'`) stay as-is (already camelCase)

2. Check actual DB enum values with a comment noting what exists. Since this is pre-launch (Assumption A4), we can ALTER TYPE to rename enum values:
   ```sql
   -- Add to migration if needed, or create 0015_enum_standardization.sql
   ALTER TYPE training_status RENAME VALUE 'pending_approval' TO 'pendingApproval';
   ```

3. Update handler code that compares against old enum string values:
   ```typescript
   // BEFORE: if (training.status === 'pending_approval')
   // AFTER:  if (training.status === 'pendingApproval')
   ```

4. If enum values in the DB are already single-word (draft, published, cancelled, completed), no rename needed — they are already valid camelCase.

NOTE: Per D-10, keep per-module enums as genuinely different state machines. Do NOT merge `trainingStatus` with `eventStatus` — they represent different workflows.
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && grep -r "pgEnum" src/handlers/ | grep -o "'[a-z_]*'" | grep "_" | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - No enum VALUE contains underscores (all camelCase or single-word)
    - pgEnum TYPE NAMES can stay snake_case (that's the PostgreSQL convention for type names)
    - Any migration for enum renames is written as `0015_enum_standardization.sql`
    - Handler comparisons use camelCase enum values
    - `bun run typecheck` passes
  </acceptance_criteria>
  <done>All enum values standardized to camelCase per D-10. Per-module enum types preserved as separate state machines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → handlers | orgId context must be set before handler execution |
| handler → DB query | organizationId WHERE clause must scope all queries to prevent cross-org access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | handler queries missing org scoping | mitigate | grep gate: `grep -r "\.where\(" src/handlers/ | grep -v "organizationId\|orgId"` — verify all queries include org filter |
| T-03-04 | Elevation of Privilege | org-context.ts no longer sets tenantId | mitigate | Verify no handler still reads tenantId after Task 1; typecheck catches missing property |
| T-03-05 | Tampering | enum value mismatch between DB and code | accept | Pre-launch; enum rename is atomic ALTER TYPE |
</threat_model>

<verification>
- `grep -r "tenantId" services/api-ts/src/handlers/ services/api-ts/src/middleware/` returns 0 matches (excluding test files that test the OLD behavior)
- `bun run typecheck` passes
- `bun test src/middleware/org-context.test.ts` passes
- No `*.types.ts` files with `pgTable` definitions remain in old handler directories
</verification>

<success_criteria>
- Translation glue code (tenantId alias) fully removed from runtime
- Each DB entity has exactly one schema definition (DATA-01)
- All enum values use camelCase (DATA-02)
- No handler references tenantId from context (DATA-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-model-unification/03-02-SUMMARY.md`
</output>
