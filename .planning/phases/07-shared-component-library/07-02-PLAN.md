---
phase: 07-shared-component-library
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - packages/ui/.ladle/config.mjs
  - packages/ui/.ladle/components.tsx
  - packages/ui/src/ladle-globals.css
  - packages/ui/src/stories/button.stories.tsx
  - packages/ui/src/stories/card.stories.tsx
  - packages/ui/src/stories/input.stories.tsx
  - packages/ui/src/stories/badge.stories.tsx
  - packages/ui/src/stories/dialog.stories.tsx
  - packages/ui/src/stories/table.stories.tsx
  - packages/ui/src/stories/tabs.stories.tsx
  - packages/ui/src/stories/form.stories.tsx
  - packages/ui/package.json
autonomous: false
requirements: [UINF-02]

must_haves:
  truths:
    - "Running 'bun run storybook' in packages/ui launches Ladle preview"
    - "At least 8 components have Ladle stories showing variants"
    - "Stories render with correct shadcn theming (CSS variables applied)"
  artifacts:
    - path: "packages/ui/.ladle/config.mjs"
      provides: "Ladle configuration"
      contains: "stories"
    - path: "packages/ui/.ladle/components.tsx"
      provides: "Global CSS and providers for Ladle stories"
      contains: "ladle-globals.css"
    - path: "packages/ui/src/stories/button.stories.tsx"
      provides: "Button component stories with variants"
      contains: "Story"
  key_links:
    - from: "packages/ui/.ladle/components.tsx"
      to: "packages/ui/src/ladle-globals.css"
      via: "CSS import"
      pattern: "import.*ladle-globals"
    - from: "packages/ui/src/stories/button.stories.tsx"
      to: "packages/ui/src/components/button.tsx"
      via: "component import"
      pattern: "from.*components/button"
---

<objective>
Add Ladle component preview to `packages/ui`. Create stories for the most commonly used components so developers can browse and test variants in isolation.

Purpose: Provide component documentation and visual testing without full Storybook overhead.
Output: Ladle serves at localhost with 8+ component stories showing all variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-shared-component-library/07-RESEARCH.md
@.planning/phases/07-shared-component-library/07-01-SUMMARY.md

@packages/ui/package.json
@packages/ui/src/components/button.tsx
@apps/account/src/styles/globals.css (for CSS variable reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Ladle and create story files</name>
  <files>packages/ui/package.json, packages/ui/.ladle/config.mjs, packages/ui/.ladle/components.tsx, packages/ui/src/ladle-globals.css, packages/ui/src/stories/*.stories.tsx</files>
  <read_first>
    - packages/ui/package.json
    - packages/ui/src/components/button.tsx
    - packages/ui/src/components/card.tsx
    - packages/ui/src/components/input.tsx
    - packages/ui/src/components/badge.tsx
    - packages/ui/src/components/dialog.tsx
    - packages/ui/src/components/table.tsx
    - packages/ui/src/components/tabs.tsx
    - packages/ui/src/components/form.tsx
    - apps/account/src/styles/globals.css (extract shadcn CSS variable block)
  </read_first>
  <action>
1. Run `npm view @ladle/react version` to verify current version. Install:
   `cd /Users/elad-mini/Desktop/memberry/packages/ui && bun add -D @ladle/react`

2. Create `packages/ui/src/ladle-globals.css` with the shadcn CSS variable defaults extracted from account's globals.css. Include:
   - :root block with all --background, --foreground, --primary, --secondary, etc. CSS variables
   - .dark block with dark theme variables
   - Base tailwind directives: @tailwind base; @tailwind components; @tailwind utilities;
   - The body base styles from shadcn

3. Create `packages/ui/.ladle/config.mjs`:
   ```js
   export default {
     stories: "src/stories/**/*.stories.tsx",
   }
   ```

4. Create `packages/ui/.ladle/components.tsx` — global decorator that imports the CSS:
   ```tsx
   import "../src/ladle-globals.css"
   import type { GlobalProvider } from "@ladle/react"

   export const Provider: GlobalProvider = ({ children }) => (
     <div className="p-4">{children}</div>
   )
   ```

5. Create story files in `packages/ui/src/stories/` for these 8 components:
   - **button.stories.tsx**: Default, Destructive, Outline, Secondary, Ghost, Link, Small, Large, Icon, Loading (disabled)
   - **card.stories.tsx**: Default with CardHeader/CardTitle/CardDescription/CardContent/CardFooter
   - **input.stories.tsx**: Default, Disabled, With placeholder, With label (using Label component)
   - **badge.stories.tsx**: Default, Secondary, Destructive, Outline
   - **dialog.stories.tsx**: Basic dialog with trigger, title, description, action buttons
   - **table.stories.tsx**: Table with TableHeader/TableBody/TableRow/TableHead/TableCell showing sample data
   - **tabs.stories.tsx**: TabsList with 3 TabsTrigger items + TabsContent panels
   - **form.stories.tsx**: Simple form with FormField, FormItem, FormLabel, FormControl, FormMessage using react-hook-form

   Each story file imports from `../components/{name}` (relative path). Each story function has `export const VariantName: Story = () => <Component ... />` pattern.

6. Update packages/ui/package.json scripts to include: `"storybook": "ladle serve"`

7. Verify: `cd packages/ui && bun run typecheck`
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/packages/ui && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/.ladle/config.mjs exists
    - packages/ui/.ladle/components.tsx imports ladle-globals.css
    - packages/ui/src/ladle-globals.css contains CSS variable definitions including --primary and --background
    - At least 8 .stories.tsx files exist in packages/ui/src/stories/
    - packages/ui typecheck passes
  </acceptance_criteria>
  <done>Ladle configured with 8+ component stories, CSS variable globals, and typecheck passing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Ladle component preview for packages/ui with 8 component stories showing all variants</what-built>
  <how-to-verify>
    1. Run: `cd /Users/elad-mini/Desktop/memberry/packages/ui && bun run storybook`
    2. Open the URL Ladle prints (usually http://localhost:61000)
    3. Verify the sidebar shows Button, Card, Input, Badge, Dialog, Table, Tabs, Form stories
    4. Click through Button stories — confirm Default (blue/primary), Destructive (red), Outline (bordered), etc. render with correct colors
    5. Check that components are styled (not unstyled raw HTML) — CSS variables are applying
    6. Click Dialog story — confirm dialog opens/closes
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — component preview server runs locally for development only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | N/A | Ladle dev server | accept | Development-only tool, not deployed to production |
</threat_model>

<verification>
- `cd packages/ui && bun run typecheck` exits 0
- `ls packages/ui/src/stories/*.stories.tsx | wc -l` >= 8
- `bun run storybook` launches without errors (verified by human)
</verification>

<success_criteria>
Ladle preview serves all 8+ component stories with proper shadcn theming applied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared-component-library/07-02-SUMMARY.md`
</output>
