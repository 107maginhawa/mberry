---
phase: 15-domain-remediation
plan: "14"
type: execute
wave: 3
depends_on: []
files_modified:
  - services/api-ts/src/handlers/association:member/repos/membership.schema.ts
  - services/api-ts/src/handlers/association:member/repos/governance.schema.ts
  - services/api-ts/src/handlers/dues/repos/dues.schema.ts
  - services/api-ts/src/generated/migrations/0026_missing_fk_constraints.sql
autonomous: true
requirements: [AUDIT-10]
must_haves:
  truths:
    - "FK constraints added for all identified missing references"
    - "Data cleanup handles orphaned records"
  artifacts:
    - path: "services/api-ts/src/handlers/dues/repos/dues.schema.ts"
      provides: "Schema with FK on dues_category_override.categoryId"
    - path: "services/api-ts/src/handlers/association:member/repos/governance.schema.ts"
      provides: "Schema with FKs on election_nominee.nominatedBy and officer_term.positionId"
    - path: "services/api-ts/src/handlers/association:member/repos/membership.schema.ts"
      provides: "Schema with FK on membership_application.reviewedBy"
---

<objective>
Add missing FK constraints for dues_category_override.categoryId (CRITICAL), election_nominee.nominatedBy, membership_application.reviewedBy, and officer_term.positionId.
</objective>

<context>
See 15-CONTEXT.md for key business rules and file inventory.
See 15-PATTERNS.md for test infrastructure and transaction patterns.

Bug: Four FK constraints are missing. The most critical is `dues_category_override.categoryId` — without it, overrides can reference nonexistent categories, causing silent data corruption. The others (`election_nominee.nominatedBy`, `membership_application.reviewedBy`, `officer_term.positionId`) allow orphaned references.
</context>

<tasks>
## Data audit
1. For each missing FK, query for orphaned records:
   - `dues_category_override` rows where `categoryId` has no matching category
   - `election_nominee` rows where `nominatedBy` has no matching person
   - `membership_application` rows where `reviewedBy` has no matching person
   - `officer_term` rows where `positionId` has no matching position
2. Document orphans and decide cleanup (nullify, delete, or backfill)

## Schema changes
3. Add FK references in each schema file:
   - `dues_category_override.categoryId` → `dues_category.id`
   - `election_nominee.nominatedBy` → `person.id`
   - `membership_application.reviewedBy` → `person.id`
   - `officer_term.positionId` → `position.id`

## Migration
4. Write data cleanup migration that handles orphaned records before adding FKs
5. Run `bun run db:generate` to produce the migration SQL

## Verification
6. Apply migration to dev DB
7. Attempt to insert records with invalid FK references — verify DB rejects
8. Verify 2123+ tests still pass
</tasks>
