---
phase: 15-domain-remediation
plan: "06"
type: execute
wave: 1
depends_on: ["01", "04"]
files_modified:
  - services/api-ts/src/handlers/dues/
autonomous: true
requirements: [AUDIT-D2]
must_haves:
  truths:
    - "Dead handler files deleted (refundPayment.ts, recordDuesPayment.ts, etc.)"
    - "Shared utils (fund-math.ts, expiry-extension.ts, settle-payment.ts) preserved"
    - "No broken imports after deletion"
    - "All tests pass"
  artifacts:
    - path: "services/api-ts/src/handlers/dues/"
      provides: "Clean handlers/dues/ directory with only live, imported files remaining"
---

<objective>
Delete dead handler files in handlers/dues/ now that their logic has been inlined into live code (plan 01) and the lifecycle service (plan 04). Preserve shared utils that are still imported.
</objective>

<context>
See [15-CONTEXT.md](./15-CONTEXT.md) and [15-PATTERNS.md](./15-PATTERNS.md) for domain remediation background.

**Depends on plan 01**: Refund logic from `dues/refundPayment.ts` must be inlined into live `refundDuesPayment.ts` first.
**Depends on plan 04**: Lifecycle service must absorb any remaining orchestration logic from dead handlers.

**Dead code candidates in `handlers/dues/`**:
- `refundPayment.ts` — logic inlined to live refundDuesPayment.ts in plan 01
- `recordDuesPayment.ts` — superseded by `association:member/recordDuesPayment.ts`
- Any other handler files that duplicate functionality now in `association:member/`

**Must preserve**:
- `fund-math.ts` — shared utility, likely still imported
- `expiry-extension.ts` — shared utility for expiry computation
- `settle-payment.ts` — still used (refactored in plan 04 to delegate to lifecycle service)
- `getDuesConfig.ts` — still used for config fetching
- Any files with active imports from other modules
</context>

<tasks>
## Identify dead files

1. List all files in `services/api-ts/src/handlers/dues/`
2. For each handler file (not in `utils/` or `repos/`), grep for imports across the codebase
3. Mark files with zero external imports as dead (candidates for deletion)
4. Cross-reference with plan 01 and plan 04 outputs to confirm logic has been migrated

## Delete dead files

5. Delete confirmed dead handler files:
   - `refundPayment.ts` (logic inlined to live handler in plan 01)
   - `recordDuesPayment.ts` (if superseded by association:member version)
   - Any associated dead test files
6. Do NOT delete:
   - `utils/fund-math.ts`
   - `utils/expiry-extension.ts`
   - `utils/settle-payment.ts`
   - `getDuesConfig.ts`
   - `repos/` directory and schema files
   - Any file with active imports

## Verify

7. Run `bun run typecheck` — no broken import errors
8. Run `bun test` in api-ts — all tests pass
9. Grep for any remaining references to deleted files — clean up any stale imports
10. Verify the dues router still registers all live routes correctly
</tasks>
