---
phase: 15-domain-remediation
plan: "15"
type: execute
wave: 3
depends_on: []
files_modified:
  - services/api-ts/src/handlers/association:member/repos/governance.schema.ts
  - services/api-ts/src/handlers/association:member/repos/elections.schema.ts
  - services/api-ts/src/generated/migrations/0027_temporal_checks_and_position_normalization.sql
autonomous: true
requirements: [AUDIT-8, AUDIT-9]
must_haves:
  truths:
    - "Election startDate < endDate enforced at DB level"
    - "Officer term endDate > startDate enforced"
    - "Election positions normalized to FK"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/repos/governance.schema.ts"
      provides: "Schema with CHECK constraints on officer term dates"
    - path: "services/api-ts/src/handlers/association:member/repos/elections.schema.ts"
      provides: "Schema with CHECK constraints on election dates and normalized position FK"
---

<objective>
Add temporal CHECK constraints for date ordering and normalize election positions from JSONB to FK referencing the position table.
</objective>

<context>
See 15-CONTEXT.md for key business rules and file inventory.
See 15-PATTERNS.md for test infrastructure and transaction patterns.

Bug: Officer term overlap, election date ordering, event duration, slot boundaries — all missing CHECK constraints. Election positions are stored as JSONB while `nominee.positionId` is varchar and `governance.position.id` is uuid, creating a type mismatch and no referential integrity.
</context>

<tasks>
## CHECK constraints
1. Add CHECK constraint to elections table: `startDate < endDate`
2. Add CHECK constraint to officer_terms table: `endDate > startDate`
3. Add CHECK constraints for event/slot temporal ordering where applicable

## Position normalization
4. Audit current JSONB positions data in elections table
5. Create migration to:
   - Add `positionId` FK column to election_positions (or normalize the JSONB)
   - Migrate existing JSONB position data to reference `position.id`
   - Ensure `nominee.positionId` type matches `position.id` (both uuid)
6. Drop or deprecate the JSONB positions column after migration

## Migration
7. Run `bun run db:generate` to produce the migration SQL
8. Review generated SQL for correctness

## Verification
9. Apply migration to dev DB
10. Attempt to insert election with endDate < startDate — verify DB rejects
11. Attempt to insert officer term with endDate < startDate — verify DB rejects
12. Verify position FK references resolve correctly
13. Verify 2123+ tests still pass
</tasks>
