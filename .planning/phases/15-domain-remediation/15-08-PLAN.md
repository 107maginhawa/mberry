---
phase: 15-domain-remediation
plan: "08"
type: tdd
wave: 2
depends_on: []
files_modified:
  - services/api-ts/src/handlers/association:member/openElectionVoting.ts
  - services/api-ts/src/handlers/association:member/openElectionVoting.test.ts
autonomous: true
requirements: [BR-33]
must_haves:
  truths:
    - "Cannot open voting if any position has fewer than 2 candidates"
    - "Can open voting when all positions have 2+ candidates"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/openElectionVoting.test.ts"
      provides: "Unit tests enforcing BR-33 two-candidate minimum per position"
    - path: "services/api-ts/src/handlers/association:member/openElectionVoting.ts"
      provides: "Handler with per-position candidate count validation"
---

<objective>
Fix BR-33 violation in openElectionVoting — enforce 2+ candidates per position before opening voting.
</objective>

<context>
See 15-CONTEXT.md for key business rules and file inventory.
See 15-PATTERNS.md for test infrastructure and transaction patterns.

Bug: `services/api-ts/src/handlers/association:member/openElectionVoting.ts:31` only checks `nominees.length > 0` globally. It does not group nominees by position or verify each position has >= 2 candidates, violating BR-33.
</context>

<tasks>
## RED phase
1. Create `openElectionVoting.test.ts` with failing tests:
   - Election with 1 candidate for a position — rejected with 422
   - Election with 0 candidates for a position — rejected
   - Election with 2+ candidates for every position — succeeds
   - Election with mixed (some positions valid, one invalid) — rejected

## GREEN phase
2. In `openElectionVoting.ts`:
   - Fetch all nominees for the election
   - Group nominees by position
   - For each position, verify count >= 2
   - Return 422 with details of under-filled positions if any fail
3. Run tests — all pass

## REFACTOR phase
4. Ensure error response includes which positions need more candidates
5. Verify 2123+ tests still pass
</tasks>
