---
phase: 15-domain-remediation
plan: "12"
type: execute
wave: 3
depends_on: []
files_modified:
  - services/api-ts/src/handlers/association:member/repos/membership.schema.ts
  - services/api-ts/src/generated/migrations/0024_unique_org_person_membership.sql
autonomous: true
requirements: [AUDIT-7]
must_haves:
  truths:
    - "Unique constraint on (organizationId, personId) exists in DB"
    - "Data cleanup handles any existing duplicates"
    - "Migration generated and tested"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/repos/membership.schema.ts"
      provides: "Schema with unique index on (organizationId, personId)"
    - path: "services/api-ts/src/generated/migrations/0024_unique_org_person_membership.sql"
      provides: "Migration adding unique constraint with duplicate cleanup"
---

<objective>
Add unique constraint on (organizationId, personId) to the memberships table to prevent duplicate memberships.
</objective>

<context>
See 15-CONTEXT.md for key business rules and file inventory.
See 15-PATTERNS.md for test infrastructure and transaction patterns.

Bug: `membership.schema.ts:120` lacks unique constraint on (organizationId, personId). Without it, concurrent or buggy requests can create duplicate memberships for the same person in the same org.
</context>

<tasks>
## Data audit
1. Query existing data for duplicate (organizationId, personId) pairs
2. Document any duplicates found and decide cleanup strategy (keep latest, merge, etc.)

## Migration
3. Write data cleanup migration that deduplicates any existing records
4. Add unique index to `membership.schema.ts`:
   ```typescript
   .unique('membership_org_person_unique', ['organizationId', 'personId'])
   ```
5. Run `bun run db:generate` to produce the migration SQL

## Verification
6. Apply migration to dev DB
7. Attempt to insert duplicate membership — verify DB rejects it
8. Verify 2123+ tests still pass
</tasks>
