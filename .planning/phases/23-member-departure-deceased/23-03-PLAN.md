---
phase: 23-member-departure-deceased
plan: 03
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.test.ts
  - services/api-ts/src/handlers/dues/jobs/reminderProcessor.test.ts
autonomous: true
requirements: [LIF-03]
must_haves:
  truths:
    - "generateDuesInvoicesForOrg only generates invoices for active members (resigned/deceased/expelled already excluded by WHERE status='active')"
    - "reminderProcessor only sends reminders to active/gracePeriod members (resigned/deceased already excluded by inArray filter)"
    - "Both exclusions have explicit unit tests proving departed members receive no invoices/reminders"
  artifacts:
    - path: "services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.test.ts"
      provides: "Tests proving billing exclusion for departed statuses"
      min_lines: 20
    - path: "services/api-ts/src/handlers/dues/jobs/reminderProcessor.test.ts"
      provides: "Tests proving notification exclusion for departed statuses"
      min_lines: 20
  key_links:
    - from: "generateDuesInvoicesForOrg.ts"
      to: "membership.schema.ts"
      via: "WHERE eq(memberships.status, 'active')"
      pattern: "eq.*status.*active"
    - from: "reminderProcessor.ts"
      to: "membership.schema.ts"
      via: "inArray(memberships.status, ['active', 'gracePeriod'])"
      pattern: "inArray.*active.*gracePeriod"
---

<objective>
Add explicit test coverage proving that departed/deceased members are excluded from dues billing and notification sends.

Purpose: RESEARCH.md confirms billing and notification exclusion is already implicit (WHERE status='active' and inArray(['active','gracePeriod'])), but LIF-03 requires explicit test coverage to document this guarantee and prevent future regressions.
Output: Extended test files with departure-specific test cases.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-member-departure-deceased/23-RESEARCH.md
@.planning/phases/23-member-departure-deceased/23-01-SUMMARY.md

<interfaces>
<!-- Billing: generateDuesInvoicesForOrg WHERE clause -->
From services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.ts line 74-78:
```typescript
.where(
  and(
    eq(memberships.organizationId, orgId),
    eq(memberships.status, 'active'),
  ),
);
```

<!-- Notification: reminderProcessor inArray filter -->
From services/api-ts/src/handlers/dues/jobs/reminderProcessor.ts line 99-101:
```typescript
eq(memberships.organizationId, config.organizationId),
sql`${memberships.duesExpiryDate}::date = ${targetDateStr}::date`,
inArray(memberships.status, ['active', 'gracePeriod']),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Billing exclusion tests for departed members</name>
  <files>services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.test.ts</files>
  <read_first>
    - services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.ts (full file -- understand handler flow)
    - services/api-ts/src/handlers/association:member/generateDuesInvoicesForOrg.test.ts (if exists -- extend existing tests)
  </read_first>
  <behavior>
    - Test 1: resigned member receives no invoice when generateDuesInvoicesForOrg runs
    - Test 2: deceased member receives no invoice when generateDuesInvoicesForOrg runs
    - Test 3: expelled member receives no invoice when generateDuesInvoicesForOrg runs
  </behavior>
  <action>
**If test file exists:** Add a new `describe('LIF-03: departed member exclusion')` block with 3 test cases.
**If test file does not exist:** Create it following the project test pattern (makeCtx/stubRepo or direct handler invocation).

Each test should:
1. Set up a membership fixture with the departed status (resigned/deceased/expelled)
2. Call generateDuesInvoicesForOrg
3. Assert that no invoice was generated for the departed member

The tests verify the implicit exclusion (WHERE status='active' already filters them out). This is a GREEN-on-write scenario -- the tests should pass immediately because the exclusion already works. The value is regression protection.

If the existing handler is hard to unit-test (complex DB setup), write the tests as integration-style assertions that verify the WHERE clause logic. At minimum, add a comment block documenting the exclusion guarantee:
```typescript
// LIF-03: departed members (resigned, deceased, expelled) are excluded from
// invoice generation because the query filters on eq(memberships.status, 'active').
// These tests verify this invariant is maintained.
```
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/association:member/generateDuesInvoicesForOrg.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - At least 3 test cases covering resigned/deceased/expelled exclusion
    - Tests pass (implicit exclusion already works)
    - Tests are clearly labeled with LIF-03 reference
  </acceptance_criteria>
  <done>Billing exclusion for departed statuses has explicit test coverage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notification exclusion tests for departed members</name>
  <files>services/api-ts/src/handlers/dues/jobs/reminderProcessor.test.ts</files>
  <read_first>
    - services/api-ts/src/handlers/dues/jobs/reminderProcessor.ts (understand reminder logic)
    - services/api-ts/src/handlers/dues/jobs/reminderProcessor.test.ts (existing tests to extend)
  </read_first>
  <behavior>
    - Test 1: resigned member receives 0 reminders
    - Test 2: deceased member receives 0 reminders
    - Test 3: expelled member receives 0 reminders
  </behavior>
  <action>
Extend `reminderProcessor.test.ts` with a `describe('LIF-03: departed member notification exclusion')` block.

Each test should:
1. Set up a membership with departed status + a dues expiry date that would normally trigger a reminder
2. Run the reminder processor
3. Assert no notification was created for the departed member

Same GREEN-on-write scenario -- `inArray(memberships.status, ['active', 'gracePeriod'])` already excludes these statuses.

Add a comment for auditability:
```typescript
// LIF-03: departed members excluded from reminders by inArray guard.
// This is the foundation for EML-03 (Phase 25 deceased/departed send guard).
```
  </action>
  <verify>
    <automated>cd /Users/elad-mini/Desktop/memberry/services/api-ts && bun test src/handlers/dues/jobs/reminderProcessor.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - At least 3 test cases covering resigned/deceased/expelled exclusion from reminders
    - Tests pass
    - LIF-03 reference in test descriptions
  </acceptance_criteria>
  <done>Notification exclusion for departed statuses has explicit test coverage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is test-only -- no new runtime boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Info Disclosure | reminderProcessor | mitigate | Tests prove departed members never receive notification sends (regression guard) |
| T-23-09 | Info Disclosure | generateDuesInvoicesForOrg | mitigate | Tests prove departed members never receive invoices (regression guard) |
</threat_model>

<verification>
- `bun test src/handlers/association:member/generateDuesInvoicesForOrg.test.ts` -- all pass including LIF-03 tests
- `bun test src/handlers/dues/jobs/reminderProcessor.test.ts` -- all pass including LIF-03 tests
- Full suite: `bun test src/**/*.test.ts` -- no regressions
</verification>

<success_criteria>
Explicit test coverage proves that resigned, deceased, and expelled members are excluded from both dues invoice generation and notification reminder sends.
</success_criteria>

<output>
After completion, create `.planning/phases/23-member-departure-deceased/23-03-SUMMARY.md`
</output>
