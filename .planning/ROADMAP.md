<!-- oli-magic v1.2 | generated 2026-05-30 | cycle 4 -->
<!-- source-commit: 9c473e1f -->
<!-- source-audit: docs/audits/EXISTING_CODEBASE_ADOPTION_AUDIT.md (2026-05-30) -->
<!-- prior-cycle-archive: .planning/ROADMAP.cycle_3.md -->

# Memberry — Brownfield Rescue Cycle 4

## Overview

Stabilize and standardize existing codebase against the oli pipeline specs.
Generated by `/oli-magic` from `EXISTING_CODEBASE_ADOPTION_AUDIT.md` (cycle 4, 2026-05-30).

**Entry state:** Codebase Health 8.2/10 (+0.8 vs cycle 3 baseline of 7.4).
**Prior cycle:** Cycle 3 graduated 2026-05-24 against the >= 9.0 threshold trio.
**Re-cycle trigger:** Post-graduation re-audit on commit `9c473e1f` discovered 1 new P1 cluster (5 state-transition wire-ups still pending) and 1 newly surfaced P1 (9 phantom FE endpoints producing silent 404s).

**No P0 findings.** Per oli-magic Step 3 edge case: no critical stabilization is needed. All work is improvement or new functionality.

## Progress

**0 / 4 waves complete**

## Phase Catalog

### Phase 1: Wave G1 — Transition Guard Wire-Up + Phantom Endpoint Reconciliation

**Goal:** Close the cycle-3 P1 state-machine debt. The transition-map framework exists (11 maps defined, 6 helpers, unit-tested); only call sites are missing. Eliminate silent 404s by reconciling phantom FE → BE mismatches.

**Source findings (audit §15.0, §15.1, §16-P1, §19):** IC-01, IC-02, IC-03, IC-04 + Standards Gap Matrix row 1.

**Requirements:**
- S-C4-001 (stabilize, membership): Wire `isValidMembershipTransition` into `handlers/membership/updateMember.ts` (line ~125) and `handlers/association:member/terminateMembership.ts`. Add unit tests asserting invalid transitions throw the expected `StateTransitionError`.
- S-C4-002 (stabilize, booking): Wire `isValidBookingTransition` into booking-status mutators (`handlers/booking/updateBooking*.ts`). Cover BOOKING_VALID + BOOKING_EVENT_VALID.
- S-C4-003 (stabilize, dues invoice): Wire `isValidInvoiceTransition` into invoice-status mutators in `dues/`.
- S-C4-004 (stabilize, training-enrollment): Wire training-enrollment transition guard in `training/` enrollment mutators.
- S-C4-005 (stabilize, marketplace): Wire vendor + listing transition guards in `marketplace/` mutators.
- S-C4-006 (stabilize, email queue): Wire EMAIL_QUEUE transition guard in `email/` queue worker / `communication/jobs/announcementSend.ts`.
- S-C4-007 (stabilize, association:member): Reconcile 9 phantom frontend endpoints (FE calls without BE route). Per call: implement BE, remove FE call, or redirect to existing route. Confirm via grep + OpenAPI cross-walk.

**Success Criteria:**
- All 7 remaining state-transition guards have at least one runtime call site in production handlers (confirmed via grep for `isValid*Transition` / `assertValidTransition`).
- Unit tests added per wire-up demonstrating invalid transitions throw (RED → GREEN).
- 0 phantom FE endpoints remaining in `CODE_API_SURFACE.json` reconciliation.
- Full backend test suite passes; full Hurl contract suite passes; full Playwright E2E passes.

**Mode:** tdd
**Depends on:** (none — wave 1)
**Parallel-safe within wave?** YES — slices touch distinct modules (membership, booking, dues, training, marketplace, email, association:member). Run as 7 parallel sub-agents under one wave.
**Integration test required?** S-C4-007 (touches sdk + memberry + handlers across multiple modules).
**Status:** not-started
**Disk Status:** not-started

---

### Phase 2: Wave G2 — Performance & Architectural Cleanup

**Goal:** Eliminate the cycle-3 carry-forward P2s: bound large queries, batch the 3 known N+1s, and decide the schema-registry pattern (move-or-formalize).

**Source findings (audit §15.0/15.1 IC-05/06/07, §15.5, §15.9, §16-P2):**

**Requirements:**
- S-C4-010 (refactor, communication + booking + marketplace + person): Add `.limit()` + cursor pagination to ~70 unbounded `findMany` sites. Establish convention in PERFORMANCE.md; codify default page size.
- S-C4-011 (refactor, communication): Batch the 2 N+1 sites in `communication/bulkUpdatePersonSubscriptions` and `communication/createMessage`.
- S-C4-012 (refactor, certificates): Batch the N+1 in `certificates/batchGenerateCertificates`.
- S-C4-013 (refactor, core/architecture): Decide schema-registry strategy. Either (a) move 8 handler schemas (notifications, bookings, platformAdmins, training, memberships, positions, events, eventRegistrations, invitationTokens) into `core/`, OR (b) ratify the registry pattern in ARCHITECTURE.md with an explicit ADR.
- S-C4-014 (refactor, core/ports): Introduce `core/ports/` containing port interfaces for `governance.repo` and `platform-admin.repo`, then rewrite the 4 middleware → handler dependencies (`officer-auth`, `impersonation-guard`, `platform-admin-auth`, `org-context`) to depend on the ports.
- S-C4-015 (refactor, membership ↔ dues): Replace the cross-module raw SQL subquery in `membership/repos/membership.repo.ts:131` (which reads `dues_invoice`) with a repo-interface call.

**Success Criteria:**
- 0 unbounded `findMany` calls in production code (excluding intentional bounded-by-scope queries documented inline).
- 0 N+1 patterns remaining in spot-checked hot paths (communication ×2, certificates ×1).
- Schema-registry decision recorded in ARCHITECTURE.md (either pattern documented as canonical or migration completed).
- 0 production inversions in `core/schema-registry.ts` OR explicit `[INTENTIONAL]` markers per re-exported schema.
- `core/ports/` module exists with port-interface contracts; middleware no longer imports from `handlers/`.
- Standards Gap Matrix rows for "Core → handler inversions" and "Unbounded queries" closed (row state: resolved).

**Mode:** tdd
**Depends on:** Phase 1 (state-transition guards stabilize the surface area being refactored; reduces risk during pagination + repo signature changes).
**Parallel-safe within wave?** Partial — S-C4-013 / S-C4-014 are architectural decisions (sequential, single-author); S-C4-010 / S-C4-011 / S-C4-012 / S-C4-015 can run in parallel.
**Integration test required?** S-C4-014 (middleware change spans org-context + auth flows). S-C4-015 (cross-module repo change).
**Regression risk?** YES — S-C4-014 touches shared middleware; require a full E2E pass after.
**Status:** not-started
**Disk Status:** not-started

---

### Phase 3: Wave G3 — Standards Adoption (TypeSpec + Indexes)

**Goal:** Close TypeSpec coverage from 58% → 100% on the 10 remaining handler dirs and add the two missing FK indexes for measurable query-latency improvement.

**Source findings (audit §14 row "TypeSpec coverage", §15.9 PERF-04 carry-over, §16-P3 items 8, 10):**

**Requirements:**
- S-C4-020 (new-feature/refactor, advertising): Author TypeSpec definitions for `advertising/` handlers. Generate routes + validators; replace hand-wired routes (if any) with generated ones.
- S-C4-021 (new-feature/refactor, certificates): Author TypeSpec for `certificates/` (3 endpoints).
- S-C4-022 (new-feature/refactor, comms): Author TypeSpec for the remaining `comms/` WS message contracts not already covered.
- S-C4-023 (new-feature/refactor, dues): Complete TypeSpec coverage for hand-wired `dues/` routes.
- S-C4-024 (new-feature/refactor, elections): Complete TypeSpec coverage for `elections/`.
- S-C4-025 (new-feature/refactor, jobs): Author TypeSpec for `jobs/` job-trigger endpoints.
- S-C4-026 (new-feature/refactor, marketplace): Author TypeSpec for `marketplace/`.
- S-C4-027 (new-feature/refactor, notifs): Complete TypeSpec coverage for the mixed `notifs/` handlers.
- S-C4-028 (new-feature/refactor, person): Author TypeSpec for the remaining hand-wired `person/` routes.
- S-C4-029 (new-feature/refactor, training): Author TypeSpec for the entire `training/` directory.
- S-C4-030 (stabilize, dues + invite): Add the 2 missing FK indexes — `dues_payments.idempotencyKey` and `invite.personId`. Migration + verify with EXPLAIN ANALYZE on representative queries.

**Success Criteria:**
- TypeSpec coverage ≥ 95% (24/25 handler dirs) — audit metric updates from 58%.
- All hand-wired routes (~33 today) reduced to the by-design list documented in ROADMAP.md (9 routes, middleware-ordering reasons).
- Both FK indexes present in latest migration; `db:generate` clean; integration tests still pass.
- Standards Gap Matrix rows "TypeSpec coverage" and "FK indexes" closed.

**Mode:** tdd
**Depends on:** Phase 1 (handler stability), Phase 2 (architectural decisions land before regenerating routes).
**Parallel-safe within wave?** YES — each TypeSpec module slice is independent; run 10 parallel sub-agents. S-C4-030 (index migration) runs sequentially at the end.
**Integration test required?** YES at wave close — regenerate the full OpenAPI + Hurl matrix, run contract tests against running API.
**Status:** not-started
**Disk Status:** not-started

---

### Phase 4: Wave G4 — Quality, Observability & Housekeeping

**Goal:** Knock down the remaining P3 backlog: observability (OTel), security hardening (CSRF token), tooling (FSM detector), documentation promotion, repo hygiene.

**Source findings (audit §14, §15.4, §16-P3 items 11-15):**

**Requirements:**
- S-C4-040 (new-feature, observability): Integrate OpenTelemetry. Wire the tracer into Hono middleware, propagate `traceparent`, export to the configured collector. Update OBSERVABILITY.md.
- S-C4-041 (refactor, security): Add explicit CSRF token middleware (defense-in-depth above SameSite). Document the threat-model delta in THREAT_MODEL.md.
- S-C4-042 (refactor, type-casts): Tighten the remaining 30 handler-level `as any` where feasible. Stripe SDK casts in `billing/handleStripeWebhook.ts` are explicitly out of scope per audit §21 ("Avoid"). Target: ≤ 10 casts post-wave, all at external-library boundaries or JSONB reads with inline justification.
- S-C4-043 (new-feature, tooling): Extend `oli-codebase-map` FSM detector to recognise backend pg-enum-based state machines. Re-run codebase-map; verify CODE_STATE_MACHINES.md detects 11+ FSMs (vs current 3).
- S-C4-044 (refactor, docs): Promote `DATA_GOVERNANCE_DRAFT.md` → `DATA_GOVERNANCE.md`; fill any remaining draft sections.
- S-C4-045 (refactor, housekeeping): Archive 7 legacy root-level audit markdowns (`AUDIT_04_INDEX.md`, `AUDIT_04_SUMMARY.md`, `CODEBASE_ORIENTATION.md`, `INTERACTIVE_AUDIT_QUICK_REF.md`, `INTERACTIVE_ELEMENTS_AUDIT.md`, `MEMBER_INTERACTIVE_ELEMENTS_AUDIT.md`, `TODOS.md`) into `docs/audits/archive/`.
- S-C4-046 (refactor, association:member): Continue the deferred mega-module decomposition per `.planning/deferred/14-mega-module-split/SPLIT-PLAN.md`. Target one bounded context split this cycle (e.g., `association:governance` → first module extracted).

**Success Criteria:**
- OpenTelemetry traces visible in the configured collector for at least 5 representative endpoints.
- CSRF middleware present; documented in THREAT_MODEL.md; Hurl tests assert behavior on missing token.
- `as any` density in handlers: ≤ 10 (audit §15.4).
- `oli-codebase-map` detects ≥ 11 FSMs; CODE_STATE_MACHINES.md regenerated.
- `DATA_GOVERNANCE.md` exists at canonical path (no `_DRAFT` suffix).
- 7 legacy markdown files moved into `docs/audits/archive/`; no broken inbound links.
- One `association:member` bounded context extracted; tests still passing; cross-module imports rewired.

**Mode:** tdd
**Depends on:** Phase 3 (TypeSpec coverage lands first so OTel + CSRF apply to the canonical generated route surface).
**Parallel-safe within wave?** YES — most slices independent. S-C4-046 (mega-module split) ordered last; high-risk surface area, sequential.
**Integration test required?** S-C4-040, S-C4-041, S-C4-046.
**Status:** not-started
**Disk Status:** not-started

---

## Wave Dependency Diagram

```
G1 (Stabilization: 7 slices)            ← P1 cluster
   │
   ↓
G2 (Performance + Architecture: 6 slices) ← P2 carry-forward
   │
   ↓
G3 (TypeSpec Coverage + Indexes: 11 slices) ← P3 standards adoption
   │
   ↓
G4 (Observability, Security, Hygiene: 7 slices) ← P3 final polish
```

## Slice Inventory (31 slices total)

| Wave | Slice | Module(s) | Type | Priority | Source |
|------|-------|-----------|------|----------|--------|
| G1 | S-C4-001 | membership, association:member | stabilize | P1 | IC-02 |
| G1 | S-C4-002 | booking | stabilize | P1 | IC-03 |
| G1 | S-C4-003 | dues | stabilize | P1 | IC-04 |
| G1 | S-C4-004 | training | stabilize | P1 | §9 carry-over |
| G1 | S-C4-005 | marketplace | stabilize | P1 | §9 carry-over |
| G1 | S-C4-006 | email, communication | stabilize | P1 | §9 carry-over |
| G1 | S-C4-007 | sdk, memberry, multiple handlers | stabilize | P1 | IC-01 (9 phantom) |
| G2 | S-C4-010 | communication, booking, marketplace, person | refactor | P2 | IC-05 (70 sites) |
| G2 | S-C4-011 | communication | refactor | P2 | IC-06 |
| G2 | S-C4-012 | certificates | refactor | P2 | IC-06 |
| G2 | S-C4-013 | core, multiple | refactor | P2 | IC-07 |
| G2 | S-C4-014 | core, middleware | refactor | P2 | §15.5 carry |
| G2 | S-C4-015 | membership ↔ dues | refactor | P2 | §15.6 |
| G3 | S-C4-020 | advertising | new-feature | P3 | §14 TypeSpec gap |
| G3 | S-C4-021 | certificates | new-feature | P3 | §14 TypeSpec gap |
| G3 | S-C4-022 | comms | new-feature | P3 | §14 TypeSpec gap |
| G3 | S-C4-023 | dues | refactor | P3 | §14 TypeSpec gap |
| G3 | S-C4-024 | elections | refactor | P3 | §14 TypeSpec gap |
| G3 | S-C4-025 | jobs | new-feature | P3 | §14 TypeSpec gap |
| G3 | S-C4-026 | marketplace | new-feature | P3 | §14 TypeSpec gap |
| G3 | S-C4-027 | notifs | refactor | P3 | §14 TypeSpec gap |
| G3 | S-C4-028 | person | refactor | P3 | §14 TypeSpec gap |
| G3 | S-C4-029 | training | new-feature | P3 | §14 TypeSpec gap |
| G3 | S-C4-030 | dues, invite | stabilize | P3 | §14 FK index gap |
| G4 | S-C4-040 | core (observability) | new-feature | P3 | §16-P3 item 11 |
| G4 | S-C4-041 | core (security) | refactor | P3 | §16-P2 item 7 |
| G4 | S-C4-042 | multiple handlers | refactor | P3 | §15.4 |
| G4 | S-C4-043 | tooling (oli-codebase-map) | new-feature | P3 | IC-10 |
| G4 | S-C4-044 | docs | refactor | P3 | §16-P3 item 12 |
| G4 | S-C4-045 | repo root | refactor | P3 | IC-11 |
| G4 | S-C4-046 | association:member | refactor | P3 | §14 mega-module |

**Total: 31 slices** (7 stabilize, 17 refactor, 7 new-feature).

## Out of Scope (Audit §21 "Avoid")

- Big-bang refactors of `association:member` — slice S-C4-046 explicitly extracts ONE bounded context this cycle, not the whole split.
- Tightening `as any` in `billing/handleStripeWebhook.ts` — documented external-library type-gap workarounds, retained.
- Treating `core/schema-registry.ts` as obviously-wrong without ARCHITECTURE.md ratification — S-C4-013 forces the decision rather than presuming it.

---

*Generated by `/oli-magic` 2026-05-30 from `EXISTING_CODEBASE_ADOPTION_AUDIT.md` (cycle 4) at source-commit `9c473e1f`. Prior roadmap archived to `.planning/ROADMAP.cycle_3.md`.*
