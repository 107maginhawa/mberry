### 5.10 comms

## comms module — Wave-2 (cluster B4, realtime-admin) TDD slice plan

Floor: **none today** (no `comms` key in `.coverage-thresholds.json` — confirmed). Effort **S**. Method (locked, mirrors Wave-1 ledger): characterize existing code → TDD new behavior; a MISSING-BR that is a REAL BUG → red-test then fix the right layer. DoD priority: (1) real-PG harness un-gate/standardize → (2) MISSING-BR real tests → (3) MISSING-workflow real-flow proof → (4) inter-module contract → (5) ratchet floor (set to measured, no number-chasing) → (6) fix registry/coverage drift.

**Source facts verified (against source + live `monobase` catalog):**

- Three repos under `src/handlers/comms/repos/`: `chatRoom.repo.ts`, `chatMessage.repo.ts`, `chatRoomMember.repo.ts` over tables `chat_room`, `chat_message`, `chat_room_member` (all in `repos/comms.schema.ts`). No `jobs/` dir.
- **The existing real-PG file `repos/comms-repos.integration.test.ts` is GENUINE real-Pool/drizzle and ALREADY RUNS IN A DB-BACKED CI LANE.** Confirmed: its guard is **DB-unreachable-skip-only** — `if (!dbReachable) return` (e.g. line 204), with `dbReachable` set true after a successful DDL connect (line 154). **There is NO `if (process.env['CI']) return`** anywhere in the file. `package.json` `test:integration` = `bun test $(find src -name '*.integration.test.ts')`, and `.github/workflows/ci.yml:637` runs `bun run test:integration` AS A SEPARATE PROCESS after `ci-migrate.ts` (ci.yml:630). **The plan §7/§1 premise "comms integ real-PG but never runs in a DB-backed CI lane" is STALE/WRONG** — it runs. The ACTUAL gap is that it uses **hand-written DDL** (`ddl()`, lines 55–110) on a raw named schema (`comms_repos_<ts>`), not the canonical `createScratch([...]) (LIKE public.<t> INCLUDING ALL)` from `pg-scratch.ts`. Hand-written DDL can silently drift from `public` (e.g. it models the enum columns `room_type`/`status`/`message_type`/`role` as `text`, not the real PG enums `chat_room_type`/`chat_room_status`/`message_type`/`chat_room_member_role`; it hand-declares `chat_message` cols and could miss one a repo query reads). So the leverage move is **standardize onto `createScratch`** (schema-faithful, can't drift) — NOT un-gating.
- `default-channels.ts` exports `createDefaultChannels` (provisions `#general`+`#announcements` as `room_type='channel'`, idempotent on `(organization_id, context_id)`) and `autoJoinOrgChannels` (PD-1: a member auto-joins EVERY `room_type='channel'` room in the org — inserts a `chat_room_member` row `onConflictDoNothing` AND appends to the legacy JSONB `participants`). **Both are WIRED**: `core/domain-event-consumers.ts:412` (`organization.created` → `createDefaultChannels`), `:421` (`membership.created` → `autoJoinOrgChannels`), `:469` (`membership.imported` → per-person `autoJoinOrgChannels`). **Neither function has ANY test today — not even a mock** (confirmed: no `default-channels.test.ts`). This is pure net-new coverage of a live prod path.
- `chatMessage.repo.test.ts` and `chatRoom.repo.test.ts` are **mock illusions**: `chatMessage.repo.test.ts` uses a hand-rolled `makeDb()` select-chain stub + `stubRepo(createOne)` (asserts only the `captured.organizationId` argument, never a persisted row); `chatRoom.repo.test.ts` uses `makeUpdateDb()` and asserts `typeof captured.messageCount === 'object'` (i.e. "the value is a drizzle SQL fragment" — a tautology, not real increment SQL) and `buildWhereConditions(...)` returns `toBeDefined()`. The REAL behavior these assert (org-derive-on-insert, SQL-side count increment, atomic call claim, AND-participant filter) is already covered by `comms-repos.integration.test.ts` against PG — so the mock files are redundant illusions whose fix is deletion-or-conversion once the integration suite is the source of truth.
- WS send→persist→broadcast: REST `sendChatMessage.ts:122` persists via `messageRepo.createTextMessage(room, user.id, text, organizationId)` then `roomRepo.updateLastMessage` (line 284) then best-effort `wsService.publishToChannel('chat-rooms/${room}', 'chat.message', message)` (line 294, try/catch — never fails the send). The WS-server path `ws.chat-room.ts` calls `createTextMessage` WITHOUT an org (the historic NULL-org leak, now closed by `resolveOrgId` deriving org from the room — chatMessage.repo.ts:73–86). The **persist half is provable** at the repo layer against PG (org-derive, correct room/sender, type, ordering); the WS **transport/broadcast** half (socket fan-out) is out of unit scope — note it.

**Live catalog (verified — drives every assert):**
- `chat_room`: `id uuid PK gen_random_uuid()`, `organization_id uuid NOT NULL`, `participants jsonb NOT NULL`, `admins jsonb NOT NULL`, `context_id text` (nullable), `status chat_room_status NOT NULL default 'active'`, `room_type chat_room_type NOT NULL default 'group'`, `last_message_at timestamptz`, `message_count integer NOT NULL default 0`, `active_video_call_message_id uuid`, base audit cols. Enums: `chat_room_status {active,archived}`, `chat_room_type {channel,dm,group}`. Indexes incl. gin on `participants`/`admins`.
- `chat_room_member`: `id uuid PK`, `chat_room_id uuid NOT NULL FK→chat_room ON DELETE CASCADE`, `person_id uuid NOT NULL`, `role chat_room_member_role NOT NULL default 'member'` (enum `{member,admin}`), `joined_at timestamptz NOT NULL default now()`, `last_read_at`/`muted_until` nullable. **UNIQUE CONSTRAINT `chat_room_members_unique (chat_room_id, person_id)`** — this is the room-membership uniqueness that backs `onConflictDoNothing` and the auto-join idempotency.
- `chat_message`: `id uuid PK`, `organization_id uuid NOT NULL`, `chat_room_id uuid NOT NULL FK→chat_room ON DELETE CASCADE`, `sender_id uuid NOT NULL`, `timestamp timestamptz NOT NULL default now()`, `message_type message_type NOT NULL` (enum `{text,system,video_call}`), `message text` (nullable), `video_call_data jsonb`, `parent_message_id uuid`, `reply_count integer NOT NULL default 0`. Ordering index `chat_messages_room_timestamp_idx (chat_room_id, timestamp)`. **No unique on message** (ordering is by `timestamp`, not a uniqueness constraint).

---

### Slice 1 — Standardize the comms real-PG harness onto `createScratch` (schema-faithful, drift-proof) — HARNESS, DoD #1
- **axis:** integ
- **files to CREATE / EDIT:**
  - EDIT in place `src/handlers/comms/repos/comms-repos.integration.test.ts`: replace the hand-written `ddl()` + raw `setupPool`/`scopedPool`/`SET search_path` block (lines 32–198) with `const H = await createScratch(['chat_room','chat_room_member','chat_message'])` from `@/test-utils/pg-scratch`. Keep `restoreRepo(...)` for the three repo prototypes in `beforeAll` (the cross-file prototype-pollution defense at lines 145–147 is still required — sibling comms unit files stub these prototypes). Swap `dbReachable`→`H.dbReachable`, `scopedPool`→`H.scopedPool`, `db`→`H.db`, `afterAll`→`H?.teardown()`. Replace `TEST_SCHEMA`-interpolated raw SQL in `insertRoom` / the raw `chat_message` INSERTs with `H.scopedPool.query` (search_path is pinned by `createScratch`, so drop the schema-qualified table names).
- **why (classification):** the file is genuine real-PG and CI-running today → this is **characterization-hardening, not un-gating**. The fix removes DDL-drift risk: `LIKE public.chat_room INCLUDING ALL` copies the REAL enum types + the real `chat_room_members_unique` constraint + the real NOT NULL set, so a repo query reading a column/enum the hand-DDL mis-modeled can no longer pass against a thinner fake table. (Note: `createScratch` drops FKs, so the existing `ON DELETE CASCADE` FK on `chat_room_member`/`chat_message` is NOT copied — seed parent `chat_room` rows first via `insertRoom`, which the suite already does; the cascade-delete assertions, if any, must seed/delete manually rather than rely on the FK. Verify the suite has no test depending on FK cascade — it currently relies on the hand-DDL `REFERENCES … ON DELETE CASCADE`; if a test deletes a room and expects members/messages gone, port it to an explicit delete or drop that assertion and note it.)
- **asserts (real outcomes against Postgres — all EXISTING assertions must stay green under `createScratch`):**
  - All ChatRoomMemberRepository / ChatMessageRepository / ChatRoomRepository tests in the file pass unchanged against the LIKE-copied tables — proving the suite was schema-faithful all along and is now drift-proof.
  - Add one NEW assert that only `createScratch` enables: inserting a `chat_room.status` value outside `{active,archived}` (e.g. `'frozen'`) via `H.scopedPool` raises Postgres invalid-enum-input `code === '22P02'` (the hand-DDL `text` column could NOT have caught this) — proving the real enum is now enforced. Same for `chat_room_member.role` outside `{member,admin}`.
- **est commits:** 2 (1 mechanical migration to `createScratch` + green; 1 the new enum-enforcement asserts / any drift surfaced).

### Slice 2 — default-channels auto-join real-PG suite (the wired-but-untested PD-1 prod path) — NEW
- **axis:** integ
- **files to CREATE:** `src/handlers/comms/default-channels.integration.test.ts` using `createScratch(['chat_room','chat_room_member'])`.
- **asserts (real persisted rows — these functions have ZERO tests today):**
  - `createDefaultChannels(H.db, org, [adminA])` persists exactly TWO `chat_room` rows for `org`: one `name='general' context_id='channel:general'`, one `name='announcements' context_id='channel:announcements'`, both `room_type='channel'`, `status='active'`, `message_count=0`, `participants=[adminA]`, `admins=[adminA]` — read back via `H.scopedPool`. Returns both ids.
  - **idempotency on the real unique-ish guard:** call `createDefaultChannels(org, [adminA])` twice → still exactly 2 `chat_room` rows for that org (the `(organization_id, context_id)` existence check at default-channels.ts:39–48 short-circuits), and the second call RETURNS the SAME two ids.
  - admin-member rows: when `adminPersonIds` non-empty, each created channel gets a `chat_room_member` row with `role='admin'` for each admin (default-channels.ts:76–84) — assert the persisted `chat_room_member` rows (`role='admin'`, correct `chat_room_id`).
  - **autoJoinOrgChannels (PD-1 — the member-onboarding guarantee):** seed an org with the 2 default channels + a non-channel `room_type='group'` room; call `autoJoinOrgChannels(H.db, org, personX)` → persists a `chat_room_member` row (`role='member'`) for `personX` in EACH of the 2 channel rooms and NONE for the group room (it filters `room_type='channel'`, default-channels.ts:112–119). Returns the 2 channel ids.
  - **dual-write to legacy JSONB:** after auto-join, each channel's `chat_room.participants` JSONB array CONTAINS `personX` (default-channels.ts:129–135) — read back the array. This is what keeps `listChatRooms` (participants-array filter) surfacing the channel in the member sidebar.
  - **idempotency on `chat_room_members_unique`:** call `autoJoinOrgChannels(org, personX)` twice → still exactly ONE `chat_room_member` row per (channel, personX) (the `onConflictDoNothing` target `(chat_room_id, person_id)` = the live `chat_room_members_unique` constraint), and `participants` does NOT contain a duplicate `personX`.
- **est commits:** 2 (1 createDefaultChannels persist + idempotency; 1 autoJoinOrgChannels join + dual-write + idempotency).

### Slice 3 — WS/REST send → PERSIST half against real PG; retire the mock illusions — characterization + illusion-fix
- **axis:** integ (covers BR: every message persists with non-null org derived from the room + correct ordering)
- **files to CREATE / EDIT:**
  - Add a `describe('createTextMessage persist (WS chokepoint)')` block to `comms-repos.integration.test.ts` (already real-PG after Slice 1) — OR a small `chatMessage-persist.integration.test.ts`. Prefer extending the existing suite (one `createScratch`).
  - EDIT `repos/chatMessage.repo.test.ts` and `repos/chatRoom.repo.test.ts`: these are makeDb/stub illusions whose REAL behavior is now proven on PG. Reduce them to ONLY the genuinely pure-unit assertions that do not touch SQL (e.g. `validateMessageContent` is already a pure helper covered in the integration file; the SQL-fragment-typeof and `buildWhereConditions toBeDefined()` checks should be DELETED as tautologies, with a one-line comment pointing at the integration suite). Do not leave a green mock asserting a non-behavior.
- **asserts (real rows — proving the persist half of send→persist→broadcast):**
  - **WS no-org leak closed (the chokepoint):** seed a room with `organization_id=ORG_B`; `createTextMessage(roomId, sender, 'hi')` WITHOUT an org → persisted `chat_message` row has `organization_id=ORG_B` (derived from the room via `resolveOrgId`), `message_type='text'`, `chat_room_id=roomId`, `sender_id=sender`, `message='hi'`, and `organization_id IS NOT NULL` (the SET NOT NULL invariant — the live column is `NOT NULL`). Read back via `H.scopedPool`. (The mock file only ever asserted the createOne ARGUMENT — never that a row with the right org actually lands.)
  - explicit-org short-circuit: `createTextMessage(roomId, sender, 'hi', ORG_A)` on an ORG_B room persists `organization_id=ORG_A` (caller wins; no room lookup) — a real cross-check that the override path also persists.
  - missing-room org-resolution: `createTextMessage('<absent uuid>', sender, 'hi')` (no org) → rejects `NotFoundError` (resolveOrgId can't find the room) and NO `chat_message` row is written.
  - **ordering (send→list):** insert 3 text messages with strictly increasing `timestamp`; `getChatRoomMessages(roomId, {messageType:'text', orderBy:'asc'})` returns them in `m0,m1,m2` order (the `(chat_room_id, timestamp)` index ordering) and `desc` reverses — proving the persisted ordering the WS broadcast/refetch relies on. (This overlaps the existing integration test — keep ONE copy, don't duplicate.)
  - **NOTE (out of scope, flag it):** the WS transport/broadcast half (`wsService.publishToChannel`, sendChatMessage.ts:294) is a socket fan-out that is best-effort (try/catch, never fails the send). It is NOT exercised here — call it out in the commit as a deliberate scope boundary (transport-layer, belongs to a ws-integration harness, not this repo-persist slice).
- **est commits:** 2 (1 persist + org-derive + ordering asserts; 1 retire/trim the two mock illusion files).

### Slice 4 — Ratchet floor + registry/coverage-threshold housekeeping (FOLDED into B4 finalize)
- **axis:** integ/BR (housekeeping — DoD #5/#6)
- **edit:** `services/api-ts/.coverage-thresholds.json` — ADD a `src/handlers/comms` entry set to the **measured** line coverage after slices 1–3 land (no key exists today; set to the real number, do not chase). Update `br-registry.json` + coverage-matrix entries for the comms BRs/workflows now backed by real tests (default-channel provisioning + auto-join PD-1, message-persist org-derive, the standardized real-PG repo suite). Remove any stale "MISSING/SHALLOW" rows for items this plan made REAL; reclassify the two trimmed mock files.
- **asserts:** `bun run test:integration` (real DB) green incl. the standardized comms suite + the new default-channels suite; `bun run test:unit` green after the mock-file trim; coverage gate passes at the newly-set comms floor.
- **est commits:** 1 (folded into the single B4 finalize commit).

---

**Totals:** 4 slices, ~7 commits. **Harness-standardize-first** (Slice 1) is the highest-leverage move — but note the B4 delta vs §7: comms integ is NOT CI-absent (it already runs in CI's `test:integration` lane); the real fix is migrating its hand-written DDL onto `createScratch` so it can't drift from `public`. Slice 2 is pure net-new coverage of a LIVE, wired-but-untested prod path (default channels + PD-1 auto-join). Slice 3 proves the persist half of the WS send path AND retires two tautological mock illusions. No new floor exists today → Slice 4 SETS it to measured. **No real-bug suspects found in comms** — the historic NULL-org leak is already closed (`resolveOrgId`), and both default-channel functions are correctly wired; everything classifies as characterization or test-fragility (illusion). One product-decision-adjacent item surfaced: the mock files are redundant with the integration suite (cleanup, not a behavior change).
