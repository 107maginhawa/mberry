### 5.11 platformadmin

## platformadmin module — Wave-2 (cluster B4, realtime-admin) TDD slice plan

Floor: **none today** (no `src/handlers/platformadmin` key in `.coverage-thresholds.json` — verified). Effort **S**. Method (locked, from `2026-06-21-test-gap-ledger.md`): characterize existing code → TDD new behavior; where a MISSING BR is a real bug, red-test then fix. DoD priority: (1) real-PG harness runs in CI → (2) MISSING BRs get real tests → (3) MISSING workflows get real-flow data assertions → (4) inter-module contracts proven → (5) ratchet floor toward 50 → (6) fix registry drift.

**Cluster posture (B4, realtime-admin):** per §7, platformadmin is a *maintenance* member — its real-PG harness already EXISTS and ALREADY RUNS IN CI. The B4 work here is **extension/un-skip**, not absence. There is no shared B4 fixture; platformadmin keeps its own per-module integration suite.

**Source facts verified (against source + live `monobase` catalog, NOT trusted from the brief):**
- **Harness is GENUINE real-PG and CI-RUN.** `repos/platformadmin-repos.integration.test.ts` stands up a per-run scratch schema with **hand-written DDL** (NOT `createScratch`), seeds rows, and drives the real drizzle query builders for **all 6 repo classes** (`DashboardRepository`, `AssociationRepository`, `OrganizationRepository`, `FeatureFlagRepository`, `PlatformAdminRepository`, `ImpersonationSessionRepository`). Its only skip is `if (!dbReachable) return` (no `if(process.env.CI)return` gate). CI runs it: `.github/workflows/ci.yml:630` migrates public via `ci-migrate.ts`, then `:638` runs `bun run test:integration` = `bun test $(find src -name '*.integration.test.ts')` against the service Postgres (`DATABASE_URL=postgresql://postgres:password@localhost:5432/monobase`). **So §7's "integ 8→~0, already real-PG, the gap is extension not absence" is CONFIRMED.** No un-gate needed (unlike notifs/comms).
- **Measured coverage (`bun test --coverage src/handlers/platformadmin/`, 486 pass / 0 fail):** `getNationalChapterDetail.ts` **100% line**; `dashboard.repo.ts` **100% line**; `platform-admin.repo.ts` **75.00% line / 97.10% funcs**, uncovered lines **269–315 (= the entire `featureFlagRepoPort.findEnforcementFlags` body)** + 156–159 (an `OrganizationRepository` branch). `platform-admin.repo.ts` at **75% is the module-min** binding file.
- **`findEnforcementFlags` is the headline gap — it is NOT a `test.skip`; it has ZERO tests at all.** The integration file's header prose (lines 22–28) declares it "SKIPPED (untestable without re-creating large slices of schema)" — i.e. the author hand-wrote DDL for `organization` + `feature_flag` but NOT for `subscription` or `pricing_tier`, so the 3-table resolution couldn't run. The middleware test `src/middleware/feature-flag-gate.test.ts:40` stubs `findEnforcementFlags` with canned rows, so the join SQL is proven NOWHERE. Coverage data (uncovered 269–315) corroborates.
- **`findEnforcementFlags` resolution path (platform-admin.repo.ts:269–317):** (a) always adds `orgId` to candidate `targetIds`; (b) `SELECT association_id FROM organization WHERE id=orgId` → pushes `associationId`; (c) **dynamic import** of `{ subscriptions, pricingTiers }`, then `SELECT pricing_tier_id FROM subscription WHERE organization_id=orgId` → `SELECT slug FROM pricing_tier WHERE id=tierId` → pushes the tier **slug**; (d) final `SELECT * FROM feature_flag WHERE module_name=:m AND target_id IN (:targetIds)`, mapped to `{targetType,targetId,moduleName,enabled,isOverride}`. Tier resolution is wrapped in try/catch (best-effort; missing sub narrows candidates, never throws).
- **`getNationalChapterDetail.ts` ALREADY HAS a unit test** (`national-endpoints.test.ts:102`, `stubRepo(DashboardRepository,...)`, 100% line). The brief's "no unit test" is WRONG. The genuine residual gap is that this test is **mock-based** — the suppression branch (`isSuppressed` zeroing), the cents/pct math, and the `creditComplianceBreakdown.compliant = round(cpdRate*totalMembers)` aggregation are asserted only against a hand-fed `snap()` stub, never against a real persisted `chapter_snapshot` row read back through `DashboardRepository.getChapterSnapshot`.
- **Live catalog (verified):**
  - `feature_flag`: `target_type varchar(50) NOT NULL`, `target_id varchar(255) NOT NULL`, `module_name varchar(100) NOT NULL`, `enabled boolean NOT NULL DEFAULT true`, `is_override boolean NOT NULL DEFAULT false`. **Partial-unique `idx_ff_unique` UNIQUE on (target_type, target_id, module_name)** + `idx_ff_target` on (target_type, target_id). `created_by`/`updated_by` are NULLABLE.
  - `subscription`: `organization_id uuid NOT NULL`, `pricing_tier_id uuid NOT NULL`, FK `subscription_pricing_tier_id_pricing_tier_id_fk → pricing_tier(id)`, **`subscription_org_unique` UNIQUE on organization_id**, **`created_by`/`updated_by` are NOT NULL** (this is WHY the hand-DDL omitted them — adding `subscription` to the hand-DDL requires those NOT-NULL cols; `createScratch` copies them automatically via `LIKE … INCLUDING ALL`).
  - `pricing_tier`: `slug varchar(50) NOT NULL`, `pricing_tier_slug_unique UNIQUE on slug`, `created_by`/`updated_by` NOT NULL.
  - `organization`: `association_id uuid NOT NULL`, `slug varchar(100)` with `idx_org_slug UNIQUE`.
  - `chapter_snapshot`: `org_id NOT NULL`, `association_id NOT NULL`, `snapshot_month varchar(7) NOT NULL`, `total_members integer NOT NULL`; rate/collected cols are `numeric` NULLABLE; indexes on org/association/month.
  - `national_dashboard_access`: `association_id`/`member_id`/`granted_by` NOT NULL, `revoked_at` NULLABLE (the BR-36 revoke flag).

**Harness decision (matters):** the existing suite uses **hand-written DDL**, which is exactly why `findEnforcementFlags` was skip-prosed. Slice 1 **adds `subscription` + `pricing_tier` DDL** to the existing file (cheapest, keeps one file) OR — preferred per the wave-wide convention — **migrates the `findEnforcementFlags` block to a `createScratch(['organization','feature_flag','subscription','pricing_tier'])` companion** so the NOT-NULL `created_by`/`subscription_org_unique`/`pricing_tier_slug_unique` constraints come for free from `LIKE … INCLUDING ALL` (no risk of a thinner hand-DDL table masking a real column). We DO NOT rewrite the already-green 6-class hand-DDL suite (no churn for churn's sake); we only add the enforcement coverage on `createScratch`.

---

### Slice 1 — Un-skip `findEnforcementFlags`: prove the 3-table resolution join against real Postgres (the headline platformadmin gap, DoD #1/#2) — FIRST
- **axis:** integ
- **files to CREATE:** `services/api-ts/src/handlers/platformadmin/repos/feature-flag-enforcement.integration.test.ts` (new companion using `createScratch`, so `subscription`/`pricing_tier` arrive with their real NOT-NULL + unique constraints; leaves the existing hand-DDL suite untouched). Also DELETE the "SKIPPED … Out of scope" prose block (lines 22–28) in `platformadmin-repos.integration.test.ts` and replace with a one-line pointer to the new file.
- **pattern:** `createScratch(['organization','feature_flag','subscription','pricing_tier'])`; guard `if (!H.dbReachable) return`; `afterAll(()=>H?.teardown())`. Seed parents directly (FKs dropped by `LIKE`); `subscription`/`pricing_tier` inserts MUST supply `created_by`/`updated_by` (NOT NULL in catalog). Construct via `featureFlagRepoPort(H.db as any)`.
- **asserts (real outcomes against Postgres, NOT a stubbed row set):**
  - **org-tier resolution end-to-end:** seed org `O` in association `A`; seed `subscription{organization_id:O, pricing_tier_id:T}`; seed `pricing_tier{id:T, slug:'pro'}`. Seed three `feature_flag` rows for `module_name='events'`: `{target_type:'org', target_id:O, enabled:false}`, `{target_type:'association', target_id:A, enabled:true}`, `{target_type:'tier', target_id:'pro', enabled:true}`. `findEnforcementFlags(O,'events')` returns **exactly those 3 rows** (assert the set of `targetId` values = `{O, A, 'pro'}`) — proves the org→association select (282), the subscription→tier→slug chain (289–297), and the final `inArray(target_id, [O,A,'pro'])` (303–306) all bind against real SQL.
  - **module scoping:** a `feature_flag` row for the SAME `target_id:O` but `module_name='dues'` is NEVER returned by `findEnforcementFlags(O,'events')` (proves the `eq(module_name)` AND-clause).
  - **shape mapping:** assert returned objects carry `{targetType,targetId,moduleName,enabled,isOverride}` with the real persisted `is_override`/`enabled` booleans (not just length) — proves the `.map()` at 308–314.
  - **no-subscription fail-open narrowing:** org with NO `subscription` row → `findEnforcementFlags` still resolves `{O, A}` candidates (tier branch silently narrows via the try/catch, never throws) and returns only org+association flags. Proves the best-effort wrap at 284/299.
  - **no-association edge:** an org row whose `association_id` is absent from the candidate fan-out still returns the org-level flag (orgId is always pushed at 273).
  - **negative:** `findEnforcementFlags(<unknown org>, 'events')` → `[]` (no org row, no flags).
- **est commits:** 2 (1 createScratch enforcement suite + delete the skip prose; 1 the no-sub/no-assoc edge cases).

### Slice 2 — `findEnforcementFlags` precedence input contract with the gate (inter-module: repo ⇄ feature-flag-gate) — characterize
- **axis:** inter-module
- **files to CREATE:** extend `feature-flag-enforcement.integration.test.ts` with a `describe('gate precedence over real rows')` block; import the REAL `applyFeatureFlagGate`/precedence resolver from `src/middleware/feature-flag-gate.ts` and feed it the REAL rows returned by `findEnforcementFlags` (not the canned stub the existing `feature-flag-gate.test.ts:40` uses).
- **asserts (real persisted rows → real precedence outcome):**
  - org-level `is_override:true, enabled:false` beats an `association` `enabled:true` for the same module → gate resolves DISABLED. Proves the override-precedence contract end-to-end (currently each half is tested in isolation: the gate against a stub, the repo against nothing).
  - tier `enabled:false` with no org/association rows → gate resolves DISABLED (tier-only path).
  - empty row set (module never flagged) → gate **fail-open** (enabled) per the documented "fail-open when none" behavior (platform-admin.repo.ts:267).
  - **Surface, do not fix:** if the precedence resolver and the repo disagree on what `target_type:'tier'` keys on (the repo pushes the tier **slug** into `target_id`, while feature_flag rows for tiers must also be keyed by slug, not tier UUID) — assert the real round-trip and flag any mismatch as a product/data-integrity decision rather than silently rewriting either side.
- **est commits:** 1.

### Slice 3 — `getNationalChapterDetail` real-PG: suppression + aggregation against a persisted snapshot (DoD #2/#3) — upgrade mock→real
- **axis:** workflow (BR-36 access already proven by the integration suite's national-officer block; this slice proves the drill-down MATH against a real row)
- **note:** `getNationalChapterDetail.ts` is ALREADY 100% line via the mock test (`national-endpoints.test.ts:102`) — do NOT delete that; it is fast handler-branch coverage. This slice adds the missing REAL-DATA proof that the suppression/cents/compliance math holds when the snapshot is read back through `DashboardRepository.getChapterSnapshot` against persisted `chapter_snapshot` rows.
- **files to CREATE:** `services/api-ts/src/handlers/platformadmin/getNationalChapterDetail.integration.test.ts`
- **pattern:** `createScratch(['chapter_snapshot','national_dashboard_access','organization'])`; build a real `DashboardRepository(H.db as any)`; seed a `chapter_snapshot` + a `national_dashboard_access` grant (for `resolveAssociationAccess`); drive the handler with a `makeCtx` whose `'database'` is `H.db`.
- **asserts (real persisted/aggregated data):**
  - **above-suppression chapter:** seed snapshot `{total_members:100, active:80, grace:10, lapsed:7, suspended:3, cpd_compliance_rate:'0.9', total_collected:'5000', collection_rate:'0.85', activity_count_90d:12}`; grant the caller national access; call handler → `data.totalMembers===100`, `memberStatusBreakdown==={active:80,grace:10,lapsed:7,suspended:3}`, `creditComplianceBreakdown.compliant===90` (round(0.9*100)) and `.nonCompliant===10`, `collectionRate===85` (0.85*100), `totalRevenueCents===500000` (toCents(5000)), `eventCount===12`, `isSuppressed===false`. Assert the values came through real `getChapterSnapshot` read-back (not a stub) — the `organizationName` resolves via real `getOrgNames` against a seeded `organization` row.
  - **suppressed chapter (M14-R2):** seed `{total_members: <k-anonymity threshold-1>}` (use the value `isSuppressed` actually trips on — read `utils/national-access.ts isSuppressed`); call handler → `isSuppressed===true`, and EVERY suppressed field is ZEROED: `activeMembers===0`, `collectionRate===0`, `totalRevenueCents===0`, `creditCompliance===0`, `memberStatusBreakdown==={active:0,grace:0,lapsed:0,suspended:0}`, `creditComplianceBreakdown==={compliant:0,nonCompliant:0,exempt:0}`. This is the load-bearing privacy guarantee for a national-tier endpoint — currently proven only against a stub.
  - **wrong-association scope:** snapshot exists for org under association `A`; caller has access only to association `B` → `getChapterSnapshot(org,month,B)` returns undefined → handler throws `NotFoundError('Chapter')`. Proves the assoc-scoped WHERE on real rows (the `getNationalChapterDetail` 404 branch at :41).
  - `auditResourceId`/`auditDescription`/`auditDetails` set on the ctx (the audit middleware contract) — assert `ctx.get('auditResourceId')===organizationId`.
- **est commits:** 2 (1 above-suppression + name/access read-back; 1 suppression-zeroing + wrong-assoc 404 + audit set).

### Slice 4 — Thin Hurl/e2e national flows: add real-data assertions where cheap (DoD #3) — characterize/surface
- **axis:** workflow
- **scope:** the §7 / brief flags "thin Hurl/e2e flows (status-only)". AUDIT first, then add cheap data asserts. Verify which flows assert only `status: 200`:
  - find platformadmin national flows: `grep -rln "national\|chapters\|platform/summary\|enforcement" specs/api/tests/contract/` and the admin e2e specs `apps/admin/tests/e2e/`.
- **files to EDIT (in place):** the status-only `.hurl`/`.spec.ts` national-dashboard + chapter-detail flows.
- **asserts (add only where the data is already deterministic from seed — do NOT invent new fixtures):**
  - chapter-detail Hurl: after `GET /admin/national/chapters/{orgId}`, assert `jsonpath "$.data.isSuppressed"` and `jsonpath "$.data.totalMembers"` match the seeded snapshot (not just `status == 200`).
  - national-summary: assert `jsonpath "$.data[0].associationId"` ordering / `collectionRate` against seeded aggregate (the same numbers proven in Slice 3).
  - If a flow's seed is non-deterministic / fixture-less, leave it status-only and FLAG it in the commit as a fixture gap (do not fabricate a seed path). Surface, don't force.
- **est commits:** 1.

### Slice 5 — Ratchet floor + registry/coverage-matrix drift (DoD #5/#6) — LAST [WILL BE FOLDED INTO ONE B4 FINALIZE]
- **axis:** integ/BR (housekeeping)
- **edit:** `services/api-ts/.coverage-thresholds.json` — ADD `"src/handlers/platformadmin": { "line": 50, "function": 0 }` (no key today). Measured module-min is `platform-admin.repo.ts` ~75% line (per-dir run) once `findEnforcementFlags` 269–315 is covered by Slice 1; set the floor to **50** (mid-tier target, comfortably below the measured min for CI/full-suite variance — matches booking/assoc:ops/notifs convention, no number-chasing). Add a `_comment_platformadmin` note recording: enforcement-join un-skipped onto createScratch; getNationalChapterDetail suppression/aggregation proven real-PG; the hand-DDL 6-class suite stays.
- **edit:** br-registry / coverage-matrix — flip BR-36 enforcement-flag + national drill-down rows from SHALLOW/mock to real-tested with the new integration paths cited; remove the stale "findEnforcementFlags SKIPPED/out-of-scope" annotation.
- **asserts:** `bun test:unit && bun test:integration` green incl. the new enforcement + chapter-detail integration suites; coverage gate green at the new floor; matrix has no stale "SKIPPED/MISSING" row for `findEnforcementFlags` or `getNationalChapterDetail` real-data.
- **est commits:** 1 (folded into the single B4 finalize alongside storage/comms/elections).

---

**Totals:** 5 slices, ~7 commits. **No un-gate needed** (unlike notifs/comms) — the harness already runs in CI; platformadmin's real B4 work is **un-skipping `findEnforcementFlags`** (the one deliberately-skipped real-PG hole, coverage-confirmed at uncovered lines 269–315) and **upgrading getNationalChapterDetail's suppression/aggregation from mock to real-PG**. Execution order: Slice 1 first (the headline gap + DoD #1 harness extension), then Slice 2 (gate precedence contract), Slice 3 (drill-down real math), Slice 4 (cheap e2e data asserts), Slice 5 folded into the B4 finalize. **One product-decision surfaced** (Slice 2): the tier-flag keying (`target_id` = pricing-tier **slug** vs UUID) is a repo/data-integrity contract that must be characterized, not silently changed. **No real bugs identified in platformadmin source** — `findEnforcementFlags` SQL is correct-but-unproven (characterization), and the `getNationalChapterDetail` mock test is test-fragility (real-PG replacement/augmentation, source already correct at 100% line).
