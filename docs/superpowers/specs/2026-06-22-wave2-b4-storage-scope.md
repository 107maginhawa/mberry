### 5.9 storage

## storage module — Wave-2 (cluster B4, realtime-admin) TDD slice plan

Floor currently **15** (`.coverage-thresholds.json`: `"src/handlers/storage": { "line": 15, "function": 0 }`). Effort **S**. Method (locked, from `2026-06-21-test-gap-ledger.md`): characterize existing code → TDD new behavior; where a MISSING BR is a real bug, red-test then fix the RIGHT layer. DoD priority: (1) real-PG harness runs in CI → (2) MISSING BRs get real tests → (3) MISSING workflows get real-flow tests → (4) inter-module contracts proven → (5) ratchet floor toward 50 only as real coverage lands → (6) fix registry/threshold drift.

**Cluster role (B4 = maintenance, keyed by remediation TYPE not data sharing):** storage is the **non-sharer / pattern template** — one tiny single-table repo (`stored_file`) that already owns a genuine real-PG integration suite. It builds NO shared B4 fixture; comms/platformadmin copy its `createScratch` pattern, elections borrows member/governance fixtures (not storage DDL). Per plan §7 the headline "storage integ 3→0" is **already satisfied** for the org-scoping path — the real work here is (a) the integration test is real-PG but **NOT schema-faithful** (hand-DDL, not `createScratch`/`LIKE INCLUDING ALL`) so it masks a real DB drift; (b) the BR-31 SVG suite is a pure **illusion**; (c) there is no true lifecycle workflow test.

**Source facts verified (against source + live catalog `psql monobase`):**
- Single repo: `src/handlers/storage/repos/file.repo.ts` (`StorageFileRepository extends DatabaseRepository`). Only the `stored_file` table. Custom code: `buildWhereConditions` (org/status/owner filters, lines 38-56) + `updateOneStatusById` (lines 62-87). Everything else inherited.
- Existing real-PG file: `repos/storage-repos.integration.test.ts`. **Genuine real-PG, runs in CI** — it is NOT `if(process.env.CI)return` gated; it only skips on `dbReachable=false` (lines 99-114, `console.warn … skipping`). HOWEVER it uses **raw `new Pool` + hand-written DDL** (lines 52-76), NOT `createScratch`. Its hand-DDL declares `organization_id uuid NOT NULL` (line 67) — but the LIVE table does not (see below). So the suite proves the SQL fires, but against a table whose constraints diverge from production.
- **Live catalog `\d stored_file`:** columns `id uuid PK default gen_random_uuid()`, `version int NOT NULL default 1`, `created_by uuid` (nullable), `updated_by uuid` (nullable), `filename varchar(255) NOT NULL`, `mime_type varchar(100) NOT NULL`, `size bigint NOT NULL`, `status file_status NOT NULL default 'uploading'`, `owner uuid NOT NULL`, `uploaded_at timestamptz default now()`, **`organization_id uuid` — NULLABLE (no NOT NULL)**. Indexes: `stored_file_pkey` (id), `stored_files_org_idx` (organization_id), `stored_files_owner_idx` (owner). NO unique index, NO CHECK constraints, NO FKs.
- **SCHEMA-vs-DB DRIFT (sharp):** Drizzle schema `repos/file.schema.ts:23` declares `organizationId: uuid('organization_id').notNull()` but the live column is NULLABLE. This is the EXACT pattern that produced the events Slice-4 real bug (migration 0079) — schema says NOT NULL, DB never enforced it. The integration test's hand-DDL hard-codes the NOT NULL it WANTS, so it can never detect the drift.
- **`file_status` enum** (live `pg_enum`): `uploading`, `processing`, `available`, `failed` (in that sort order).
- **BR-31 SVG:** `br-31.svg-upload-security.test.ts` tests `sanitizeSvg`/`removeScriptTags`/`removeEventHandlers`/`removeExternalReferences`/`svgHasSecurityRisk` — all **defined inline in the test file** (lines 20-61). **No such sanitizer exists anywhere in `src/`.** The REAL production XSS control is **block, not sanitize**: `uploadFile.ts:19-34` `ALLOWED_MIME_TYPES` deliberately EXCLUDES `image/svg+xml` (comment line 20: "SVG excluded — requires content sanitization to prevent XSS"); and `src/utils/sanitize.ts:39-61` `isBlockedDocumentFile()` blocks `image/svg+xml` MIME + `.svg`/`.svgz` extensions (used by the documents path). `uploadFile.test.ts:188-189` already proves storage rejects `image/svg+xml` with a ValidationError.
- **Handler tests** (`storage-handlers.test.ts`, `uploadFile.test.ts`) are all **fully mocked** — repo prototype patched (line 118 comment "Patch repo prototype"), every handler tested in isolation with stubbed `findOneById`/`updateOneStatusById`. There is NO test that drives upload→complete→download through a REAL persisted `stored_file` row + real status transitions.
- Canonical harness: `src/test-utils/pg-scratch.ts → createScratch([...tables])` (`CREATE TABLE … (LIKE public.<t> INCLUDING ALL)` — copies columns/defaults/NOT NULL/CHECK/indexes, drops FKs; `dbReachable` skip-guard; `teardown()`). Storage's existing suite predates it.

---

### Slice 1 — Migrate `storage-repos.integration.test.ts` onto `createScratch` (schema-faithful) + expose the org_id NOT NULL drift — ✅/RED
- **axis:** integ (harness — highest leverage, DoD #1)
- **files to CREATE / EDIT in place:** `src/handlers/storage/repos/storage-repos.integration.test.ts` — replace the raw `new Pool` + hand-written `ddl()` (lines 32-140) with `const H = await createScratch(['stored_file'])` from `@/test-utils/pg-scratch`; `const repo = new StorageFileRepository(H.db as any)`; guard every test `if (!H.dbReachable) return`; `afterAll(() => H?.teardown())`. Drop the hand-DDL entirely so the suite runs against `LIKE public.stored_file INCLUDING ALL` (the REAL constraints).
- **asserts (real outcomes against Postgres / real data):**
  - All existing assertions stay green on the faithful table: `createOne` round-trips `id`/`organizationId`/`owner`/`filename`/`size`/`status='available'`/`version=1` (read-back via `H.scopedPool.query`); `findOneById` hit + null-miss; `findOne`/`findMany`/`count` org+owner+status scoping; cross-org isolation (ORG_B query returns 0 / null / count 0 against an ORG_A row); `findManyWithPagination.totalCount` counts only the requesting org; `updateOneStatusById('uploading'→'available')` flips status AND bumps `version` 1→2 (read-back confirms); `deleteOneById` removes the row and leaves a sibling-org row intact.
  - **DEFAULT proof (now real, was hand-faked):** `createOne` without `status` persists `status='available'`? No — without status, the column default `'uploading'::file_status` applies; assert a minimal insert (only required cols) yields `status='uploading'`, `version=1`, `uploaded_at` non-null — proving the real defaults, not the test's hand-DDL copies.
  - **`status` is a real `file_status` enum, not text:** inserting `status='bogus'` raises Postgres `22P02` (invalid input value for enum file_status). The old hand-DDL modelled status as `text` (line 71) so this could NEVER fire — the migration to `LIKE` makes the enum real.
  - **org_id drift (red-then-decide):** `repo.createOne({ ...without organizationId })` against the faithful table **SUCCEEDS** (org_id is nullable in prod) — assert the persisted row has `organization_id IS NULL`. This RED result contradicts `file.schema.ts:23 .notNull()`. → flag as DRIFT: either (a) real bug → add a migration `SET NOT NULL` on `stored_file.organization_id` (backfill/delete nulls first, mirroring 0079) and flip the assert to expect `23502`; or (b) product decides nullable is intended → fix the Drizzle schema to drop `.notNull()`. Surface for decision in the commit; do NOT silently pick. (Spot-check: `uploadFile.ts:103` reads `organizationId` from `ctx.get('organizationId')` and passes it; a missing org header would write a null org_id row today — that is the live exposure.)
- **est commits:** 2 (1 harness migration + green carry-over + default/enum asserts; 1 the org_id-drift RED test + DRIFT flag).

### Slice 2 — BR-31 SVG: kill the phantom-sanitizer illusion, repoint at the REAL block control — ✅
- **axis:** BR (security)
- **files to CREATE / REPLACE:** rewrite `src/handlers/storage/br-31.svg-upload-security.test.ts` to import the REAL prod functions — `isBlockedDocumentFile` from `@/utils/sanitize` and the storage upload MIME guard (drive `uploadFile` with a stubbed ctx, mirroring `uploadFile.test.ts:188`). DELETE the inline `sanitizeSvg`/`removeScriptTags`/`removeEventHandlers`/`removeExternalReferences`/`svgHasSecurityRisk` helpers (lines 20-61) — they test code that does not exist in production.
- **classification:** **test-fragility (illusion)**, NOT a real security bug. Prod control is *block-not-sanitize*: SVG is rejected at the MIME allowlist (storage) and at `isBlockedDocumentFile` (documents). The phantom test was green-but-vacuous. The fix is to point assertions at the real control. (If, while doing this, any prod path is found that accepts SVG bytes WITHOUT going through the block, THAT would be the real stored-XSS bug — escalate then; current evidence says both paths block.)
- **asserts (real prod behavior):**
  - `isBlockedDocumentFile('icon.svg', 'image/svg+xml')` → `true`; `isBlockedDocumentFile('logo.svgz')` → `true` (extension-only, no mime); `isBlockedDocumentFile('photo.png', 'image/png')` → `false`; `isBlockedDocumentFile('report.pdf', 'application/pdf')` → `false`. Proves the REAL allow/deny boundary (`@/utils/sanitize.ts:56-61`).
  - `uploadFile` with `mimeType:'image/svg+xml'` throws `ValidationError("File type 'image/svg+xml' is not allowed")` — the prod allowlist (`uploadFile.ts:71-73`) rejects SVG BEFORE any DB write (assert `repo.createOne` is never called). This is the load-bearing storage XSS control.
  - Positive: `uploadFile` with `mimeType:'image/png'` passes the allowlist (does not throw at the MIME gate). Documents `image/jpeg`/`application/pdf` not blocked by `isBlockedDocumentFile`.
  - **Commit note:** record that BR-31's design is "reject SVG", not "sanitize SVG"; the old sanitizer test was an illusion (no prod implementation). Update BR registry BR-31 row from "SVG sanitized before storage" to "SVG upload blocked (MIME allowlist + extension/mime block)".
- **est commits:** 1.

### Slice 3 — Upload → complete → download lifecycle against real persisted state (the workflow gap) — ✅
- **axis:** workflow
- **files to CREATE:** `src/handlers/storage/storage-lifecycle.integration.test.ts` — `createScratch(['stored_file'])`, real `StorageFileRepository`, a **fake `StorageProvider`** (in-memory set tracking `generateUploadUrl`/`verifyFileExists`/`generateDownloadUrl` — bytes/MinIO are OUT OF SCOPE; this is a METADATA-level lifecycle test, noted explicitly). Drive the real handlers (`uploadFile`, `completeFileUpload`, `getFileDownload`) with a thin ctx whose `database` is `H.db`, asserting the persisted ROW transitions, not just responses.
- **asserts (real persisted row + real status transitions):**
  - `uploadFile` (mime `application/pdf`) → response 201 with `file` (the id); read-back via `H.scopedPool`: exactly one `stored_file` row, `status='uploading'`, `owner=user.id`, `organization_id` = the ctx org, `version=1`. Proves the create branch persists.
  - `completeFileUpload` on that id (storage `verifyFileExists`→true) → response 200; read-back: same row id, `status='available'`, `version` bumped (uploading→processing→available = TWO `updateOneStatusById` calls per `completeFileUpload.ts:80-84`, so version 1→3). Assert the real version count proves both transitions ran against SQL, not a mocked single flip.
  - **verify-failure branch:** a second uploaded row, `verifyFileExists`→false → `completeFileUpload` throws `BusinessLogicError('UPLOAD_VERIFICATION_FAILED')` AND the row is persisted as `status='failed'` (read-back) — the `completeFileUpload.ts:73-76` failure path against real PG.
  - `getFileDownload` on the now-`available` row (same owner) → response 200 with `downloadUrl` + `file` payload whose `status='available'`. A download attempt while `status='uploading'` (a fresh un-completed row) → `BusinessLogicError('FILE_NOT_AVAILABLE')` (`getFileDownload.ts:71-73`).
  - **tenant guard end-to-end:** `completeFileUpload` / `getFileDownload` with a ctx `organizationId` != the row's org → `ForbiddenError` BEFORE owner check (`completeFileUpload.ts:55`, `getFileDownload.ts:58`) — assert the foreign-org caller cannot finalize/download even with the right UUID, against a real persisted row.
- **est commits:** 2 (1 happy lifecycle upload→complete→download with version-count proof; 1 the verify-failure + tenant-guard + not-available branches).

### Slice 4 — Ratchet the storage floor + fix registry/threshold drift — ✅ (folded into one B4 finalize)
- **axis:** integ/BR (housekeeping — DoD #5/#6)
- **files to EDIT:** `services/api-ts/.coverage-thresholds.json` — raise `"src/handlers/storage"` `line` from **15** toward **50** to the actual measured line coverage after slices 1-3 land (set to the real number, no number-chasing; with the lifecycle test driving uploadFile/completeFileUpload/getFileDownload + the repo suite, the module min should clear 50 — measure, don't guess). Update `br-registry.json` BR-31 (illusion→real block control) and any coverage-matrix rows for `storage-repos.integration` (now schema-faithful + CI), `storage-lifecycle.integration` (new), and the `file.repo` org-scoping.
- **asserts:** `bun test` (api) green incl. the schema-faithful integration suite + the new lifecycle suite; coverage gate / `audit:workflows` passes at the new floor; registry has no stale "MISSING/SHALLOW/illusion" rows for the items this plan made REAL; the org_id-drift decision (Slice 1) is recorded (migration filed OR schema corrected).
- **est commits:** 1 (folded into the single B4 finalize commit alongside comms/platformadmin/elections housekeeping).

---

**Totals:** 4 slices, ~6 commits. **Harness-first** (Slice 1) is the highest-leverage move — not because storage lacks a real-PG test (it has one, and it runs in CI), but because that test is **not schema-faithful** and therefore masks a live org_id NOT-NULL drift; migrating it to `createScratch`/`LIKE INCLUDING ALL` both un-masks the drift AND makes the enum/default behavior real. Slice 2 retires the BR-31 sanitizer **illusion** (no prod sanitizer exists; prod blocks SVG) — pure test-fragility, repointed at the real control. Slice 3 adds the missing metadata-level upload→complete→download lifecycle (bytes/MinIO out of scope). One product/eng decision surfaced: **`stored_file.organization_id` is NULLABLE in prod but `.notNull()` in the Drizzle schema** — migration-to-enforce vs schema-correction, raised not silently fixed.
