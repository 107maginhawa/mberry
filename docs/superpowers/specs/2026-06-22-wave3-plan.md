# Wave-3 Test-Gap Remediation Plan — Peripheral Tier (floor 40)

**Date:** 2026-06-22
**Scope:** 5 peripheral modules — marketplace, advertising, audit, jobs, onboarding. Coverage floor **40** (peripheral; vs mid-50, critical-70).
**Method (locked, same as Waves 1–2):** characterize existing code → TDD new behavior; a MISSING-BR that is a real bug → red-test then fix the RIGHT layer. Asserts are real DATA (persisted rows, SQLSTATE, emitted payloads), never `toBeDefined`/200-only. DoD = the 6-point list in `2026-06-21-wave2-plan.md §6`.

Scoped by a 5-agent workflow (`wf_d4cb1a7e-e14`) reading real source + live `monobase` catalog. Full per-module slice plans (B1-format) are in sibling files:
- `2026-06-22-wave3-marketplace-scope.md`, `…-advertising-scope.md`, `…-audit-scope.md`, `…-jobs-scope.md`, `…-onboarding-scope.md`.

**Execution order: marketplace → advertising → audit → jobs → onboarding.**

> **B4 LESSON carried in:** several "real-PG" suites here use raw `new Pool` on the shared `public` schema (transaction-rollback or hand-scoped cleanup) instead of `createScratch` — the faithfulness fix is migrate to `createScratch(LIKE public.<t> INCLUDING ALL)`. Two are also **CI-gated** (`if(process.env.CI)return`) and never run on the gate (jobs, onboarding). And a migration that tightens a uniqueness/NOT-NULL invariant can break a flow that inserts without it — check the route + backfill before shipping one.

| Module | Harness today | Slices | Floor (cur → plan) | Real bug? | Sharpest gap |
|---|---|---|---|---|---|
| marketplace | 3 repos genuine real-PG but hand-rolled (tx-rollback-on-public + brittle prototype-restore), skip-only | 8 | 5 → 40 (measured) | conditional (IDOR red-test) | migrate to createScratch + delete restore hack; vendor/listing/order FSM + JSONB `@>`; **product decisions**: order FSM dead-states (confirmed/refunded un-driven), no DB CHECK on quantity/price; **red-test** getVendor/getOrder cross-org guard |
| advertising | 3 repos genuine real-PG (hand-rolled); **advertiser.repo has NO integration test** | 7 | 5 → 40 (measured) | **YES — opt-out** | new advertiser harness + migrate 3 to createScratch; **REAL BUG**: `member_ad_opt_out` has no `unique(org,person)` → TOCTOU duplicate opt-outs + optIn straggler → member silently stays opted-out (consent/privacy) → migration 0082 + ON CONFLICT + optIn delete-all; getAdForPlacement serve-gating (cross-org leak); reportAd auto-pause |
| audit | genuine createScratch pattern exists OUT of module (person/audit-no-pii); in-module `audit.repo.test.ts` is a **fake-db illusion** | 6 | **80 held (re-backed)** | none | floor 80 is illusion-inflated (drop the fake-db file → audit.repo.ts 94%→29%); build the in-module real-PG suite (logEvent persistence, buildWhereConditions org-scope, archive/purge/stats, verifyIntegrity tamper); **product decisions**: no append-only trigger, no purge_after guard |
| jobs (M15 Job Board) | one genuine real-PG file, but **CI-gated** (`if(CI)return`) + raw-Pool/public | 6 | 5 → 40 (measured) | **deferred (not silently built)** | createScratch + un-gate; constraint/enum/default + cross-org IDOR proofs; **BR-37 expiry is dead code** (no cron) — p2-deferred → **characterize + surface, do NOT add the cron** (out of test-gap scope); dup-application race → characterize + follow-up |
| onboarding | one genuine real-PG file, but **CI-gated** (`if(CI)return`) + raw-Pool/public | 6 | 15 → 40 (measured) | none | createScratch + un-gate; 23505 per-org unique + 23502 + jsonb dedup + M01-004 ordering BRs; emit-once completion contract; **product decision**: `onboarding.completed` event has ZERO consumers (fires into the void) |

**Decisions taken (owner defers):**
1. **advertising opt-out** (`member_ad_opt_out` missing `unique(organization_id, person_id)`) — **SHIP migration 0082** (pre-backfill dedupe keeping `min(opted_out_at)` if any dups exist → add unique index) + `addOptOut` ON CONFLICT DO NOTHING + `optIn` delete-ALL. Consent/privacy data-integrity fix; the opt-out route is user-scoped with org always present, so no org-middleware breakage.
2. **jobs BR-37 expiry** — **characterize only, do NOT build the expiry cron.** BR-37 is `p2-deferred` ("not yet implemented") in the registry; adding a cron is deferred product work, out of test-gap scope. Prove `listExpired`/`extendPosting` at the repo level, document the missing cron as a known-gap (postings don't auto-expire), and fix the registry drift (the BR-37 test path points at a non-existent file). Surfaced for a future product decision. (Mirrors the elections `updateElectionStatus` orphan handling.)
3. **jobs dup-application race** (no `unique(person_id, posting_id)`) — characterize (prove two concurrent applies both persist today); recommend a partial-unique migration as a **follow-up** (needs backfill dedupe; out of the floor-40 ratchet scope).
4. **audit floor 80** — HOLD at 80 but **re-back it with real PG** (retire the fake-db illusion only after the createScratch suite covers its lines); set the honest measured number if real coverage can't sustain 80.
5. **marketplace** — order FSM dead-states (confirmed/refunded) + missing DB CHECK on quantity/price = **characterize + surface, no silent fix**; getVendor/getOrder cross-org guard = **red-test, fix only if genuinely missing** (mirror the sibling mutate-handler guards).
6. **onboarding `onboarding.completed` orphan event** (emitted, zero consumers) — surface as a contract gap, assert current behavior, do NOT silently add a consumer (mirrors the booking missing-consumer precedent).

**No org_id NOT-NULL drift** in marketplace / audit / onboarding (Drizzle `.notNull()` matches the live catalog — verified). The only Wave-3 migration is advertising 0082 (opt-out unique index).

---

## ✅ WAVE-3 COMPLETE (2026-06-22)

All 5 peripheral modules landed on `wave3/peripheral` (30 builder slices), then folded into one finalize commit.

### Slices + commit sha ranges (per module)
| Module | Slices | Sha range (S1 → last) |
|---|---|---|
| marketplace | 7 (S1–S7) | `1bd7e8d3` → `3ca14898` |
| advertising | 6 (S1–S6) | `70ac34b5` → `ab019a3d` |
| audit | 5 (S1–S5) | `7d8af626` → `99f25a3c` |
| jobs (M15) | 5 (S1–S5) | `a0a24729` → `9480f68a` |
| onboarding | 6 (S1–S6) | `8c59871e` → `22c9a6b0` |

### Real bugs fixed (2)
1. **marketplace `getVendor` cross-org IDOR** (S6, `14246798`) — `getVendor.ts` returned a vendor regardless of `x-org-id`; red-tested then org-scoped to mirror the sibling mutate-handler guards.
2. **advertising opt-out missing uniqueness** (S3, `ad1f83a5`) — `member_ad_opt_out` had no `unique(organization_id, person_id)` → TOCTOU duplicate opt-outs + `optIn` straggler left a member silently opted-out (consent/privacy). Fix = **migration 0082** (`0082_unique_member_ad_opt_out_org_person.sql`, pre-backfill dedupe keeping `min(opted_out_at)` → add unique index) + `addOptOut` ON CONFLICT DO NOTHING + `optIn` delete-ALL.

### Test-infra fix
- **preload-pristine prototype-pollution** — marketplace + advertising repos migrated off the brittle hand-rolled prototype-restore machinery onto `createScratch`, and the repos were **registered in `src/test-utils/preload-pristine.ts`** (the pollution was cured by registration, NOT by deleting the handler mock tests, which remain as guard coverage).

### Floors set (`services/api-ts/.coverage-thresholds.json`) — honest measured per-file min
| Module | Floor before → after | Full-suite module-min line% (file) |
|---|---|---|
| marketplace | 5 → **80** | 86.59 (`marketplace.schema.ts`) |
| advertising | 5 → **80** | 88.51 (`advertising.schema.ts`) |
| audit | **80 held** | 85.11 (`audit.schema.ts`) — now real-PG backed |
| jobs | 5 → **80** | 87.50 (`jobs.schema.ts`) |
| onboarding | 15 → **85** | 92.86 (`onboarding.schema.ts`) |

All floors set just below each module's lowest-covered file (schemas are the floor-draggers) with CI headroom — no number-chasing, all far above the peripheral-40 target.

### Audit illusion deleted
- Deleted `src/handlers/audit/repos/audit.repo.test.ts` (a `makeMockDb` scripted-chain fake-db illusion) AFTER the in-module real-PG `audit.repo.integration.test.ts` (S1–S5) covered logEvent persistence + enum/org invariants, `buildWhereConditions` filter SQL + findMany/count, archive/purge retention state-machine + `getAuditStatistics`, `verifyIntegrity` tamper-detection over persisted rows, and `listAuditLogs` org-scope. **Measured `audit.repo.ts` = 99.05% line WITH the illusion AND 99.05% WITHOUT it** — the real-PG suite, not the mock, was carrying the file. No coverage lost; floor HELD at 80 (now honest, was illusion-inflated).

### Known-gaps + product decisions (all characterize-only, no silent fix)
- **jobs BR-37 expiry** — dead code (no cron; postings don't auto-expire). `listExpired`/`extendPosting` characterized at the repo level; BR-37 stays `p2-deferred`, registry drift fixed (repointed off the non-existent `events/br-37.job-posting-expiry.test.ts` to the real `jobs.repo.integration.test.ts`).
- **jobs dup-application race** — no `unique(person_id, posting_id)`; two concurrent applies both persist today. Recommended as a follow-up partial-unique migration (needs backfill dedupe).
- **onboarding `onboarding.completed` orphan event** — emitted with ZERO consumers; surfaced as an unconsumed-event contract test, NOT silently given a consumer.
- **marketplace** — order FSM dead-states (`confirmed`/`refunded` un-driven) + no DB CHECK on quantity/price; characterized + surfaced.
- **audit** — no append-only DB trigger; purge ignores per-row `purge_after`; surfaced.
- **advertising** — creative `pause` = revert-to-pending semantics; surfaced.

### BR-registry updates (`docs/ver-3/business/br-registry.json`)
- **BR-37** — repointed backend to `jobs.repo.integration.test.ts`, kept deferred + p2 (cron is dead code), deferredReason updated to document the characterize-only decision.
- **BR-38** — flipped `p2-deferred` → `p1-business` (REAL), repointed to `verifyVendor.test.ts` + `vendor.repo.integration.test.ts` + `marketplace-org-isolation.integration.test.ts` (the implemented contract is the admin vendor-verification gate; the old "Referral Disclosure" billing path was a deferred placeholder that never existed).

### Matrix baseline delta
- `scripts/audit/coverage-baseline.json` = **A=0 B=0 C=15 (unchanged)** — Wave-3 is backend test-gap work and added no frontend routes/flows/phase-1 BR gaps. Matrix gate PASS, no regression.

### Full gate (all GREEN)
- `bun test` (full API suite): **8650 pass / 0 fail / 6 todo** — 8656 tests across 838 files.
- `bun run typecheck` (cross-workspace): **0 errors** (api-ts, sdk-ts, admin, memberry, ui all exit 0).
- coverage gate (`scripts/coverage-gate.ts`) at new floors: **PASS** (774 files checked).
- coverage-matrix gate (`scripts/audit/coverage-matrix.ts --gate`): **PASS** (A=0 B=0 C=15, no regression).
