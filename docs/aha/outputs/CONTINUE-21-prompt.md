# Continuation prompt — AHA Step 21 (next `04`: Dues & Payments — **Batch B subset** decision-free: FIX-004 position-gate + FIX-005 fund-split validation + FIX-006 invoice self-scope)

Paste the block below into a fresh session (after /clear), or run: `execute docs/aha/outputs/CONTINUE-21-prompt.md`.

> This is a **`04-module-or-group-fix-tdd.md`** pass (TDD fix, ONE module, decision-free subset). Three independent P1 fixes, all `V1 REQUIRED`, none product-blocked. **FIX-004** — add a **Treasurer/President position gate** to ~10 currently-ungated financial dues mutations (`updateDuesConfig`, `deleteDuesConfig`, `upsertDuesGatewayConfig`, `disconnectDuesGateway`, `testDuesGatewayConnection`, `upsertDuesFunds`, `createDunningTemplate`, `updateDunningTemplate`, `deleteDunningTemplate`, `runDunning`, `recalculateAgingBucket`); gateway creds / dues amounts / fund splits are currently mutable by any `association:admin` org member. **FIX-005** — wire the **existing** `validateFundSplits` util into `upsertDuesFunds` so fund percentages must sum to 100% server-side (today: zero server validation). **FIX-006** — **self-scope `listDuesInvoices`** for non-officers (today it returns ALL org invoices + person names + amounts to any member — a financial-privacy leak); mirror the PAY-02 self-scope `listDuesPayments` already uses. Follow the fix-ready plan as the primary guide. Do NOT expand scope. **Do NOT touch the cross-module settle seam** (`association:member/utils/settle-payment.ts` → `membership/utils/membership-lifecycle.ts`) — that rules out FIX-007 over-refund cap and FIX-010 proof-atomicity (both Batch B but they touch refund/expiry math; defer to a separate settle-seam pass). Do NOT start FIX-001/002/003 (Batch A — DONE), FIX-007 gateway-call / FIX-008 dunning-wiring / FIX-009 funnel / FIX-011..016 (Batch C, mostly PD-gated), any token-expiry / PayMongo / partial-refund-direction change (PD-gated), or any V2/Do-Not-Build item. **No new DB migration, no schema change** this pass. Stop after saving the fix report.
>
> **FIX-004 approach (prescriptive — read before coding):** the 10 ops are TypeSpec-generated. The codebase already uses `@extension("x-require-position", #["Treasurer", "President"])` in `specs/api/src/modules/dues-custom.tsp` (`getDuesDashboard`/`getDuesMetrics`/`getDuesMemberSummary`), and CLAUDE.md P1.5 mandates declaring static position gates via `@extension`, NOT hand-called `requirePosition`. **PRIMARY:** add `@extension("x-require-position", #["Treasurer", "President"])` to each of the 10 ops in `specs/api/src/association/member/dues.tsp`, then regenerate (`cd specs/api && bun run build` → `cd services/api-ts && bun run generate`). **Test wrinkle `[NEEDS CONFIRMATION]`:** the existing `dues-mutation-auth.test.ts` calls handlers **directly**, so it only exercises INLINE gating — it will NOT catch route-level generated middleware. Prove the TypeSpec gate with a **generated-output assertion** (read `services/api-ts/src/generated/openapi/routes.ts` and assert each of the 10 ops now carries the position middleware — RED before regen, GREEN after) and/or a **live Hurl contract case** (boot API on 7299, non-officer member → 403 on a representative op). **FALLBACK only if the typespec regen toolchain genuinely fails in-env (verify first):** inline `const denied = await requirePosition(ctx, [POSITION_TITLES.TREASURER, POSITION_TITLES.PRESIDENT]); if (denied) return denied;` at the top of each non-stub handler (mirror `recordDuesPayment.ts` / `confirmPaymentProof.ts`), proven via the existing `dues-mutation-auth.test.ts` direct-handler pattern; document the deviation as `[BLOCKED BY ENVIRONMENT]`. Do NOT implement the `runDunning` / `recalculateAgingBucket` stub LOGIC — only gate them (their logic is FIX-008 / Batch C).
>
> **ENV NOTE (verified working 2026-06-12):** Docker up (postgres/minio/mailpit/stripe-mock). DB `localhost:5432/monobase` migrated + seeded. **FIX-005 + FIX-006 need only `bun test`** (mock-based handler tests via `make-ctx.ts`; no live stack). **FIX-004 needs the TypeSpec regen toolchain** (`cd specs/api && bun run build`, then `cd services/api-ts && bun run generate`) — the prior dues pass noted `db:generate` (drizzle-kit) exits 127 by design, but that is the **migration** toolchain, NOT the typespec generator; the typespec build+generate is the standard P1.5 flow and `dues-custom.tsp` already proves the generator emits `x-require-position`. **Restart the API after regen** (new route middleware; no hot-reload). Optional live RBAC proof via Hurl: boot `cd services/api-ts && SERVER_PORT=7299 bun src/index.ts`, then `API_URL=http://localhost:7299 bun run test:contract` (`hurl` 8.0.1 installed). Kill throwaway servers when done (`lsof -ti tcp:7299 | xargs kill`). The full Hurl suite has **3 known pre-existing failures** (`impersonation-flow`, `member/governance/position-crud`, `platformadmin-extended-flow`) — none are dues; do NOT attribute them to this batch.

---

Continue the AHA remediation. Execute **`docs/aha/prompts/04-module-or-group-fix-tdd.md`** for **Dues & Payments, Batch B subset (decision-free: FIX-004 position-gate + FIX-005 fund-split validation + FIX-006 invoice self-scope)**, using TDD (RED→GREEN per fix). Then STOP after saving the fix report.

CODEBASE_ROOT = /Users/elad-mini/Desktop/memberry

## The canonical AHA prompt sequence (do not forget this)

```txt
00-aha-shared-rules.md            # rules (always loaded)
01-platform-discovery-audit-index.md   # DONE
02-module-or-group-audit-gap-plan.md   # DONE (dues-payments)
03-organize-gap-plan-for-fixing.md     # DONE (dues-payments)
04-module-or-group-fix-tdd.md          # RUN ONCE PER MODULE/BATCH — repeats (THIS PASS = dues-payments Batch B subset)
05-cross-cutting-pattern-audit.md      # DONE
06-database-schema-audit.md            # DONE (re-flag only if schema changes — FIX-004/005/006 add none)
07-consolidate-roadmap.md              # DONE + RE-RUN later (Track C)
```

Rules: never run `04` without a `03` fix-ready plan; execute only the SELECTED subset; stop after the fix report.

## What just completed (do NOT redo)

- **`04` Realtime Comms — Batch B subset — COMPLETE (2026-06-12).** FIX-007 (membership OR-shim: chat authz now grants if user is in JSONB `participants` OR the `chat_room_member` join table, via existing `isMember`, in `getChatRoom`/`getChatMessages`/`ws.chat-room.ts`; no new repo method) + FIX-009 (Vite `ws:true` verify-then-fix: live probe confirmed the dev proxy never forwarded the WS upgrade → added one line → re-probe shows the upgrade now reaches :7213; added a config-presence regression guard). Adversarially verified (Correctness/Scope SOLID; one Test-Integrity must-fix on FIX-009 regression coverage resolved). See `realtime-comms-fix-report.md` §"Batch B subset". Realtime-comms next items are PD-1/2/3-gated or the carry-forward DM-UI pass.
- **`04` Dues & Payments — Batch A — COMPLETE (2026-06-11).** FIX-002 (cross-org refund tenant guard), FIX-003 (per-org/year atomic receipt counter + scoped-unique, Batch F migration `0062`), FIX-001 (online-payment ledger: checkout mints pending `DuesPayment` row + correct `metadata.paymentId`; webhook settles by real UUID, flips status, marks invoice paid). Migration `0062` hand-written (live-apply was env-blocked at the time; the DB is migrated now). See `dues-payments-fix-report.md` §1–§15. **APPEND a "Batch B subset" section; do NOT rewrite the Batch A section.**
- **`04` Platform Admin Batch B, Marketplace/Ads/Reviews A–D, Jobs Batch A, and the full Track A list through A11 — all COMPLETE** (prior sessions).

## This pass — execute `04` for Dues & Payments, Batch B subset (decision-free)

1. Load + strictly follow `docs/aha/prompts/00-aha-shared-rules.md`, then `docs/aha/prompts/04-module-or-group-fix-tdd.md`.
2. Inputs:
   - Fix-ready plan (PRIMARY): `docs/aha/module-fix-plans/dues-payments-fix-ready-plan.md` (§3 FIX-004/005/006 rows; §4 Batch B; §5 test-first; §6 files; §7 shared deps; §8 Q-NC1; §11 Do-Not-Build; §12 root-cause).
   - Raw gap plan (CONTEXT): `docs/aha/module-gap-plans/dues-payments-gap-plan.md` (§14 RBAC gaps; BR-05 fund splits; PAY-02 self-scope).
   - Prior fix report (what's done — **APPEND** a "Batch B subset" section; do NOT rewrite the Batch-A section): `docs/aha/module-fix-plans/dues-payments-fix-report.md`.
   - Module slug = `dues-payments`. Readable name = "Dues & Payments". Dues handlers live under `services/api-ts/src/handlers/member/duesspecialassessments/`; repos under `services/api-ts/src/handlers/dues/repos/`.
3. Invoke `superpowers:test-driven-development` (RED-first). FIX-005/006 flip honest unit REDs. FIX-004 RED depends on the chosen proof (generated-output assertion before regen, or the direct-handler 403 test if using the inline fallback).
4. **Selected subset — Batch B decision-free (3 fixes):**
   - **FIX-004 — RBAC position gate on ~10 financial mutations (P1, V1 REQUIRED).** Currently only `record/confirm/reject` payment ops are gated (inline `requirePosition` in their handlers); the 10 ops above in `specs/api/src/association/member/dues.tsp` carry only `@extension("x-security-required-roles", #["association:admin"])` + `@extension("x-audit", ...)` — NO position gate. Gateway creds / dues amounts / fund splits are mutable by any `association:admin`. Fix via the **PRIMARY TypeSpec `@extension("x-require-position", #["Treasurer", "President"])` + regen** path (see the blockquote for the exact approach, the test wrinkle, and the env-blocked inline fallback). `Q-NC1` (who holds `association:admin`) only affects severity SIZING — the fix proceeds regardless and is decision-free. Gate the two stubs (`runDunning`, `recalculateAgingBucket`) too, but do NOT implement their logic.
   - **FIX-005 — server-side fund-split validation (P1, V1 REQUIRED).** `upsertDuesFunds.ts` calls `repo.replaceFunds(...)` with ZERO validation (only the client checks the 100% sum). Wire the existing `validateFundSplits(funds: FundSplit[]): string | null` from `services/api-ts/src/handlers/member/membership/utils/fund-math.ts` (`FundSplit = { fundId: string; percentage: number }`; returns null if valid, an error message string otherwise — it does NOT throw). Build `FundSplit[]` from the incoming `{ fundName, percentage }` (use `fundName` as `fundId`, `percentage` as a number — NOT the stringified value the handler maps for the DB) BEFORE `replaceFunds`, and `throw new ValidationError(msg)` when non-null. RED: `upsertDuesFunds` with splits summing ≠ 100 → 400/ValidationError; valid 100 → 200. New `upsertDuesFunds.test.ts` (no test exists today).
   - **FIX-006 — self-scope `listDuesInvoices` (P1, V1 REQUIRED).** `listDuesInvoices.ts` queries `repo.findManyWithPagination({ organizationId, membershipId?, status? })` with no officer/self check → any member sees all org invoices. Mirror the PAY-02 pattern from `listDuesPayments.ts`: `const denied = await requireOfficerTerm(ctx); const isOfficer = denied === null;` then constrain non-officers to their OWN invoices. **Nuance `[NEEDS CONFIRMATION]`:** invoices are keyed by `membershipId`, not `personId` (unlike `listDuesPayments`, which filters by `personId`). Verify how an invoice maps to the caller — resolve the caller's membership(s) in the org (`session.user.id` → membership) and filter `membershipId` to those; do NOT add broad new repo surface beyond what self-scoping needs. RED: plain member → sees only own invoices; officer → sees all. New `listDuesInvoices.test.ts` (mirror `listDuesPayments.test.ts` `[PAY-02]` block; both use `stubRepo(OfficerTermRepository, { findActiveByPersonAndOrg: async () => [...] })`).
5. **Do NOT implement in this pass (out of subset / gated / later):**
   - FIX-001, FIX-002, FIX-003 + Batch F migration `0062` — already DONE (Batch A). Do not redo.
   - **FIX-007 (over-refund cap + eligibility)** and **FIX-010 (`confirmPaymentProof` transaction atomicity)** — Batch B per the plan, but both touch the load-bearing settle/refund/expiry seam (`refundDuesPayment.ts` / `confirmPaymentProof.ts` → `settle-payment.ts` → `membership-lifecycle.ts`, `[CROSS-MODULE RISK]`). Excluded from THIS decision-free subset; run as a dedicated settle-seam pass.
   - **FIX-007 gateway-refund-API call** — `[NEEDS PRODUCT DECISION Q-PD6]` + billing dependency (Batch C/E).
   - **FIX-008** (dunning escalation wiring) — Batch C; only GATE `runDunning` here, do not wire `dunning-escalation.ts`.
   - **FIX-009** (first-invoice funnel / Pay-Now CTA / reminder links) — `[NEEDS PRODUCT DECISION Q-PD7/Q-PD8]` + cross-module (m05/m07).
   - **FIX-011..016** (pending-expiry timeout, dup-warning UI, webhook per-intent idempotency, life-member block, partial-refund rounding, Hurl coverage) — Batch C.
   - Token-expiry change (Q-PD1), PayMongo wiring (Q-PD5), partial-refund expiry direction (Q-PD2), and everything in fix-ready §10 Deferred / §11 Do-Not-Build. Do NOT register the 5 unregistered helper handlers as public endpoints.
   - **No DB migration, no schema change.** If a fix appears to need one, STOP and document it as blocked — do not expand scope.
6. TDD / test discipline: write/flip the failing test FIRST. FIX-005/006 are clean unit REDs (mock ctx via `make-ctx.ts`). FIX-004: if using the TypeSpec path, assert the generated `routes.ts` lacks the position middleware for the 10 ops BEFORE regen (RED) and carries it after (GREEN), and/or add a live Hurl 403 case; if forced to the inline fallback (regen broken), use the `dues-mutation-auth.test.ts` direct-handler `expect(res.status).toBe(403)` pattern. Do NOT weaken assertions or fake-green.
7. **Pre-flight reads BEFORE touching code (do not skip; line numbers may have drifted across prior passes — re-verify):** `handlers/member/duesspecialassessments/recordDuesPayment.ts` + `confirmPaymentProof.ts` (the existing inline `requirePosition([TREASURER, PRESIDENT])` pattern, ~L30–33, import from `@/core/auth/officer-checks`); `specs/api/src/association/member/dues.tsp` (the 10 ops, ~L842 `updateDuesConfig`, L857 `deleteDuesConfig`, L989 `recalculateAgingBucket`, L1005 `createDunningTemplate`, L1044/1058 dunning update/delete, L1084 `runDunning`, L1235 `upsertDuesGatewayConfig`, L1250 `testDuesGatewayConnection`, L1263 `disconnectDuesGateway`, L1290 `upsertDuesFunds`); `specs/api/src/modules/dues-custom.tsp` (existing `x-require-position` syntax); `handlers/member/duesspecialassessments/dues-mutation-auth.test.ts` (RBAC test harness — `stubRepo(OfficerTermRepository, …)` + `expect(res.status).toBe(403)`); `handlers/member/duesspecialassessments/upsertDuesFunds.ts` (~L27 `replaceFunds`, no validation) + `handlers/member/membership/utils/fund-math.ts` (`validateFundSplits`); `handlers/member/duesspecialassessments/listDuesInvoices.ts` (~L18–23 unfiltered) + `listDuesPayments.ts` (~L21–40 PAY-02) + `core/auth/officer-checks.ts` (`requireOfficerTerm`); the `dues-payments-fix-report.md` Batch-A section (so you APPEND, not overwrite). The CLAUDE.md "Audit + officer/position via TypeSpec extensions (P1.5)" section for the `@extension` contract.
8. **Regen workflow (FIX-004 only):** after editing `dues.tsp`, run `cd specs/api && bun run build` then `cd services/api-ts && bun run generate`; NEVER hand-edit generated files (`generated/openapi/*`); the regen WILL change generated routes/validators/openapi/SDK — that is expected churn, not a hand-edit. Restart the API after regen. FIX-005/006 are handler-local (no regen). If the typespec regen genuinely fails in-env, use the documented inline fallback for FIX-004 and mark `[BLOCKED BY ENVIRONMENT]`.
9. Validate: focused Bun unit tests for FIX-005 (`upsertDuesFunds.test.ts`) + FIX-006 (`listDuesInvoices.test.ts`) + FIX-004 (RBAC proof) → full dues module suite (`bun test src/handlers/member/duesspecialassessments/`) → full api-ts `bun test` (record vs the current **6187 pass / 1 fail / 4 todo** baseline; the 1 fail is the PRE-EXISTING + UNRELATED `registerEmailJobs`; `getNextBookableTime` is a separate booking clock-boundary-flaky test — do NOT attribute either to this batch) → monorepo typecheck (`bun run --filter '*' typecheck`, expect 5/5) → for FIX-004, if proving via Hurl, the live RBAC contract case (the 3 pre-existing Hurl fails are NOT dues; do not attribute or expect to flip them). `check:sdk-compat` exits 1 by design (frozen baseline) — FIX-004 adds NO operationId (only a middleware extension), so do NOT `--update` it. Save the fix report (APPEND a "Batch B subset — FIX-004 + FIX-005 + FIX-006" section). STOP.

## Remaining-work sequence (the todolist — keep in this order)

**Track A — decision-free `04` passes:**
- A1–A11 (Membership, Elections, Auth/RBAC, Billing, Communications, Documents, Notifications, Person, Marketplace A/B/C/D, Jobs Batch A, Platform-admin Batch B, **Realtime Comms Batch B subset**) — ✅ DONE.
- **A12 Dues Batch B subset (FIX-004 position-gate, FIX-005 fund-splits, FIX-006 invoice self-scope) — THIS PASS.**
- A13 Training Batch E (FIX-014 real E2E proof of P0 credit journey).

**Carry-forward loose ends (small, eng-confirm — slot anytime):**
- **Dues FIX-007 (over-refund cap + eligibility) + FIX-010 (proof-atomicity transaction)** — Batch B but touch the settle/refund seam; run as a dedicated settle-seam pass with membership-status side-effect assertions.
- **Realtime FIX-006** (DM creation UI) — decision-free frontend build; its own comms-UI pass.
- **Jobs Batch B** — handler-org-trust hardening (`createJobPosting` body-org, `searchJobPostings` org-scope default).
- **Auth/RBAC `officerAuthMiddleware` dead-triplet** — decide delete-vs-amend (`/codex`).
- **Notifications stripe-webhook silent-fail** — `handlers/billing/handleStripeWebhook.ts` omits `organizationId` on 5 `createNotification` calls. `[CROSS-MODULE RISK]`.
- **3 pre-existing contract failures** (impersonation / governance position-crud / platformadmin committees authority drift) — addressed in those modules' RBAC-gated passes, NOT decision-free.

**Track B — decision-gated (the bottleneck):**
- B1. P0/P1 product decisions, incl. **dues Q-PD1 (token expiry), Q-PD2 (partial-refund expiry direction), Q-PD5 (PayMongo V1?), Q-PD6 (gateway-refund V1?), Q-PD7/Q-PD8 (first-invoice funnel + Pay-Now CTA)**, realtime PD-1/2/3, plus platform-admin Q1–Q4, elections G2, documents Q1, marketplace G-06/G-13/vendor-identity, person Q-1/Q-4/Q-7. Full agenda in roadmap §13.

**Track C — consolidate + ship (after A + B land):**
- C1. Re-run `07-consolidate-roadmap.md`.
- C2. Milestone Step 6: `--update` the frozen `check:sdk-compat` baseline, then commit/PR the working tree.

## Env state (after realtime-comms Batch B, 2026-06-12)

- Docker up (postgres + mailpit + minio + stripe-mock). DB migrated + seeded. **This pass needs: `bun test` (FIX-005/006); the typespec regen toolchain + an API restart (FIX-004); optionally the API + Hurl for a live RBAC 403 proof. No DB migration, no schema change.**
- Known-good baselines (current): full `bun test` (api-ts) = **6187 pass / 1 fail / 4 todo** (the 1 fail PRE-EXISTING + UNRELATED: `registerEmailJobs`; `getNextBookableTime` is a separate booking clock-boundary-flaky test, also out of scope). Monorepo `tsc` = **0 errors (5/5)**. Admin app = **61 pass / 0 fail**. Full Hurl suite = **152/155 files** (3 pre-existing fails: `impersonation-flow`, `member/governance/position-crud`, `platformadmin-extended-flow`). Dues module suite is green from Batch A (286+ tests).
- `check:sdk-compat` exits 1 **by design** (frozen baseline). FIX-004 adds only a position-middleware extension to existing ops — no operationId change → no SDK surface change. Do NOT `--update` until milestone Step 6.

## Tree / commit rules

- NOTHING committed; working tree dirty (~300+ files across all prior AHA passes incl. realtime Batch B + dues Batch A). PRESERVE it. FORBIDDEN: `git reset --hard`, `checkout .`, `clean -fd`, `restore .`, `rm -rf`. This pass ADDS/edits ONLY: `specs/api/src/association/member/dues.tsp` + the regen'd generated files (FIX-004, expected churn); `handlers/member/duesspecialassessments/upsertDuesFunds.ts` (FIX-005); `handlers/member/duesspecialassessments/listDuesInvoices.ts` (FIX-006); their tests (`dues-mutation-auth.test.ts` and/or a generated-output assertion test for FIX-004; new `upsertDuesFunds.test.ts`; new `listDuesInvoices.test.ts`); and the fix report. (Inline-fallback only if regen broken: the ~10 handler files instead of `dues.tsp`.) No new migration, no schema, no settle-seam files, no `core/*`. Prior-pass dirty files are NOT yours — leave them. Do not commit unless asked.

## Ground rules

- Follow `docs/aha/prompts/00-aha-shared-rules.md` (§2 sequence, §20 fix/TDD rules, §23 stop conditions). Primary guide: `docs/aha/prompts/04-module-or-group-fix-tdd.md`. Execute ONLY the Dues & Payments Batch B decision-free subset (FIX-004 position-gate, FIX-005 fund-split validation, FIX-006 invoice self-scope). Do NOT start FIX-007/010 settle-seam fixes, FIX-007-gateway / FIX-008 / FIX-009 / Batch C, any PD-gated change, or any other module. Save the fix report and stop.

execute systematically
