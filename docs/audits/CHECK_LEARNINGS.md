---
oli-version: "1.0"
based-on:
  - docs/audits/CHECK_SUMMARY.md
  - docs/audits/codebase-map/.map-meta.json
  - .oli/config.json (MISSING)
last-modified: 2026-05-31
last-modified-by: oli-check
---

# Checker Self-Limitations

Rolling backlog of where **/oli-check itself** could not verify or lacks a rule. Distinct from project gaps (those live in `CHECK_SUMMARY.md` matrix + GATE). These rows NEVER affect the GATE.

**How to use:** paste this whole table into the oli-check skill repo issue tracker to harden the skill. Rows marked `◆` are new or recurring this run — prioritize them.

| category | observation [evidenced\|reflective] | evidence_ref | suggested_improvement | first_seen | last_seen | times_seen |
|----------|-------------------------------------|--------------|-----------------------|------------|-----------|------------|
| freshness-trust | Run reported THESIS NOT FULLY IN FORCE because `oli-engine` binary not on PATH and `.oli/config.json` missing — producer fell back to regex. Cannot promote to PASS until either tier-3-live runs or engine installs. ◆ | TRUST STATUS banner — `producer: regex-fallback`, `MAP-FRESHNESS: STALE-OVERLAP` | Ship a built-in trust-state bootstrapper that detects the missing engine + missing config and emits a one-command remediation (`oli-engine install --bootstrap-config`) inside CHECK_SUMMARY §5 instead of leaving the user to figure it out. | 2026-05-31 | 2026-05-31 | 1 |
| engine-field-gap | `loading_state_hygiene` field unavailable → confidence + enforcement consumers degraded; verdict null for 369/369 UI components [evidenced] ◆ | discovery report — `fields_unavailable: loading_state_hygiene` consumed by `oli-check --confidence` Step 4.5 and `oli-check --enforcement` UI hygiene | Backport the v4+ engine `loading_state_hygiene` extractor to the regex-fallback producer (or downgrade these checks to advisory `[fallback-mode]` so they don't silently pass). | 2026-05-31 | 2026-05-31 | 1 |
| engine-field-gap | `interaction_hygiene` field unavailable → enforcement UI-hygiene sub-checks cannot verify [evidenced] ◆ | discovery report — `fields_unavailable: interaction_hygiene` | Same as above — add a regex-fallback extractor or mark the dimension as `audit-only` until field present. | 2026-05-31 | 2026-05-31 | 1 |
| engine-field-gap | `CODE_DATA_MODEL` artifact not produced → traceability cannot walk FK chains; consistency cannot match schema↔spec [evidenced] ◆ | discovery report — `fields_unavailable: CODE_DATA_MODEL` consumed by traceability + consistency | Either generate `CODE_DATA_MODEL.{json,md}` from Drizzle schema files in regex-fallback mode, or have consistency dimension fall back to direct schema-file inspection. | 2026-05-31 | 2026-05-31 | 1 |
| engine-field-gap | `CODE_SPEC_TRACE` artifact not produced → traceability blocked on algorithm 5g (FE field-phantom); empty `api_calls` in CODE_COMPONENT_REGISTRY [evidenced] ◆ | discovery report — `fields_unavailable: CODE_SPEC_TRACE`; traceability finding `TR-DISC-CALLS` | Add an AST or regex pass over component files to populate `api_calls` even in fallback mode; otherwise an entire trace algorithm silently produces zero findings. | 2026-05-31 | 2026-05-31 | 1 |
| low-confidence-heuristic | 504 graph-anchored nodes routed to `unverified` bucket in confidence dim (hygiene verdict null + partial FE↔BE edge resolution) [evidenced] ◆ | CONFIDENCE_REPORT — `unverified=504`; threshold=MEDIUM | Surface `unverified` count beside each per-module score so reviewers see *why* a 9/10 isn't actually a 10 — currently the per-module table looks clean but masks the unverified split. | 2026-05-31 | 2026-05-31 | 1 |
| low-confidence-heuristic | 76 trace cells routed to `unverified` due to map sha lag (49 BR→code + 27 endpoint→handler) [evidenced] ◆ | TRACE_REPORT — `unverified=76` | When map is STALE, traceability should auto-trigger discovery regen before producing the trace verdict, not after — preventing wasted analyzer runs against stale anchors. | 2026-05-31 | 2026-05-31 | 1 |
| low-confidence-heuristic | 4 compliance nodes routed to `unverified` (terminology / auth / phantom / N+1 — all map-sourced "0 violations" claims pending regen) [evidenced] ◆ | COMPLIANCE_REPORT — U-2.4-01..04 | Same auto-regen-before-verdict pattern; or block "0 violations" claims from rolling up to PASS until source signal is fresh. | 2026-05-31 | 2026-05-31 | 1 |
| dimension-coverage-gap | Runtime dim has 5 ✗ uncovered modules (m12, m13, m15, m16, m17, m19) — none received an explicit walker / e2e plan despite specs existing [evidenced] ◆ | RUNTIME_TEST_PLAN — per-module table 3 ✓ / 11 ⊘ / 8 ✗ | Runtime tier-2 plan should emit a stub walker template for EVERY spec'd module, not only those with shipped frontend code — otherwise the per-module table conflates "no UI yet" with "checker had no rule". | 2026-05-31 | 2026-05-31 | 1 |
| dimension-coverage-gap | Enforcement DEGRADED for m20/m21/m22 (no per-module + per-file sub-check artifacts emitted despite code present + specs present) [evidenced] ◆ | ENFORCEMENT_REPORT — m20/m21/m22 DEGRADED | Enforcement orchestrator should auto-fan-out to sub-checks for any module-spec it discovers, not require prior artifacts to exist — currently new modules silently DEGRADE until a follow-up run. | 2026-05-31 | 2026-05-31 | 1 |
| dimension-coverage-gap | Traceability flagged 3 zero-anchor specs (m20/m21/m22) — specs are prose-only, no BR/AC/WF/SM IDs. Checker has no rule to suggest the ID convention to the spec author [evidenced] ◆ | TRACE_REPORT — ZA-01/02/03 | When a spec lacks anchor IDs, traceability should call out a remediation template (e.g. "add BR-NNN headers per /oli-spec-modules") rather than just reporting zero anchors. | 2026-05-31 | 2026-05-31 | 1 |
| toolchain-gated | Cannot run live runtime tier-3 because (a) no `--live` flag, (b) map at v1 (engine pre-v4) so even with --live the executor would route to Tier-3-lite only [evidenced] ◆ | RUNTIME_TEST_PLAN — "Why Tier 3 skipped" section | Make the trust-banner escalation block recommend `--runtime --live` more prominently — currently it's buried in §5 "What's Next" and easily missed. Also: Tier-3-lite vs Tier-3-full distinction should be in the gate verdict block (full = cleared, lite = still WARN-WITH-PROOF). | 2026-05-31 | 2026-05-31 | 1 |
| toolchain-gated | Seed coherence STATIC mode because LIVE replay requires booting full API stack — checker has no built-in `bootApi(testProfile=seed-only)` helper [evidenced] ◆ | SEED_COHERENCE_REPORT — `mode: STATIC` | Add a seed-only API boot helper (in-memory PG / testcontainers) so LIVE-REPLAY mode is achievable in CI without humans wiring it. | 2026-05-31 | 2026-05-31 | 1 |
| freshness-trust | STALE-OVERLAP tier reported because 4 uncommitted seed-file edits overlap api-core module. Skill has no rule to distinguish "user mid-flight" from "stale state" [evidenced] ◆ | discovery report — `MAP-FRESHNESS: STALE-OVERLAP (working tree)` | Add a "working-tree-uncommitted" sub-tier so a user actively editing isn't penalized identically to drift-from-committed state. Suggest auto-rescan-on-stash-+-rescan-+-pop dance. | 2026-05-31 | 2026-05-31 | 1 |
| config | `.oli/config.json` absent → defaults applied silently (confidence_threshold=MEDIUM, no producer override) [evidenced] ◆ | trust banner — `.oli/config.json: MISSING` | When config absent, emit a one-shot wizard prompt (`oli init`) on first /oli-check call, or auto-write a discovered-defaults config so the next run has greppable settings. | 2026-05-31 | 2026-05-31 | 1 |
| new-stack-case | Hono `.route()` chained API registration partially miscounted by regex fallback (116 endpoints vs prior 421 from engine) — fallback is conservative on chains [reflective] ◆ | discovery report — "backend Hono endpoints = 116 (down from 421)" | Add a Hono-specific regex rule for `.route('/x', subRouter)` chains so fallback parity stays within 90% of engine count. | 2026-05-31 | 2026-05-31 | 1 |
| dimension-coverage-gap | UI consistency dim found 3387 findings BUT all are KNOWN under genesis-mode flag — checker has no ratchet to surface "new since last run" deltas separately from baseline [evidenced] ◆ | ENFORCEMENT_REPORT — UI-consistency Phase 1.6 (1 P0 / 301 P1 / 1376 P2 / 1709 P3, all KNOWN) | Enable ratchet on next run; until then UI findings flood the count without actionable signal. Add a `genesis_flag_age_days` field that auto-flips to ratchet mode after N days. | 2026-05-31 | 2026-05-31 | 1 |
| dimension-coverage-gap | Compliance per-module sub-check has no rule for "spec exists + code zero" vs "spec exists + code partial" — both render as ✗ but root-cause is different [reflective] ◆ | COMPLIANCE_REPORT — V-M13-001 (zero) vs V-M16-002 (FE missing only) both listed as P1 | Split per-module verdict into `✗ unimplemented` vs `✗ partial` vs `✗ drift` so triage signal is preserved. | 2026-05-31 | 2026-05-31 | 1 |
| low-confidence-heuristic | Map shows `unverified=0` for components/endpoints in regex-fallback mode — but that's only because the field set is reduced, not because confidence is high [reflective] ◆ | discovery report — `unverified node count (confidence < MEDIUM): 0` | When producer=regex-fallback, the trust banner should add a "false-zero warning" line: unverified=0 in this mode means "no node had confidence to measure" not "every node verified". | 2026-05-31 | 2026-05-31 | 1 |
| low-confidence-heuristic | UI-consistency contrast detector emitted P0 `EU-CONTRAST-text-secondary-bg-white` (1.07:1) on 2 sites that are FALSE POSITIVES: arbitrary-value class `text-[var(--color-text-secondary)]` (#554B60, true 8.2:1 on white) was tokenized as the bare `text-secondary` utility and mis-mapped to `--secondary` (#F0E8EC pink). Compounded by pairing a `data-[state=active]:`-gated `bg-white` with base-state text, ignoring the active variant also recolors text. No bare `text-secondary` exists in the repo. [evidenced] ◆ | Phase B fix run — grep(text-secondary)=0 bare matches; institutional-membership-table.tsx:98 + member-table.tsx:203; contrast(#554B60,#fff)=8.20 | (1) Resolve Tailwind arbitrary-value classes `text-[var(--x)]` to their referenced custom property, not the bare-utility namesake. (2) Make the contrast pair-resolver state-variant aware: a `data-[state=*]:`/`hover:`/`focus:` prefixed bg must only pair with text colors under the SAME variant prefix. (3) Honor `// oli-ui: exempt(reason=...)` for EU-CONTRAST findings, not only className-override findings. | 2026-05-31 | 2026-05-31 | 1 |

**Recurring (`times_seen > 1`):** none — this is the first run that emits CHECK_LEARNINGS.md per the new always-on aggregator rule. Next run will bump `times_seen` for any of these still observed.
