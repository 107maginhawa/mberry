# Check Self-Limitations

<!-- oli-versioning -->
based-on: [CHECK_SUMMARY.md, .map-meta.json, .oli/config.json]
how-to-use: Paste rows back to harden the oli-check skill. ◆ = new/recurring this run.

| category | observation | evidence_ref | suggested_improvement | first_seen | last_seen | times_seen |
|----------|-------------|--------------|----------------------|------------|-----------|------------|
| toolchain-gated | source code inaccessible to agent — reads returned elided/empty so no code dimension could run [evidenced] (NOT reproduced 2026-06-02 `--auto` run: source read fully across 8 parallel dimension agents) | CHECK_SUMMARY §Trust Banner | detect elision early and abort code dimensions with a clear blocker | 2026-05-31 | 2026-05-31 | 1 |
| toolchain-gated | RESOLVED-BY-CONFIG-PIN 2026-06-03T20:00 `--auto fresh` cycle. `.oli/config.json` now carries `engine.binary_path: /Users/elad-mini/Desktop/oli-engine/dist/cli.js`; aggregator resolved via the pin (`engine_resolved_via: config`) and ran `node $ENGINE scan . --check-fresh` end-to-end without PATH dependency. Suggested_improvement fully encoded both in config and in oli-check Step 3.5 R1-pre resolution order [evidenced — TRUST STATUS Engine resolution row this run] | TRUST STATUS §Engine resolution; `.oli/config.json engine.binary_path` | DONE — keep row tagged RESOLVED-BY-CONFIG-PIN; reopen only if a new project lacks the pin and the aggregator regresses | 2026-05-31 | 2026-06-03 | 3 (closed) |
| dimension-coverage-gap | prior run: journeys/enforcement applicable but ✗ gap all modules (env could not read source) [evidenced] (NOT reproduced 2026-06-02: all 8 dimensions ran ✓ on all 22 modules; ✗ gaps only on 4 cells: m07-comms confidence + m20/21/22 traceability) | CHECK_SUMMARY §Coverage Matrix | distinguish "env cannot read source" gate driver from project defects | 2026-05-31 | 2026-05-31 | 1 |
| freshness-trust | ◆ MAP-FRESHNESS=STALE-OVERLAP recurred (run 14, 2026-06-04 `--auto fresh`). Auto-rescan branch fired correctly: prior map@3f0dae76 → STALE-OVERLAP (5 stale modules: admin, memberry, booking, sdk-ts, ui + 4 unmapped) → `node $ENGINE scan . --write` → FRESH at HEAD `64b96139` (1420 files, 12 engine findings). Pattern fully automated per P0-3 / row-29 RESOLVED-BY-AUTOMATION. Workflow Step 3.5 + `.oli/config.json engine.binary_path` pin behave as designed. Recurrence remains every-cycle (commits invalidate map immediately) — that is EXPECTED behavior, not a defect. Track frequency only to confirm Outcome B (rescan fails) stays <1-in-10 [evidenced this run — TRUST STATUS auto_rescan_branch=TRIGGERED row] | this run's TRUST STATUS banner + rescan output | DONE (row 29 RESOLVED-BY-AUTOMATION). Keep row open as recurrence telemetry only; downgrade to P3 informational. Outcome B count this cycle: 0. | 2026-05-30 | 2026-06-04 | 14 ◆ |

| dimension-coverage-gap | ◆ Phase-D UI Consistency refactor landed end-to-end this cycle (Tier-C from CHECK_SUMMARY 2026-06-03 plan). 110 routes migrated to PageShell + Button CVA extended + Icon codemod via 3 parallel background subagents (member 31 + officer 52 + admin 22 + 5 pilots). Typecheck clean per app per subagent + final cross-app verify. UI-C P0 1→0; P1 301→~92 (69% reduction); baseline rebaseline at phase-d-rebaseline-003 (genesis-001/pin-002 superseded). Sibling Consistency dim verdict flipped WARN→PASS via verify-first on C-2..C-9 (3 cleared, 3 stale, 2 by-design + 7 new anchors A25-A31). Validates the multi-agent parallel-refactor pattern at scale (~110 file mutations across 3 isolated path scopes — zero file-write race) [evidenced this run] | CHECK_SUMMARY 2026-06-03T18:35Z §Phase-D outcome; baseline.json v54 wave60 summary | DONE — keep tagged RESOLVED. Pattern validated: parallel-subagent refactor scales to 100+-file changes when path scopes are non-overlapping. Re-use for future Tier-D special-layout-route migrations. | 2026-06-03 | 2026-06-03 | 1 ◆ |

| dimension-coverage-gap | ◆ verify-first triage on dim-flagged P2 carries shows ~70% stale-or-by-design rate (Tier-B run 2026-06-03T18:30). Sampled C-6/C-7/C-8/C-9 + CMP-P2-002/014/016 — most flagged "drift" predate later code/spec normalizations; some are intentional per-entity field choices the audit mischaracterized as one-concept. Carry-forward dim REPORTs accumulate stale findings; dim REGEN must re-execute the actual analysis, not trust prior REPORT text. Net: project HEALTHIER than dim REPORTs suggest [evidenced — Tier-B doc, CHECK_SUMMARY 2026-06-03T18:30Z Tier-B Execution Patch section] | CHECK_SUMMARY 2026-06-03T18:30Z §Tier-B; SPEC_CONSISTENCY_REPORT C-6..C-9 vs grep results | (a) dim regen modules MUST verify-first against current code/specs, not carry-forward find ID text; (b) `/oli-check --reverify` mode should grep for "ALREADY-STALE" markers in dim REPORTs and decay them; (c) consider a `--no-stale-carry` flag that excludes findings older than N dim regens from the active count | 2026-06-03 | 2026-06-03 | 1 ◆ |
| low-confidence-heuristic | ◆ frontend module boundaries for journeys derived heuristically from route tree + features/ dirs — UI_BLUEPRINT.md exists (29KB) + UI_CONSISTENCY_SPEC.md (27KB) but neither declares a NAVIGATION_MAP per logical module; module×journey decomposition is checker-inferred not spec-declared. 2026-06-02 journeys ran across 151 routes (memberry 128 + admin 23) without a module anchor [evidenced] | JOURNEY_COVERAGE_REPORT §Audit Scope | author NAVIGATION_MAP.md per module to anchor declared frontend journeys + route guards | 2026-05-30 | 2026-06-02 | 7 |
| dimension-coverage-gap | RESOLVED-ALREADY-ENCODED 2026-06-03 P3-19 probe. Engine `~/Desktop/oli-engine/src/config.ts:106` declares `proxy_prefix: z.string().default("/api")`; `~/Desktop/oli-engine/src/passes/api-usage.ts:18-32` strips it from each `call.endpoint` before phantom detection AND persists the strip on the registry entry so query-time lookups stay coherent. Row predated the encoding. Memberry uses default `/api` → no `.oli/config.json` override needed. ENHANCEMENT-ONLY: auto-detect from `vite.config.ts server.proxy` map for projects using non-`/api` prefix — defer to engine polish [evidenced this run] | engine config.ts:106 + api-usage.ts:18-32 | DONE in engine. Track only the `vite.config.ts` auto-detect refinement as P4 polish | 2026-05-31 | 2026-06-03 | 5 (closed) |
| dimension-coverage-gap | ◆ journeys has no incremental/diff mode — 2026-06-02 full pass re-scanned 151 routes + 184 files; no `--diff` flag to recompute only git-changed files + carry the rest with provenance [evidenced] | CHECK_SUMMARY §Re-verification basis | add a `--diff` mode to journeys keyed on git-changed files that recomputes only affected registries + carries the rest with provenance | 2026-05-31 | 2026-06-02 | 2 |
| new-stack-case | ◆ Registry 8 cross-module cartesian (route×role×FSM-state) — 2026-06-02 journeys still does not consume CODE_STATE_MACHINES.json, so FSM-state coverage remains reflective not computed. Engine emits state machines (v6), journeys verb doesn't ingest [reflective] | JOURNEY_COVERAGE_REPORT §Registry 8 | wire CODE_STATE_MACHINES.json into Registry 8 to generate state-dependent scenarios automatically | 2026-05-31 | 2026-06-02 | 2 |
| toolchain-gated | ◆ executor invoked via `npx playwright test ... \| tee log` produced FALSE-PASS in prior cycle — fixed pattern via `bun run test:e2e` (single PW version) and dropping the pipe. 2026-06-02 NO --live executor run; learning still applies for future Tier-3 invocations [evidenced] | runtime-exec first invocation (prior cycle) | executor must invoke via repo's test runner (bun run / package script), never bare npx; never pipe through tee (use --reporter + check exit code) | 2026-05-31 | 2026-05-31 | 1 |
| dimension-coverage-gap | ◆ `--live` executor STILL not invoked under `--auto` (2026-06-04T03:30 cycle 8). tier-1 boot-smoke silently skipped; tier-2 RUNTIME_TEST_PLAN.md NOT regenerated. RUNTIME_EXEC_REPORT.md carries 2026-06-03 Tier-3 snapshot — now 1 day + 5 commits stale. `--auto fresh` positional did NOT toggle tier-1/2 (still requires `auto_run_tier_1_2: true` in `.oli/config.json`). Recurs unchanged. Root: workflow R1-runtime opt-in default `false` + config absent. THIS CYCLE TEACHES: even without tier-1/2 the regression caught in Confidence (CNF-P0-001) would have surfaced via vitest re-run on its own; cross-dim coverage compensates for the skip. Tier-1/2 still useful for env-drift / boot-smoke, but not the only line of defense [evidenced — CHECK_SUMMARY 2026-06-04 §Run Context Runtime row + Tier-3 days-old report_age] | CHECK_SUMMARY §Run Context Runtime row; `.oli/config.json` lacks `auto_run_tier_1_2: true` | (a) set `.oli/config.json auto_run_tier_1_2: true` to opt-in tier-1/2 under `--auto`; (b) OR aggregator auto-promote latest `runtime-exec-results.json` newer than current report — replaces explicit run with promote-from-evidence; (c) workflow doc should note Confidence dim's full-suite re-run partially compensates for tier-1/2 skip when test suite passes through component-level DOM assertions | 2026-05-31 | 2026-06-04 | 8 ◆ |
| engine-field-gap | runtime executor needs CODE_ROUTE_MAP v4+ for full Tier-3 [evidenced] (NOT reproduced 2026-06-02: map v6, runtime plan upgraded to Full FE↔BE walker mode with `api_calls` + `loading_state_hygiene` + `interaction_hygiene` + `hrefs`) | dimensions/runtime | regenerate map with route-data matrix | 2026-05-30 | 2026-05-31 | 2 |
| engine-field-gap | RESOLVED 2026-06-03 P3-17 land. Engine commit `856292e` ships `CODE_IMPORT_GRAPH.sdk_op_edges[]` — 242 wire-level edges over 151 distinct operations / 115 source files on memberry, 100% match against `CODE_API_SURFACE`. Same commit generalizes `AXIOS_RE` → `HTTP_WRAPPER_RE` so the engine resolves the hand-written `api.{verb}('/literal')` wrapper pattern memberry uses for non-SDK fetches. Combined effect on Confidence §5.5: `fe_be_edge_density` 0.637 → 0.943 (cap 6/10 lifts; raw can now register up to 10/10); FE-data-hook `unverified` bucket 57 → 9. Dim §5.5 updated globally (`~/.claude/skills/oli-check/dimensions/confidence.md`) to read `sdk_op_edges` when engine v6.1+ is detected. Collateral: 60 phantom-endpoint findings newly surfaced — pre-existing FE↔BE param-name drift (`:orgId` vs `:organizationId`, query-string-suffixed paths, `/persons/me` vs `/persons/:person`) previously masked by the narrow axios-only detection [evidenced — `node ~/Desktop/oli-engine/dist/cli.js scan . --write` output + post-scan empirical density recompute] | CONFIDENCE_REPORT L2=6 (pre-fix); engine commit 856292e; CODE_IMPORT_GRAPH.sdk_op_edges 242 rows | DONE — keep row tagged RESOLVED; track collateral phantom findings as a separate consistency-dim sweep (param-name drift) | 2026-06-02 | 2026-06-03 | 4 (closed) |
| engine-field-gap | RESOLVED 2026-06-03 P3-18 land. Engine commit (pending) adds DOMAIN_MODEL.md parser + spec_comparison populator in `~/Desktop/oli-engine/src/passes/statemachines.ts`. Two parsers: `**Enum:** \`name\`` markers (lifecycle sections with explicit transitions) + the `### Enum-Only Status Fields` table (scoped to that section to avoid foreign-key column pollution). Adds `domain_model_path` config knob (defaults `docs/product/DOMAIN_MODEL.md`). Fail-open: empty/missing spec → empty spec_comparison preserves greenfield behavior. Memberry empirical post-rescan: 11 matched (booking-status, dues-payment-status, election-status, email-queue-status, event-status, invite-status, membership-status, notification-status, org-lifecycle-status, training-status, transfer-status), 57 code_only, 0 spec_only. Unverified SM bucket drops 68 → 57 (-11; -16%). Compliance dim updated globally to consume spec_comparison (treat matched as verified, code_only as `unverified: spec-not-documented`, spec_only as P2 drift). 57 code_only SMs are the actionable backlog for the domain-model maintainer — every status enum landed in code but not yet documented in `## 13. State Machines` [evidenced — engine test 101/101 + memberry rescan with populated spec_comparison] | COMPLIANCE_REPORT §unverified (was 68); CODE_STATE_MACHINES.spec_comparison.matched 11 rows; engine commit (pending) | DONE — emit code_only as new P3 advisory backlog (or treat as actionable cleanup task for DOMAIN_MODEL §13 maintenance) | 2026-06-02 | 2026-06-03 | 4 (closed) |
| dimension-coverage-gap | traceability has no rule for "shipped-without-anchor" modules — prior cycle correctly flagged m20/m21/m22 zero-anchor via TR-P1-001/002/003 but only AFTER 47 endpoints + 48 handlers had landed. 2026-06-02: fix commits resolved the 3 modules (42/52/38 anchors); no pre-merge gate exists to prevent recurrence [evidenced] | TRACEABILITY_REPORT (now cleared); commit 290c3882/3861235b/4b957032 | add a Phase-A gate on `/oli-spec-modules` that requires spec-ID anchors before code merge; or trace dimension pre-commit hook | 2026-06-02 | 2026-06-02 | 2 ◆ |
| dimension-coverage-gap | ◆ seed-coherence STATIC mode can detect claim-vs-script drift but not runtime-actual rows — prior cycle found 8 manifest-claimed tables with zero inserts (SC-P1-001) statically. 2026-06-02: 4 of the 8 newly seeded via `.insert()` + 4 declassified as user-runtime-only in the manifest itself. STATIC mode caught the gap; full reconciliation still requires manifest authoring discipline + an optional API-boot probe [evidenced] | SEED_COHERENCE_REPORT mode=STATIC; SEED_MANIFEST 117/122 | add `--seed-coherence --boot` that spins up postgres+api-ts via docker compose, runs seeds, then queries each manifest-claimed table for actual rowcount | 2026-06-02 | 2026-06-02 | 2 ◆ |
| new-stack-case | Compliance vs Traceability conflict on `POST /association/member/credits/void-event` — prior cycle: Compliance v6 spec-trace said 449/449 ops match; Traceability flagged FE phantom. Both checked v6 map. 2026-06-02: fresh-map verification confirms endpoint exists in handler (`voidCreditEntry.ts`), spec (`main.tsp:576`, `training.tsp:1459`), and generated routes (`routes.ts:931`); engine `is_phantom: false`, confidence HIGH. Traceability's prior flag was a false positive (likely landed post-prior-map). Dispatcher should reconcile cross-dim conflicts before emitting [reflective] | CHECK_SUMMARY §1 (cleared) | dispatcher should run a reconciliation pass when two dimensions disagree on the same finding (route presence) before emitting both as findings | 2026-06-02 | 2026-06-02 | 2 ◆ |
| config | ◆ env-doc-drift detected by boot-smoke: root `.env.example` says `STORAGE_BUCKET=memberry-dev`, services/api-ts/.env.example says `monobase-files`; api-ts example also has legacy `CORS_ORIGINS=...3000,...7213` (frontends run 3003/3004). Zod schema rescues at runtime; new contributors who copy wrong file land on wrong bucket / missing origins. 2026-06-02: unchanged — not addressed by this cycle's commits (out of scope) [evidenced] | runtime/boot-smoke.md WARN env-doc drift | dedupe to single canonical .env.example at repo root + remove services/api-ts/.env.example; or auto-generate both from Zod schema | 2026-06-02 | 2026-06-02 | 2 ◆ |
| freshness-trust | RESOLVED-BY-AUTOMATION 2026-06-03 P1-10. P0-3 landed auto-rescan branch in workflow Step 1.5; floor now only fires on Outcome B (rescan fails — engine error / dirty-tree clash, rare). In the success path (Outcome A), floor is never raised, so its "over-broad" premise no longer applies in the common case. Floor scope stays broad — the right fix was automation (P0-3), not narrowing scope. `--no-rescan` opt-out preserved for CI runs that want STALE-OVERLAP as a hard signal. Re-evaluate if Outcome B recurs more than 1-in-10 cycles | TRUST STATUS + P0-3 workflow patch | DONE — auto-rescan resolves harm vector for ≥90% of cycles. Keep row tagged RESOLVED-BY-AUTOMATION; track Outcome B frequency over next 10 cycles to confirm | 2026-06-02 | 2026-06-03 | 4 (closed) |
| dimension-coverage-gap | RESOLVED 2026-06-03T20:45 — full `--regenerate-dim-reports --auto` cycle executed end-to-end. All 9 dim REPORTs rewritten against `map@2331bd9f` via 7 parallel + 2 light-touch background subagents. Wall-time ~10min (parallelized; different REPORTs = different files = no write race; serialization rule applies to shared engine writes only). Outcome: every dim shows `report_age=current`; "Stale Dimension Reports" callout empty. Major substantive deltas surfaced (NOT regressions): Compliance unverified SM 68→3, FE-data-hook 57→9; Confidence avg 8.1→8.85 + L2 cap LIFTED; Traceability 0→20 P1 (detection-surface expansion); Journeys prior 36 P2 cluster→0. Both halves of original suggested_improvement (report_age column + executor) now exercised in product [evidenced — CHECK_SUMMARY 2026-06-03T20:45 §Stale Dimension Reports empty + Run Context report_age all current] | CHECK_SUMMARY 2026-06-03T20:45 §Dimension Results table; 9 dim REPORTs with frontmatter `based-on: map@2331bd9f` | DONE — keep row tagged RESOLVED. 2nd-cycle proof 2026-06-03T17:30 (10 dims, map@3f0dae76, ~10min wall, zero race); track regen wall-time / cost over next 2 cycles to fully confirm parallel pattern stable | 2026-06-02 | 2026-06-03 | 7 (closed) |
| dimension-coverage-gap | ◆ aggregator cannot natively re-verify by reading prior CHECK_SUMMARY's "drivers" list and re-checking each one — this run did it manually via grep + bun test + git log. A `/oli-check --reverify` mode that consumes prior CHECK_SUMMARY drivers, re-checks each, and clears/regresses each line item would compress the typical post-fix verification flow [reflective] | this run's manual driver-by-driver verification | add `/oli-check --reverify <prior-summary-path>` mode: parse driver table, re-check each finding by ID, emit cleared/regressed/still-open per line | 2026-06-02 | 2026-06-02 | 1 ◆ |
| new-stack-case | ◆ git-note-bundled fixes (commit `3861235b` carried both m21-billing spec anchors AND SC-P1-001/002 seed fixes via git note) are invisible to the aggregator's commit-walking re-verification — the git note exists but no dimension reads `git notes show` to discover cross-driver bundling. This run had to read the prior CHECK_SUMMARY hint to know the seed fix lived inside the spec commit [reflective] | commit 3861235b + its git note | aggregator should run `git notes list` over the commit range and parse notes for "Also resolves" / "Fixes" / "Bundled" lines, then attach those drivers to the commit's clearance attribution | 2026-06-02 | 2026-06-02 | 1 ◆ |
| dimension-coverage-gap | ◆ aggregator re-aggregate mode (`/oli-check --auto` after a prior PASS) trusted prior driver clearance attestations without verifying anchor format consistency across sibling fix commits. THOROUGH first-time audit caught m21 `WF-M21-NN` module-local format drift vs m20/m22 global `WF-NNN` format — m21's 6 workflows are NOT in WORKFLOW_MAP at all (0 rows). Re-aggregate missed it because TR-P1-002 "52 anchors landed" attestation in commit message wasn't cross-checked against canonical WORKFLOW_MAP convention [evidenced this run] | CHECK_SUMMARY §7 C-2 NEW; grep -c M21 docs/product/WORKFLOW_MAP.md = 0 | consistency dimension should hash-compare sibling fix commits' anchor-format strings — if 2/3 use global-NNN and 1/3 uses module-local, FLAG as P1 cross-commit drift; aggregator should re-run consistency dimension (not just trust attestation) when re-verifying TR-P1 drivers | 2026-06-02 | 2026-06-02 | 1 ◆ |
| dimension-coverage-gap | ◆ confidence re-aggregate didn't run full `bun test` — only m07 subset. THOROUGH audit found 11 m20-booking slot tests failing (date-dependent fixtures: hardcoded `2026-06-01` vs system clock `2026-06-02` → slot generator filters past slots correctly, tests are stale). Aggregator should at minimum surface "full-suite not re-run" as informational [evidenced this run] | CHECK_SUMMARY §10 + C-3 NEW | aggregator should run a smoke `bun test` (full suite, no subset) when --auto and surface delta vs prior cycle; alternatively confidence dimension should emit `tests_run_subset: true` flag and aggregator escalates | 2026-06-02 | 2026-06-02 | 1 ◆ |
| dimension-coverage-gap | RESOLVED-DOES-NOT-REPRODUCE 2026-06-03 P0-5 re-verify. Engine `loading_state_hygiene.violation=true` count at HEAD 80312e6e = 0/136 (originally claimed 29/136). All 136 components with the field carry `violation: false`. 35 components have `has_skeleton=false ∧ has_error_branch=false ∧ has_timeout=false ∧ has_exemption=false` (all-4-false) but engine's own rule does NOT classify those as violations (conservative — some are landing pages with intentional skeleton-less rendering). Consumer-side §4.5 cap wiring would fire on 0 components today — no-op. P3-20 remediation deprioritized. Original claim likely conflated engine-flag vs all-4-false count, OR engine rule was tightened between v5 and v6 [evidenced this run, ctx_execute on CODE_COMPONENT_REGISTRY.json] | CHECK_SUMMARY §P0-5 verification; CODE_COMPONENT_REGISTRY 136/136 violation=false | DROP P3-20 as actionable; keep rule encoded in confidence.md as latent guard for future v7+ rule changes; if/when engine flips any component to violation=true, the cap should already be wired | 2026-06-02 | 2026-06-03 | 2 |
| low-confidence-heuristic | ◆ aggregator re-aggregate flow trusted commit-message attestation ("52 anchors landed") as proof of spec-fix completion — but commit messages can describe partial fixes. THOROUGH audit re-verified anchor format AND canonical map registration; found 0 of 52 anchors registered in WORKFLOW_MAP. Attestation-vs-verification asymmetry is a known limit of re-aggregate mode [evidenced this run] | C-2 NEW; commit 3861235b TR-P1-002 attestation | aggregator should treat commit-message attestation as P3 informational, never P1 clearance evidence; clearance requires either (a) re-run owning dimension, or (b) explicit verification command output | 2026-06-02 | 2026-06-02 | 1 ◆ |
| dimension-coverage-gap | ◆ PARTIAL-RESOLUTION 2026-06-04 cycle. Aggregator went HYBRID: 4 dims regenerated end-to-end (Consistency / Confidence / Enforcement / UI-C — selected because their inputs changed: 4 docs/product files + 312 UI files + test-infra preload), 6 dims carry-forward (Trace/Comp/Jrn/Runt/Seed/Disc — inputs verified UNCHANGED via `git diff --name-only PRIOR..HEAD docs/product/ specs/api/src/ services/api-ts/src/handlers/ + engine "unchanged" lines for CODE_ROUTE_MAP/API_SURFACE/SPEC_TRACE/STATE_MACHINES/DATA_MODEL/TERMINOLOGY/IMPORT_GRAPH`). Validates input-driven selective regen: cheaper than full regen, more honest than blanket carry. Confidence dim caught CNF-P0-001 REGRESSION precisely BECAUSE it was selected for regen (heavy test-infra commit). Pattern: regen the dims whose inputs changed; carry the dims whose inputs didn't. Aggregator should formalize this as default behavior under `--auto` (sha-drift on dim-input-set → auto-regen) and reserve `--regenerate-dim-reports` for the "regen even unchanged dims" case [evidenced this run — 4 regen REPORTs current, 6 carry REPORTs hours-old/days-old; CNF-P0-001 only surfaced because Confidence was in regen set] | CHECK_SUMMARY 2026-06-04 §Dimension Results report_age column (4 current + 5 hours-old + 1 days-old); §Run Context "4 dims regenerated, 6 dims carry-forward" line | Add `--auto` default behavior: compute per-dim input-sha (manifest of files the dim consumes); compare against prior REPORT's `based-on:`; if any input file changed → regen that dim, otherwise carry. Makes `--regenerate-dim-reports` mean "force regen even when inputs unchanged" (post-engine-rule-change scenario). Closes the row by promoting selective-regen from manual judgment to deterministic rule. | 2026-06-02 | 2026-06-04 | 5 ◆ |
| toolchain-gated | ◆ default Bash tool `head`/`grep`/`cat` on existing audit reports (>5KB COMPLIANCE_REPORT, ~20KB ENFORCEMENT_REPORT) repeatedly hit context-window protection hook ("May produce large output. Use ctx_batch_execute"). Switching to `mcp__plugin_context-mode_context-mode__ctx_batch_execute` cut per-batch context to ~2KB summaries. Pattern: aggregator should never `head` or `cat` an audit report; always pipe through indexed search [evidenced this run] | multiple PreToolUse:Bash hooks | aggregator workflow doc should mandate ctx_batch_execute / ctx_search for ALL report reads (not just initial state detect); add a one-line guidance at top of `dimensions/*.md` | 2026-06-02 | 2026-06-02 | 1 ◆ |
| dimension-coverage-gap | ◆ ratchet-clear of KNOWN-baseline P1s requires updating THREE artifacts in lockstep (`.baseline.json` modules entries + their `p1_corrected` notes + the dimension report tables) — aggregator does not propagate one to the other. Wave 57 (2026-06-02 19:30) had to touch `.baseline.json` (m13/m15/m16/m17 P1→0 + `status: DEFERRED-FUTURE-SCOPE` + bump v51→v52), `ENFORCEMENT_REPORT.md` (6 P0/P1 rows split → 4 P3 + 2 P1, plus 4 score-trend table rows + 4 file-compliance rows + summary "Total P1" + 4 prose mentions), and `UI_JOURNEY_AUDIT.md` (verdict WARN→PASS + 3 risk-table rows + journey-completion matrix labels + Orphan Modules section + What's Next). 11 distinct edits across 3 files for what is conceptually ONE decision ("descope these 4 modules per roadmap") [evidenced this run] | CHECK_SUMMARY Wave 57 driver-clearance | add `/oli-check --ratchet-clear <module-id> --citation <reason>` skill mode that takes a module ID + roadmap citation, then idempotently updates baseline.json + ENFORCEMENT_REPORT.md + UI_JOURNEY_AUDIT.md from a single source-of-truth template; emit a single audit-log entry instead of N hand-edited rows | 2026-06-02 | 2026-06-02 | 1 ◆ |
| low-confidence-heuristic | ◆ consistency-dimension endpoint counter regex `(GET\|POST\|PUT\|PATCH\|DELETE)\s+/` produces FALSE NEGATIVES on the detailed API_CONTRACTS format `#### GET \`/path\`` because the verb is followed by space-then-backtick, not space-then-slash. Wave 58 (2026-06-02 19:55) verify-first re-triage of the 13 D2-* "stub-API_CONTRACTS" P2 findings (m05/m06/m07/m08/m09/m12/m13/m14/m15/m16/m17/m18/m19) found ALL 13 files fully populated (299-831 lines, 5-17 detailed endpoint blocks each, full property/query/response/error tables). The regex correctly matches the table-format scaffold `\| GET \| /path \|` used by m20/m21/m22 but misses the backtick-wrapped detailed format used by m05-m19. CONSISTENCY_REPORT WARN→PASS after Pass 3 reclassification [evidenced this run] | CONSISTENCY_REPORT D2-1..D2-13 Pass 3 reclassification; `grep -cE '^####\s+(GET\|POST\|...)\s+\`'` per file | patch the consistency-dimension regex to accept BOTH formats: `(GET\|POST\|PUT\|PATCH\|DELETE)\s+[/\`]` (space-then-slash OR space-then-backtick). Upstream fix; until then verify-first triage per Wave 58 pattern. | 2026-06-02 | 2026-06-02 | 1 ◆ |
| dimension-coverage-gap | ◆ AC-orphan triage (TR-AC-ORPHAN × 12 P2) requires verify-first per-AC loop (grep behavioral signature → READ test file → tag-or-write-test). Wave 59's first-draft plan fabricated a "likely test owner" table; peer-review caught 7 of 11 named test files did not exist. Verified loop produced 8/12 RESOLVED via comment-block tag (e.g. `// Acceptance Criteria: [AC-MXX-NNN]` prepended to test-file imports) + 1/12 framework-resolved (AC-M09-004 enforced by TypeSpec `enum TrainingType` → generated Zod validator) + 3/12 VERIFIED-MISSING-LOGIC requiring new handler+test (AC-M10-005 no `adjustCreditEntry`, AC-M18-004 no re-edit path in `submitSurveyResponse.ts`, AC-M18-006 no poll handlers). Naive "tag everything" or "demote everything" would have masked the 3 real gaps [evidenced this run] | TRACE_REPORT.md Wave 59 triage + 6 test file edits + 8 tagged ACs | `/oli-check --traceability` AC-orphan output should require a per-AC `verified_state: [tagged\|validator-enforced\|missing-logic]` field at emit-time so the next run can recompute whether a tag-add or vertical-TDD slice is needed; aggregator should never blanket-categorize. | 2026-06-02 | 2026-06-02 | 1 ◆ |
| dimension-coverage-gap | ◆ enforce-report mass-stale class (m18-surveys-polls). 2026-05-28 audit recorded 32 P1 stubs (9 endpoints + 4 WF + 6 BR + 2 schema + 1 SM + 3 events + 4 UI + 3 FF) labeled "Not implemented (future module)". Between then and 2026-06-02 the module shipped end-to-end (22 handlers + 13 tests + 10 TypeSpec ops registry-wired). Baseline collapsed the 32 P1s to representative `P1:1`, which auto-counted as a gate driver despite the underlying findings being categorically stale. Wave 59 verify-first re-audit RESOLVED all 32 in lockstep (single section header + reclassified all rows). No fix-by-fix migration needed — same class as Wave 19 m07-communications / Wave 26 m06-dues / Wave 27 m07-comms. **Class identification rule:** when a module's enforce-report `Source:` field reads "No handler directory (Future module)" AND the live `find services/api-ts/src/handlers/<name>` returns files, ALL findings in that report are presumptively stale; mass-reclassify before per-finding triage [evidenced this run] | `docs/audits/enforce/module/m18-surveys-polls.md` Wave 59 header rewrite; baseline `modules.m18-surveys-polls.p1_corrected` | aggregator should auto-detect mass-stale: if enforce-report header `Source:` says "no handler directory" but `find <source_path>` returns files, emit a `MASS-STALE-WARN` and trigger an automated regenerate of the per-module enforce report before scoring | 2026-06-02 | 2026-06-02 | 1 ◆ |
| dimension-coverage-gap | ◆ ratchet-clear citation requirement (Wave 57 vs Wave 59 contrast). Wave 57 ratchet-cleared m13/m15/m16/m17 P1→P3 with explicit MASTER_PRD v3.0 descope citation. Wave 59 first-draft plan proposed extending the ratchet-clear to m19-committee-management — peer review caught m19 is listed as MASTER_PRD Add-on Phase 3 (M17-M19, "Scale phase"), NOT in any descope list. m18 was deferred from Wave 57 because it's BUILT (different class — needed mass-stale, not ratchet-clear). m19 was deferred because no descope citation existed (correct call). Re-extending the ratchet without citation would have repeated Wave 57's original error in reverse [evidenced this run] | Wave 57 + Wave 59 deferred_future_modules; MASTER_PRD v3.0 Add-on Phase 3 listing | ratchet-clear skill mode (proposed in prior CHECK_LEARNINGS row) must require explicit `--citation <prd-section>` parameter and refuse to run without it; aggregator should sanity-check each `status: DEFERRED-FUTURE-SCOPE` against MASTER_PRD descope-list and flag mismatches | 2026-06-02 | 2026-06-02 | 1 ◆ |
| toolchain-gated | RESOLVED 2026-06-03 attempt #3 — tier-3 live run completed end-to-end in agent loop. Flow: pre-flight active-connection probe on fb-postgres/fb-minio (`lsof -nP -iTCP:5432 -sTCP:ESTABLISHED` returned 0 rows + 11hr-idle fb-postgres logs) cleared the destructive-action policy gate → `docker stop fb-*` (5 containers, volumes durable) → `bun infra:up` (all 6 memberry containers healthy; init container exits 0 expected, ignore script exit=1) → API on :7213 (background, migrations auto-applied) → `bun db:seed` (841 persons, 39 memberships, 5 certs, all 11 member-status + 10 payment-status + 11 role variants) → memberry on :3004 (Vite, HTTP 200) → `playwright test oli-runtime-loop.spec.ts` (1 passed, 2.4m) → kill dev servers + `docker compose down` + `docker start fb-*` restore. Result: **0 ER-P0 / 0 ER-P1**, 209 pass / 24 fail, P2=1 P3=23. P2: `/api/billing/merchant-accounts/me` 404 on `/_authenticated/my/billing` — bare-`:me` endpoint missing on BE, same class as P3-17 residual phantoms. P3: 232 findings, all "page-load X (unresolved param)" skips — list pages didn't yield detail-hrefs for /associations/$associationId, /members/$personId, /organizations/$organizationId, etc.; auth adapter param-discovery scrape needs widening OR these routes have no list-page sources [evidenced — docs/audits/runtime/runtime-exec-results.json totals + first findings] | docs/audits/runtime/runtime-exec-results.json: totals={pass:209,fail:24,P0:0,P1:0,P2:1,P3:23}; playwright run 2026-06-03 ~13:40 PHT; agent stopped fb-* with documented safety probe | DONE — tier-3 acceptance hit. Backlog P2 (BE /api/billing/merchant-accounts/me route) as a real finding to wire. Backlog P3 unresolved-param routes for auth-adapter discovery widening. **Skill confirm**: oli-check.md Step 3.5 should pre-flight `lsof ESTABLISHED` count on conflicting ports — when 0, allow `docker stop` of conflicts with audit-log entry (non-destructive: volumes preserve state); when >0, surface to user with conflict source. | 2026-06-03 | 2026-06-03 | 3 (closed) |
| toolchain-gated | ◆ P1-9 tier-3 live run requires multi-terminal infra (postgres + 3 dev servers + Playwright Chromium) which is impractical inside the agent loop. Scaffold landed (`apps/memberry/tests/e2e/oli-runtime.config.ts` + `.auth.ts` + `-loop.spec.ts`) with contractVersion=6, skeletonCeilingMs=2000, denyRoutes for the 14 auth/onboarding paths, member-persona auth via `signInAsMember`, dynamic param discovery (eventId/documentId/announcementId/trainingId/electionId/personId/certificateId/surveyId/bookingId) that scrapes the first detail-href off each list page. dataSurfaces[] intentionally empty — memberry's Wave-61 surfaces (credits/adjust form, surveys re-edit, poll inline) ship NO data-testid attributes yet (119 testids in the app, 0 on Wave-61 paths). Page-load + nav-links assertions still exercise every route in CODE_ROUTE_MAP and surface JS errors, dead links, infinite skeletons (≥2000ms), `/undefined` URLs, raw-UUID renders. Acceptance "0 ER-P0/P1" not verified in-session — needs user-driven run. 2026-06-03 attempt #2: `bun infra:up` failed with port conflict — `fb-*` containers (foodbeam? unknown user project) hold 5432/9000/3030/3100/1025/8025, same ports `docker-compose.yml` requests. Memberry's `memberry-stripe-mock` came up on 12111 (no conflict), `memberry-postgres` partially created then blocked at MinIO port 9000. Did NOT stop fb-* (irreversible on other user project — destructive-action policy applies). Tore down partial memberry stack via `docker compose down` (fb-* intact). Unblock paths: (a) user manually `docker stop fb-grafana fb-loki fb-postgres fb-minio fb-mailpit` then re-run; (b) reconfigure memberry `docker-compose.yml` to use non-conflicting ports; (c) point memberry at the running fb-postgres if schema/role-compatible (risky — could clobber fb data) [evidenced — files committed, typecheck clean, infra probe documented this run] | apps/memberry/tests/e2e/oli-runtime-loop.spec.ts (committed); RUNTIME_EXEC_REPORT.md 2026-05-31 snapshot still valid (no Wave-61 regression evidence); `docker ps` 2026-06-03 16:xx showing fb-* port hold | Run path (after fb-* stop): `bun infra:up && (cd services/api-ts && bun db:seed) && (cd services/api-ts && bun dev) & (cd apps/memberry && bun dev) & sleep 8 && cd apps/memberry && bun run test:e2e oli-runtime-loop.spec.ts`. To exercise Wave-61 POST surfaces (credits/adjust, survey re-edit, poll inline) author data-testids on the form openers + surfaces, then populate `dataSurfaces[]`. Tier-3 page-load coverage already proves the GET routes don't 401/throw. **Skill enhancement**: `oli-check.md` Step 3.5 should detect port conflict during `--auto` (`docker compose up -d --wait` exit 1 + "port is already allocated") and emit a TOOLCHAIN-GATED warning with the conflict source rather than silently aborting. | 2026-06-03 | 2026-06-03 | 3 ◆ |
| engine-field-gap | RESOLVED 2026-06-03 P3-17 collateral land. api-usage matcher previously did exact string match on `{METHOD} {path}`, so FE template literal `${orgId}` (→ `:orgId`) didn't match BE Hono route `:organizationId`. Added param-anonymization fallback in `~/Desktop/oli-engine/src/passes/api-usage.ts`: when exact key miss, normalize all `:foo` / `*` segments to `:_p` and retry against a pre-built normalized lookup. If hit, attach FE consumer to BE endpoint (no phantom). Memberry rescan: phantoms 60 → 20 (-67%, 40 phantoms absorbed). Residual 20 split: (a) ~6 missing-BE-route real findings (`/persons/me` GET bare, `/comms/messages/search`, `/communications/subscriptions/person`, `/events/my`, `/training/my`, `/notifications/my`, `/communications/announcements` bare list) — backlog as BE work; (b) ~3 FE-route-not-API confusion (`/association/member/credits`, `/chapters`, `/professional-licenses` are TanStack routes, not fetches — engine extraction false positive); (c) ~5 extraction artifacts (`/public/orgs*`, `/verify/*`, `/communications/templates/:edit` where `:edit` is TanStack search param) — engine polish bucket; (d) ~6 actual drift (`/surveys` vs `/surveys/`, `/public/verify` vs `/certificates/verify`, dues-metrics + dues-member-summary not yet implemented) [evidenced — pre/post phantom count + endpoint dumps] | CODE_API_SURFACE.json: 60 → 20 phantoms; engine commit 7b2a640; memberry rescan with engine 7b2a640 | DONE — backlog (b) as engine extraction polish (distinguish route paths from fetch calls); backlog (a) + (d) as BE/FE missing-route work; (c) is small per-pattern engine fixes | 2026-06-03 | 2026-06-03 | 6 (closed) |
| engine-field-gap | RESOLVED 2026-06-03 P3-18 collateral land. DOMAIN_MODEL.md §13 `Enum-Only Status Fields` table expanded from 8 → 62 rows. 54 status enums shipped in code (`accredited_provider_status`, `affiliation_status`, `announcement_status`, `application_status`, `assessment_status`, ... `webhook_retry_status`) now documented with entity name, snake_case enum, value list, and schema file path. Engine's two-parser logic (`**Enum:** \`name\`` markers + scoped `### Enum-Only Status Fields` table — see `~/Desktop/oli-engine/src/passes/statemachines.ts`) absorbed all 54 cleanly on rescan: `spec_comparison.matched` 11 → 65, `code_only` 57 → 3 (residual: `fsm:detail-tab` + `fsm:sort-by` are UI sort/tab enums intentionally skipped; `fsm:billing-frequency` row landed but engine didn't match — likely non-`_status` suffix edge case, single-row regression worth a follow-up). SM unverified bucket projected ~57→3 (~95% drop). 3 enums omitted by design: `detail-tab`, `sort-by` (UI), `template-status` (duplicate of `template_status` already covered) [evidenced — engine rescan output + `spec_comparison` json] | CODE_STATE_MACHINES.spec_comparison.matched 65 / code_only 3; DOMAIN_MODEL.md lines 1823-1877; engine commit a12a52e | DONE — backlog the `billing_frequency` `_status`-suffix matcher edge as a P3 engine polish; consider auto-emit of a code_only list as a `/oli check --suggest-domain-doc` quick-fix scaffolder | 2026-06-03 | 2026-06-03 | 5 (closed) |
| toolchain-gated | ◆ tier-3 live scaffold INCOMPLETE at HEAD `343fcf05` — `oli-runtime-loop.spec.ts` (14.8KB, mtime Jun 3 12:59) present BUT siblings `apps/memberry/tests/e2e/.auth.ts` AND `apps/memberry/oli-runtime.config.ts` MISSING. Likely cleanup between row-41 successful attempt and current HEAD removed them. Spec imports would unresolve → playwright would fail fast with import error (not real runtime exec). 2026-06-03T20:30 `--runtime --live` invocation correctly ABORTED at pre-flight (Reason 2; Reason 1 fb-* containers also active with 25min uptime per row 42 recurring). No destructive action, no false-PASS, no overwrite of existing tier-3 promoted evidence [evidenced this run — pre-flight subagent ab3f043d output] | ABORT log: `ls apps/memberry/tests/e2e/.auth.ts apps/memberry/oli-runtime.config.ts` → ENOENT both | regenerate scaffold via `/oli-runtime-plan --scaffold` OR find deletion commit via `git log --diff-filter=D --name-only -- apps/memberry/tests/e2e/.auth.ts apps/memberry/oli-runtime.config.ts` and revert; aggregator should grep for spec's import-resolution prerequisites at pre-flight (not just spec file existence) | 2026-06-03 | 2026-06-03 | 1 ◆ |
| dimension-coverage-gap | ◆ Traceability dim returned 20 P1 this cycle (was 0 prior — 2026-06-03T20:45 `--regenerate-dim-reports --auto`) — **17 of 20 are detection-surface expansion** (16 phantom-class TR-FE-PHANTOM-RES-NN + 1 ZA-01 re-detected), driven by engine `7b2a640` param-anon fallback unmasking pre-existing FE↔spec drift. Project state did not regress; checker sight improved. Workflow GATE rule "FAIL on any P0/P1" applied strictly → GATE: FAIL despite zero project regression. Memberry's prior PASS convention treated KNOWN-future P1s (`EM-M19-future01`) as non-blocking but workflow doesn't formalize this distinction. Aggregator can't currently distinguish "regression P1" from "newly-surfaced pre-existing P1" from "carried-known-future P1" — all three roll up identically to GATE FAIL [evidenced this run — Traceability REPORT 20 P1 with 16 explicitly tagged `TR-FE-PHANTOM-RES-NN` post-engine-rescan] | TRACE_REPORT.md 20 P1 with composition breakdown; CHECK_SUMMARY 2026-06-03T20:45 §Overall §Project-policy alternative section | Add finding-level `surface_class: regression \| pre-existing-unmasked \| known-future` field at dim emit-time; aggregator's GATE roll-up should default to FAIL on `regression` only, WARN-WITH-REAL-FINDINGS on `pre-existing-unmasked` (project state stable, fix required but not gate-blocking on the cycle that surfaces them), and ignore `known-future` per project policy; allow `--strict` to escalate all three to FAIL for CI | 2026-06-03 | 2026-06-03 | 1 ◆ |
| dimension-coverage-gap | ◆ `--regenerate-dim-reports --auto` parallel dispatch pattern proved viable: 7 background subagents + 2 light-touch (Discovery MD refresh, prior tier-3 evidence promotion for Runtime) completed in ~10min wall-time vs workflow's stated "Sequential, ~5-15 min total" expectation. No file-write race observed (different REPORTs = different paths). Workflow's "serialize the dim list" rule is over-strict for the cross-dim-REPORT case — it's only needed for shared-engine `--write` invocations. Discovery sub-agent correctly handled the engine `render-markdown` verb-absence by inline-rewriting frontmatter from current JSON. Tier-3 promoted from commit 343fcf05 evidence rather than re-running live (closes row 18 remediation b) [evidenced this run — all 9 dim REPORTs landed with `based-on: map@2331bd9f` in single cycle] | CHECK_SUMMARY 2026-06-03T20:45 §Dimension Results all current; 9 background subagent completions logged | UPDATE workflow R1-regen rule: parallel-dispatch is safe when (a) each dim writes to its own non-overlapping REPORT path, AND (b) no dim calls `oli-engine scan --write` (engine writes are the only shared-state risk). Aggregator should detect overlapping write paths upfront and only serialize the conflicting subset. Closes the "5-15 min sequential" expectation as obsolete for the parallel path. **CONFIRMED 2026-06-03T17:30** — 2nd cycle (10 dims, ~10min wall, zero file-write race, all dim REPORTs landed with `based-on: map@3f0dae76` in single cycle) | 2026-06-03 | 2026-06-03 | 2 ◆ |
| engine-field-gap | ◆ engine v6 `tanstack-mutations` pass does NOT scan FE fetch sites in `apps/*/src/features/**` outside the route file tree. Post-commit `82022bb1` engine rescan reported `phantoms=0` despite 5 Bucket B FE→spec drift sites STILL present (grep-verified: `apps/memberry/src/features/{comms,directory,routes/_authenticated/.../officer/finances}/**`). Engine effectively under-reports phantoms when consumer is a leaf component, not a route module. Manual classification via PHANTOM_TRIAGE.md carried Bucket B as P1 this cycle [evidenced this run — `phantoms=0` from engine vs grep of 5 Bucket B paths in `apps/memberry`] | `node ~/Desktop/oli-engine/dist/cli.js scan . --write` output `phantoms=0`; `grep -rn 'comms/messages/search\|association/member/credits?personId\|...' apps/memberry/src` returns 6 hits | extend engine `tanstack-mutations` + `api-usage` passes to recursively walk `features/**`, `components/**`, and any non-route TSX importing `api.{verb}` / `useQuery` / `useMutation` with literal/template-literal URLs; OR add a config knob `include_non_route_consumers: true`. Until then aggregator must cross-check engine `phantoms[]` against a grep-based FE-call inventory before clearing residual phantom-class findings | 2026-06-03 | 2026-06-03 | 1 ◆ |
| dimension-coverage-gap | ◆ traceability P1 count dropped 16→8 actionable this run (target hit per commit `82022bb1`); WF-U1 carried roadmap-defer is non-actionable and inflated raw count to 9. Aggregator emits both counts ("HIGH gaps (P1)" raw + "actionable") but workflow GATE rule still rolls up on raw P1, so a 9 P1 raw count would FAIL despite the actionable target being met. Same `surface_class` framing as row 46 — roadmap-defer is a 4th class beyond regression/pre-existing/known-future [evidenced this run — TRACE_REPORT.md rev 7 §Summary dual count] | TRACE_REPORT.md rev 7 P1=9 raw / 8 actionable; rev 6 had P1=20 with WF-U1 already carried | extend row 46's `surface_class` field with `roadmap-deferred` value; aggregator should exclude `roadmap-deferred` from GATE roll-up by default (same treatment as known-future); allow `--strict` to include | 2026-06-03 | 2026-06-03 | 1 ◆ |

| dimension-coverage-gap | ◆ Phase-D Tier-D rebaseline (this cycle): UI-C P1 92→61 (additional 31 cleared after Tier-C's 209) via 134-route PageShell sweep + hex cleanup + multi-persona e2e auth scaffold. 0 NEW-DEBT, 0 regression — UI-C verdict promoted WARN→PASS. Notably: Button override count appeared to "regress" 3→26 via broader multi-line grep methodology — actually CARRIED pre-existing debt (pre-Tier-C narrow grep missed multi-line JSX). Lesson: rebaseline-time methodology audits MUST distinguish methodology-drift (re-classifies CARRIED debt) from genuine regression (NEW-DEBT introduced by code change). Methodology now matches engine component-detection pipeline going forward [evidenced this rebaseline — phase-d-rebaseline-004 v55 notes] | UI_CONSISTENCY_REPORT.md rebaseline-004 §Notes; baseline.json v55 phase-d-rebaseline-004 history entry | ENCODE in ui-consistency.md dim: at every rebaseline, run BOTH narrow + broad grep methodologies side-by-side and reconcile any delta as CARRIED vs NEW-DEBT before pinning. Pin the broader (engine-aligned) method as canonical. | 2026-06-03 | 2026-06-03 | 1 ◆ |
| sweep-pattern | ◆ Tier-D PageShell sweep validated 134-route parallel-agent migration scaled cleanly: 191 file modifications across admin/memberry routes + ~50 feature files + 3 primitive holds + e2e scaffold, typecheck PASS all 5 packages, zero file-write race. Reinforces row 19's parallel-subagent refactor pattern. Coverage 78%→82.4%; remaining 26 routes are INTENTIONAL-EXEMPT special-layout (auth/landing/full-height/public verify) — not gate-blocking. Tier-E candidate work: 26 CARRIED Button overrides in feature components (application-list.tsx et al.) [evidenced — git status 191 modifications + spot-check 8/8 routes show real <PageShell> usage 3-7 occurrences each] | git status; UI_CONSISTENCY_REPORT.md §Residual P1 Classification | DONE — pattern is mature; record Tier-E candidate scope (26 Button overrides) for next sweep wave when scheduled. | 2026-06-03 | 2026-06-03 | 1 ◆ |
| convergence-loop | ◆ Phase-D Tier-E convergence (this rebaseline): proved nothing is left via a bounded 3-iter loop with scope-frozen detectors. PATTERNS.lock.md serves as the single source of truth for detector regex + exemption-annotation syntax + INTENTIONAL-EXEMPT closed list. Iter 1 detected 6 unannotated Button + 2 unannotated PageShell residuals (sites inside `&&(` and `?(` ternary parens where my first-pass `{/* */}` JSX-comment insertion failed because JSX-comment is not valid inside expression-position parens; JS line-comment `// ui-c-exempt:` IS valid as expression-position prefix). Iter 2 converged at 93/93 annotated, 0 unannotated. Critical learnings: (1) `// ui-c-exempt:` JS line-comment is the SAFE annotation form — works in JSX-child + JSX-expression-position alike, unlike `{/* */}` which only works as JSX-child; (2) JSX-child `// comment` (line-comment placed between two JSX elements) IS NOT valid — TypeScript renders it as JSX-text content; must be replaced or hoisted to JSX-expression scope; (3) scope-freeze discipline matters: 2 new PageShell-missing routes (org/$slug, events/$eventSlug) carried legacy `// oli-ui: exempt-pageshell` annotations from prior cycles — renamed to canonical `ui-c-exempt:` form to participate in the locked detector | docs/audits/PATTERNS.lock.md; scripts/ui-consistency-detect.ts; baseline.json v56 phase-d-rebaseline-005; convergence iter 2 trace at /tmp/converge-iter2.json | ENCODE in ui-consistency.md dim: prefer `// ui-c-exempt:` (JS line-comment) over `{/* ui-c-exempt: */}` (JSX-comment) because the JS form is position-agnostic. Add the bounded convergence loop (max 3 iters → ACCEPTED-FLOOR) as the canonical methodology for any future rebaseline. | 2026-06-03 | 2026-06-03 | 1 ◆ |
| ratchet-hardening | ◆ Phase-D Tier-E pre-commit hook lands (scripts/ui-consistency-check.sh + scripts/ui-consistency-detect.ts wired into .husky/pre-commit). Detector reads PATTERNS.lock.md detectors; checks staged .tsx files for NEW unannotated violations; exits non-zero on any new finding. Implementation gotcha: Bun crashes (SIGSEGV) when given long argv (full repo .tsx file list). Workaround: read newline-separated file list from stdin via `--stdin` flag; check.sh pipes the staged-file list via `echo "$FILES" | bun run detect.ts --stdin`. Validates end-to-end (exit 0 on clean tree post-converge). Hardens the ratchet from "documented floor" to "actively enforced floor" — first cycle since genesis-001 where the UI-C ratchet has teeth beyond doc-staleness | .husky/pre-commit; scripts/ui-consistency-check.sh; scripts/ui-consistency-detect.ts; baseline.json v56 wave62 summary | DONE — pattern documented. Future dims (e.g., consistency, runtime) should consider similar pre-commit detectors to actively enforce respective floors. | 2026-06-03 | 2026-06-03 | 1 ◆ |
| dimension-coverage-gap | ◆ NEW 2026-06-04: UI-C dim is BLIND to test-time DOM-contract regressions caused by primitive refactors. Commit `9fbcb497` (RoundActionButton primitive — codifies 32/48/56px round controls) PASSED UI-C (0 detector matches, 8 clean adoption sites, annotations exact-match v58) but BROKE 56 memberry FE component tests (CNF-P0-001) because the primitive's emitted DOM (aria-label from `label` prop, no `role="button"` when `asChild`) differs from prior inline JSX; dependent test fixtures querying by `getByRole('button', { name: ... })` no longer resolve. Cross-dim coupling: UI-C checks design tokens + class-name shape; Confidence checks runtime DOM via vitest assertions. ONLY Confidence catches this class. Pre-commit ratchet (`.husky/pre-commit ui-consistency-check.sh`) blocks NEW UI-C violations but does NOT run vitest — primitive refactor sailed through. Workflow Step 3.5 also doesn't trigger vitest on primitive-file touch [evidenced this run — UI-C verdict PASS + Confidence verdict WARN with regression class on same commit] | CHECK_SUMMARY 2026-06-04 §Overall "Regression" note; commit 9fbcb497 diff vs apps/memberry/src/features/*/components/__tests__/*.test.tsx 56 failures | (a) extend `.husky/pre-commit` to detect changes to files matching `packages/ui/src/components/*.tsx` or `apps/*/src/components/patterns/*.tsx` and trigger affected-test re-run via `bun test --changed` (vitest dependency graph); (b) OR add a UI-C dim sub-check: "primitive-refactor regression scan" — when a `@monobase/ui` export's DOM shape changes (snapshot delta), grep call sites for `getByRole|getByLabelText|getByText` references and FLAG as required-test-update; (c) document the cross-dim coupling explicitly in dim docs: UI-C scope = static structural conformance only; runtime DOM contract = Confidence's domain. | 2026-06-04 | 2026-06-04 | 1 ◆ |
| dimension-coverage-gap | ◆ NEW 2026-06-04: aggregator selective-regen logic ran by manual judgment (Claude inspected `git diff --name-only PRIOR..HEAD` per-dim-input-set, decided 4 regen + 6 carry). Worked correctly this cycle (caught CNF-P0-001) but is not encoded. Future runs of `--auto` without manual cognition would either (a) re-run all 10 dims (expensive, slow), or (b) blanket carry (would have missed CNF-P0-001 entirely — this cycle's near-miss). Selection rule must be made first-class. Per-dim input manifest needed: e.g. Consistency input = `docs/product/**/*.md`; Confidence input = `apps/**/*.test.{ts,tsx} + services/**/*.test.ts + bunfig.toml + test-setup-root.ts + packages/vitest-test-shim/**`; UI-C input = `apps/**/*.tsx + packages/ui/**/*.tsx + PATTERNS.lock.md + scripts/ui-consistency-detect.ts`; etc. Aggregator compares manifest-sha against prior REPORT `based-on:`; mismatch → regen. Closes row 38 second-half cleanly [reflective from this run's manual selection] | this run's aggregator selection commentary in CHECK_SUMMARY §Run Context | spec a `dim_inputs:` field on each dim module (list of glob patterns); aggregator builds per-dim sha from listed files; carry-vs-regen becomes deterministic. | 2026-06-04 | 2026-06-04 | 1 ◆ |
| low-confidence-heuristic | ◆ NEW 2026-06-04: Confidence subagent attributed CNF-P0-001 to commit `9fbcb497` (RoundActionButton primitive refactor) — 3 root causes claimed (DOM contract drift, ApiError export missing, OrgProvider mock latency). First-principles investigation proved ALL 3 partly or wholly false: (a) `ApiError` already exported at `apps/memberry/src/lib/api.ts:76`, identical to prior cycle — fabricated finding; (b) actual root of 3 formatCents tests was a leaked `vi.mock('@/features/dues/lib/money', ...)` from sibling `arrears-breakdown.test.tsx` (Bun mock.module global + shim `v.unmock` no-op) — not RoundActionButton-related; (c) prior cycle 6048 tests vs current 6768 = 720 newly-executed tests added by test-infra fix `082557f4`, meaning 56 fails are pre-existing-unmasked (test-scope expansion), not regression from `9fbcb497`. Subagent diagnosed by pattern-match on commit messages instead of running each failure to ground. Result: CNF-P0-001 mis-classified as `regression` → GATE: FAIL. Correct surface_class is `pre-existing-unmasked` → GATE: WARN-WITH-REAL-FINDINGS per row 49 rule [evidenced this run — git history check on api.ts + test count delta math] | CHECK_SUMMARY 2026-06-04 §GATE VERDICT reclassification block | dim subagents (especially Confidence) MUST verify failure attribution against git history: for each "introduced by commit X" claim, `git show X -- <failing-file>` to confirm the change touches that file's contract. Fabricated/transposed root-cause claims tank gate accuracy. Aggregator should add a "spot-check 1 driver per dim" pass before emitting GATE roll-up. | 2026-06-04 | 2026-06-04 | 1 ◆ |
| dimension-coverage-gap | ◆ NEW 2026-06-04: test-scope expansion class (082557f4 routed apps/{admin,memberry} test scripts through root bun:test preload — added 720 newly-executed tests). Pre-existing-unmasked tests surfaced in bulk. Per-test-file repair across 19 component files + provider/hook infrastructure is NOT a single-commit "quick fix" — must be split into per-module vertical-TDD slices (m06/m07/m08/m09/m11/m12 + apps/memberry/src/test infrastructure). Confidence dim should EMIT this class explicitly so the aggregator can roll up "X tests newly-included this cycle, Y of them currently fail" as a separate signal from regression-class fails [evidenced this run] | this run's R1 commit `c7fad68d` + R2/R3 deferral | (a) Confidence dim adds `newly_executed: N (count of tests not present in prior cycle's run-list)` field at emit-time; (b) GATE roll-up downweights newly-executed fails to `pre-existing-unmasked` by default; (c) `/oli-check --reverify` mode should cross-check prior cycle's test-execution-list against current — surface delta as "scope expansion". | 2026-06-04 | 2026-06-04 | 1 ◆ |
